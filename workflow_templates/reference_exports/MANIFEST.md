# Reference API Exports

These files are immutable application-side copies of user-exported ComfyUI API graphs. The creator originals remain under `J:\Hermes-Remote\comfyui\workflowsbackup\API-Workflows`.

They are **reference evidence**, not graphs submitted directly by Music Video Producer. Some contain creator-specific media paths, multiple active output branches, or incomplete virtual/shared loader wiring. Runtime adapters in `src/music_video_producer/workflows.py` expose only reviewed semantic controls.

**One deliberate exception to byte-fidelity.** The two `songplanner-*` exports as originally exported carried third-party copyrighted lyrics (W.A.S.P., "Harder Faster") in `55.idea` and `63.text`. Those two text fields — and only those two — were replaced with neutral placeholder prose on 2026-08-17, and the SHA-256 values below are of the replaced files. Every node id, `class_type`, link and model reference is byte-identical to the creator's export, verified field by field, which is what these files are evidence *of*: the adapters are built from the graph topology, never from the Director-supplied text. The creator originals on the ComfyUI machine are unchanged.

| File | SHA-256 | Audit status |
|---|---|---|
| `flux-user-export.json` | `0ed08fd477785d1e6661b4ca345a294cddb53074229521483f94837750c70f01` | 15 nodes; complete; matches Flux adapter with optional LoRA loader deliberately omitted |
| `h3-director-user-export.json` | `fd270d7f71744f39115881dfb2693b1f0c7bd5f424cc0dfd34f5703f42d023ed` | 12 nodes; API format, but required CLIP/video-VAE/audio-VAE links were omitted by virtual editor wiring; corrected by explicit runtime adapter |
| `h3-ltx23-user-export.json` | `d4027e76b2d1d3a7bd070505f2f493e275bcd519f9f6a5d0c692004e70890eca` | 56 nodes; all server node classes registered; combined creator path retained for reference only |
| `h3-ltx25-user-export.json` | `fe009123bf7e8c85841a8263cae7f85551ba44423b57423b309b8f6aa93909b4` | 65 nodes; all server node classes registered; contains hard-coded media and the diagnosed SeedVR2→LTX dimension boundary |
| `h3-ultra-references-user-export.json` | `bbcd1bcd1df6f7d826ccf439ce01b8817a517c171a2a72b0e83c1144e196ff46` | 29 nodes; all classes registered; media loader supports 9 pictures, 3 videos with paired audio, and 3 standalone audios; adapted as an explicit 18-node Ultra reference stage |
| `songplanner-invented-user-export.json` | `fb26b3720c47918e14b15b7d55583454b338fa24d31b54414a8b1ab9fbef1420` | 17 nodes; complete API format; adapted as an explicit 10-node invented-lyrics stage dropping the PreviewAny/CR Text UI nodes, the SeedNode indirection, and the dead tiled-decode branch; the adapter saves FLAC (lossless master, matching the live-verified direct adapter) where the export saved mp3/V0 |
| `songplanner-known-lyrics-user-export.json` | `2df49d8ce9a72f4532e26f645af04e4706cc03c5cfeba3d62aa41447524a6659` | 17 nodes; complete API format; node-by-node diff against `songplanner-invented-user-export.json` confirmed 2026-08-16 that the only differences are node `45.lyrics` and preview-node `58.source`, both rewired from planner output `["55", 1]` to the CR Text lyric sheet `["63", 0]`; adapted by the same 10-node core with the lyric sheet passed as a literal to node 45 |

| `h3-references2v-user-export.json` | `2cd52d29d34dda10845fb0b7d52bcab724983a30fb9b78aca0de3149526747de` | 29 nodes, 25 classes; the canonical standalone reference-to-video graph. **Corroborates the `default` sampling profile exactly** — `simple`/20/`res_multistep`, a `Power Lora Loader` carrying no LoRA at all, and `shift_audio: 3`, which independently confirms the adapter's choice of `3` over the LTX pipeline's `6` |
| `h3-turbo-references2v-user-export.json` | `1c08fdfbe36b59fa3fa84389e084d35382011181088bf451ab6b54ff6b25d928` | 29 nodes, 25 classes; the canonical turbo reference-to-video graph. **A different bundle from the shipped `turbo` profile**: `minimax_h3_turbo_v4_step600_ema_pruned_comfyui.safetensors` at strength **1.0** with `simple`/8/`euler`, where the shipped profile takes `minimax_h3_ref2v_turbo_4step_v0.1_comfyui_bf16.safetensors` at 0.7 with `beta`/4/`euler` from the LTX music-video pipeline. Both LoRAs are installed; both bundles are evidenced; only the shipped one has been rendered from this application |
| `ltx25-audioreplacer-user-export.json` | `9cf755fe83588b6f15fc8c363bf363150fb1f6f5051df2adb482e22e4d4ebb5d` | 8 nodes, 7 classes, **only 3 reachable** from its single `VHS_VideoCombine` (`LoadAudio`, `VHS_LoadVideo`, the saver) — **5 orphans**, the worst ratio in this directory and the reason the caution below exists. The reachable subgraph names **no model file at all**, so the correct dependency list for an adapter built from it is empty, while the five orphans (a `UNETLoader`, two `VAELoaderKJ`, a `CLIPLoader`, a `LatentUpscaleModelLoader`) name a 22B transformer, a video VAE, an audio VAE, a Gemma text encoder and a spatial upscaler. Adapted as `workflows.build_audio_replace_payload` with three departures, each verified against live `/object_info` on 2026-08-18 rather than assumed: `VHS_LoadVideo` → `VHS_LoadVideoPath` (the combo enumerates ComfyUI's *input* directory, which this installation publishes empty, while a take lives under *output*); `LoadAudio` → `VHS_LoadAudio` (the same trap, confirmed — that combo's only option is the one file in the input directory, and the master song lives under the application's data root on another drive; `VHS_LoadAudio` is also the only one of the two that can express a window, through `seek_seconds`/`duration`); and **`format: "LTXV"` → `"None"` on the loader**, because the schema defines LTXV as `{"target_rate": 24, "dim": [32, 0, 768, 512], "frames": [8, 1]}` and an H3 take lands on the 17k+5 grid — 90 frames would be conformed to 89 and desync the restored song. The export's saver also hardcodes `frame_rate: 25`; the adapter reads the take's own rate through an added `VHS_VideoInfo` |
| `ltx25-enhancer-user-export.json` | `6f6c70102b8b8e244847518cdbe4a90881dc4782cfbd6b3e2b03eaca4b66fd37` | 20 nodes, 19 classes; the standalone LTX 2.5 enhancement graph that takes an existing video through `VHS_LoadVideo` rather than regenerating H3 — the evidence ROADMAP item 8 was waiting for. Note `ManualSigmas` (`0.909375, 0.725, 0.421875, 0.0`), `CFGGuider` at cfg 1, an **empty** text prompt, and `ImageScaleToMaxDimension` against an `INTConstant` of 1920 rather than the SeedVR2→normalization boundary the reference chain uses |

| `ltx25-i2v-user-export.json` | `5badf3afdab51e38f4182de8690af8c1ceeb1a25834bd487f71aef1803ec5127` | 55 nodes, **all reachable**; the evidence for the `image_to_video` mode. `LTXVImgToVideoInplace` applied twice (strength 1.0 at base, 0.7 after upsample); unlike the enhancer, the detailer LoRA **and** the latent spatial upscaler are in the executed path here |
| `h3-first-last-user-export.json` | `6e2407da979c5dd63a0e7e4c556e77cc1313c5afd10718076f977916034b1017` | 28 nodes, 27 reachable; the evidence for the `first_last` mode. **Loads a different checkpoint from the reference graphs**: the reachable UNET is `minimax_h3_fl2va_pruned_int8_convrot.safetensors` — a dedicated first/last model — and the single orphan is the inherited `ref2va` loader. Same default bundle as References2V: `simple`/20/`res_multistep`, an empty `Power Lora Loader`, `shift_audio: 3` |
| `ltx25-fml-user-export.json` | `664ca31ee9f5815258261fdc7c3c6726d81ea03f6d21d8525917107614e85151` | 64 nodes, **all reachable**; the evidence for `first_middle_last`. `LTXVFirstLastFrameControl_TTP` at two stages (1.0 then 0.5) plus `LTXVMiddleFrame_TTP` with **position as a parameter** (`0.51`, strength 0.35) |
| `ltx25-videoextender-user-export.json` | `d73e8713c40d46473dfb29928dd471f0f30cc33d078ad38d5375d54020a1b218` | 61 nodes, **all reachable**; the evidence for `extend`. `LTXVAudioVideoMask` with `max_length: "pad"`, `existing_mask_mode: "add"` |
| `ltx25-videoextender-noaudio-user-export.json` | `22f3463edce14acacb35ada20b752f655e73a9e4877790967860b019190d5c17` | 65 nodes, **54 reachable, 11 orphans** — the audio branch (`LTXVAudioVAEDecode`, `TrimAudioDuration`, calculators) left in place but disconnected. An adapter must derive from the reachable subgraph, per the standing caution below |
| `h3-image-edit-user-export.json` | `92dfef429f23d9816b202c0aab64f86261018426bddc693c0a85ca29fcb01646` | 27 nodes, **24 reachable**; the evidence for AI Mod (added 2026-08-19 from `MiniMaxH3 ImageEditing - API.json`). Image editing **is a `length: 5` reference render**: `MiniMaxH3ReferenceToVideo` conditioned with the dedicated **image VAE** (`minimax_h3_t1_image_vae_step1597`) in the conditioner's `vae` seat *and* as the decoder, sampling into an `EmptyMiniMaxH3ImageLatentAV`, saved by `SaveImage`. The base picture passes through `ImageScaleToTotalPixelsX` (2 MP, ×32, crop, lanczos) into `ref_image_0`; pictures 1–8 ride the splitter unscaled. The three orphans are the mirror image of the keyframe export's: the Image Comparer (UI), the video VAE loader, and **`fl2va`** — here `ref2va` is the reachable UNET. **No `MiniMaxH3SigmaShift` and no preview node exist in this graph.** The Any Switch pairs resolve to their `INTConstant` arms at runtime (rgthree first-non-null): canvas 1920×1080. Default bundle: `simple`/20/`res_multistep`, empty Power Lora. The editor-format twin carries the creator's image-edit prompting guide (subject definitions, `[reference generation]` summary, `attribute_transfer`/`fully_preserved` retention, `Silence`/`N/A` sound fields) — referenced by `workflows.image_edit_prompt`, never copied wholesale |
| `h3-turbo-image-edit-user-export.json` | `db654d0fca3571520e7b9eb39ce1eb4e39aa038030bb8c553b9760edaa7f0d62` | 27 nodes, 24 reachable; the turbo twin, identical graph except the bundle: `minimax_h3_turbo_v4_step600_ema_pruned_comfyui.safetensors` at strength **1.0** with **`beta`/8/`euler`** — note the scheduler differs from the turbo References2V export's `simple`/8/`euler` with the *same* LoRA. Same class, different graphs, different scheduler: bundles are evidenced per export, never borrowed across |

Imported: 2026-08-16; the four exports above them added 2026-08-18 from the Director's per-mode API exports. The five mode exports were added later the same day because the sixty-second gap analysis found the four unrenderable `ShotMode`s had evidence on the creator machine that had never been brought in as auditable evidence — an adapter built from an uncommitted file on another drive is an adapter whose evidence can drift. A cross-graph note worth keeping: in the I2V and FML graphs the detailer LoRA and the latent spatial upscaler are **in** the executed path, while in the enhancer the upscaler is orphaned — same class, different graphs, opposite reachability. **Reachability is per file, never per class.**

**One file here is a saved editor graph, not an API export.**

| File | SHA-256 | Audit status |
|---|---|---|
| `krea2-charactersheet-user-export.json` | `ac36130b6be2caa4b624cf2b4739a142c3150a2f7001b7dcba49ee0a99bee399` | 45 nodes; **saved editor format** (`nodes`/`links`/`groups`, with a `mode` on every node) copied byte-identically from `J:\Hermes-Remote\comfyui\workflowsbackup\MusicVideo Pipeline (Advance)\Krea2_CharacterSheet-20260814.json`. Not API format and **never submittable** — it is here because the question it answers cannot be answered by an API export, which carries no `mode` field. Three `KSampler`s: node 53 is `mode: 0` and is the whole of the sheet path; nodes 146 and 172 are `mode: 4` (bypassed) and belong to an optional character-*portrait* generator in a group titled `2nd Sampling` at x ≈ -1129, while the `CharSheet-*` groups sit at positive x. **`Music-Video.md` beside it is wrong about this graph** and should not be built from: it calls the sheet "Three KSamplers", reads node 172's `denoise` 0.3 as CFG (widget order is seed, control, steps, cfg, sampler_name, scheduler, denoise; all three samplers are cfg 1.0), calls `res_multistep` the final pass when node 146 is the first of the bypassed pair, and states the QuadView LoRA "forces the 4-panel layout" when a 2026-08-18 probe asked for four views and got six. Its Resolution note says 1 MP is the sweet spot while the file's own `EmptySD3LatentImage` is 1536×1024 (1.57 MP), which is what the adapter emits |

Added 2026-08-18, as the evidence for the single-pass pin in `tests/test_workflows.py`.

**A caution about the wider 2026-08-17 export set.** The Director separated the nested subgraphs into per-mode API graphs under `J:\Hermes-Remote\comfyui\workflowsbackup\API-Workflows`. Several of the smaller ones carry **orphaned loader nodes** — `LTX2.5 AudioReplacer` has 8 nodes of which only 3 (`LoadAudio`, `VHS_LoadVideo`, `VHS_VideoCombine`) are reachable from its output, and `LTX2.5 VideoSlicer` has 9 of which 3 are reachable. The rest are UNET/VAE/CLIP loaders inherited from the parent graph. ComfyUI will not execute them, but any adapter built from these files must be derived from the *reachable* subgraph rather than from the node list, or it will declare model dependencies the task does not need.

The AudioReplacer half of that caution is no longer hypothetical: it was imported above on 2026-08-18 and its adapter is the first in this project whose correct dependency list is **empty**. `tests/preflight_audio_replace.py` inverts the usual model-file check for exactly that reason — a single model filename in that payload is a failure, because every one that could appear would have come from an orphan. `LTX2.5 VideoSlicer` remains unimported and unadapted.

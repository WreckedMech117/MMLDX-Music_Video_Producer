.venv/
.env
__pycache__/
.pytest_cache/
.ruff_cache/
*.py[cod]
data/projects/
# Generated on first use by `effects.write_default_luts`, and a place the Director drops
# their own colour work. Neither belongs in the repository: the five defaults are ~5 MB of
# arithmetic that regenerates in a fifth of a second, and nobody else's LUTs may be
# committed at all.
data/luts/
!data/.gitkeep
.DS_Store
Thumbs.db
test-artifacts/
# Planning artifacts (briefs, PRDs) are deliverables and stay versioned;
# anything else BMAD writes under _bmad-output is transient -- except the
# deferred-work ledger, which is the standing record of known-unfixed issues.
# A per-story spec is spent once its story lands; that ledger only accumulates,
# and leaving it untracked meant every deferral was lost on a fresh clone.
_bmad-output/*
!_bmad-output/planning-artifacts/
!_bmad-output/implementation-artifacts/
_bmad-output/implementation-artifacts/*
!_bmad-output/implementation-artifacts/deferred-work.md

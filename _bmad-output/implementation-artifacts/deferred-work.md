# Deferred Work

Everything above `## Resolved` is **open**. Everything below it has shipped and carries a `resolved:` line saying where.

**A new entry goes at the end of the open section, not at the end of the file** (corrected 2026-08-30). Appending to the file put fourteen open gaps -- three from Epic 11, six from Epic 10, two from Epic 8 and three older -- underneath a heading that says they shipped, and the entries themselves were correct the whole time: the lie was **positional**, which is the one kind no reader of an individual entry can catch. `tests/test_stale_claims.py` now fails if an entry under `## Resolved` has no `resolved:` line, so the next append lands loudly rather than quietly. The guard is deliberately one-directional -- a shipped entry annotated with its `resolved:` line where it was written, above the heading, over-reports open work and misleads nobody.

- source_spec: `_bmad-output/implementation-artifacts/epic-9-retro-2026-08-26.md`
  summary: `preview_fingerprint` recomposes the chain internally rather than being handed the one the route already composed, so the route composes twice and the two calls could drift apart. The hazard is closed by a test rather than by structure.
  evidence: `914ae92` changed the fingerprint's fourth slot from the stored stack to the composed chain, which is the fix for a cached preview outliving a composer correction. The route composes once for rendering and `preview_fingerprint` composes again for naming, from arguments it already hashes. Two composes with different arguments would name a clip after a picture it does not hold — the exact defect just repaired — and nothing structural prevents it; `test_the_chain_the_route_renders_is_the_chain_it_names_the_clip_after` watches both name lookups and asserts the route composes exactly twice with identical arguments, and two mutations confirm it bites. The structural alternative is to pass the composed stages in and drop the recompose, which makes the drift impossible rather than detected. It was not taken for two reasons worth recording rather than inferring: at the time the work was split across two agents and the signature is pinned by a cross-language contract test in the other agent's file (`test_frontend_contract.py` asserts `"stack"` is a parameter of `preview_fingerprint`, because it holds the client's `previewInputKey` to the server's inputs), so the rename would have handed that agent a red test; and changing a function again for elegance immediately after fixing and mutation-verifying it is churn on a freshly proven path. The change is small — the parameter rename, passing `EffectStages` instead of a stack, and one line in that contract test — and is worth doing the next time this function is opened for another reason.
  rechecked: 2026-08-30 -- **restated, and the case is now stronger than when it was written.** The entry's own structural remedy -- *"pass the composed stages in and drop the recompose"* -- was built in Epic 11, as a **sibling function in the same module**: `effects.boundary_fingerprint` takes `chains: Sequence[EffectStages]`, already composed by the route, and AD-28's 2026-08-29 amendment states the reason in terms (*"Two compositions cannot disagree when there is only one"*). So `effects.py` now holds both shapes of one decision, side by side, and the older one is the one still guarded by a test rather than by structure. Both reasons the entry gave for not doing it then are spent: the work is no longer split across two agents, and the function is no longer freshly proven -- it has since been read closely enough to grow a seventh input. What has not changed is the cross-language contract test asserting `"stack"` is a parameter of `preview_fingerprint`, which still has to move in the same commit.
  resolved: **decided against, 2026-08-31** (Epic 11 retrospective item 79), and closed rather than carried — an entry nobody will act on is one nobody re-reads. Both reasons this entry gave for deferring really are spent, and Epic 11 really did build the better shape next door (`boundary_fingerprint` takes composed `chains`). **The reason not to is one this entry never considered, and it is measured rather than argued.** `test_the_monitor_decides_the_preview_clip_without_asking_the_server_for_a_stale_flag` holds an **exhaustive classification** of `preview_fingerprint`'s parameters: every one is sorted into `keyed`, `carried` or `not_keyed` with its reason, and the assertion is set equality against the live signature. Two consequences. (1) **`stack` is a *keyed* input**: the client's `previewInputKey` keys on the stack, because a browser can see a stack and cannot compose a chain — so replacing `stack` with a pre-composed `chain` breaks the contract at its root. (2) **The classification would go blind.** `envelope`, `shot_start` and `clip_seconds` are classified today as *reaches the chain*, which forces anyone adding a chain input the fingerprint needs to decide whether the client's key should follow it; collapse six parameters into one opaque `chain` and that question stops being asked. Demonstrated by mutation on 2026-08-31 rather than reasoned: adding an unclassified chain-feeding parameter is **CAUGHT**, and removing `stack` from the signature is **CAUGHT**. That guard is precisely the one nobody had when `previewInputKey` and `preview_fingerprint` disagreed for a whole epic and the Monitor showed a picture driven by a replaced song — so the refactor would remove the coverage that exists *because of* the defect it is meant to prevent a cousin of. **The asymmetry with `boundary_fingerprint` is a reason, not an inconsistency:** that function has no client-side stack to key on and its route composes each leg once, so passing chains in costs it no coverage. **What remains true** is this entry's actual concern — two compositions that could drift, held by `test_the_chain_the_route_renders_is_the_chain_it_names_the_clip_after` rather than by structure. The honest response is to keep that test strong, not to weaken the classification; if it is ever revisited, the move is to give `build_effect_stages`' own signature the same exhaustive classification first, and only then hand the chain over.

- source_spec: `_bmad-output/implementation-artifacts/spec-9-7-audit-fixes.md`
  summary: The effect clock and the picture disagree by up to half a frame at a seam. A split Shot's second clip is told it begins `clip.start - approved_start` seconds into the Shot — an un-rounded timeline number — while the picture it shows begins at `round(clip.offset * 24)` frames into the take. Sub-frame *continuity* error in `slow_zoom`'s ramp, `handheld_shake`'s phase and `grain`'s seed; not a count, a dimension or a timestamp defect, and the frame rule is untouched.
  evidence: Two numbers describe the same seam and only one of them is quantized. `assembly_plan` splits a Shot with `offset=clip.offset + (seg_start - clip.start)` (`src/music_video_producer/assembly.py`, the overlap resolution), an exact float; `trim_args` then cuts at `skip = round(offset * ASSEMBLY_FPS)`, so the picture resumes on the frame grid. The effect chain is handed `clip_offset=clip.start - clip.approved_start` (`src/music_video_producer/app.py:7686`), which is not. The divergence is exactly the residue of the split clip's take offset against the 24 fps grid, so it is bounded by half a frame (20.833 ms) and is zero whenever the seam lands on a frame. Reproduced through the real functions 2026-08-26: a Shot covering 0–8 s with a 0.25 s lead, another laid over 2.1–4.1 s, gives three clips of [50, 48, 94] frames — total 192, exactly `round(8.0*24)`, so the *count* is right — and the third clip's take offset of 4.35 s cuts at take frame 104 (4.083333 s) while `build_effect_stages` composes `min((t+4.1)/8\,1)`. Divergence 16.6667 ms, 0.4000 frame. What that is worth, as arithmetic off the composed strings: at the half-frame worst case a `slow_zoom` at 1.6 over an 8 s Shot mis-scales by 0.6 * (0.02083/8) ≈ 0.16 %, about 3 px across a 1920-wide delivery before `trunc(.../2)*2` quantizes it, and `handheld_shake`'s `sin(2*PI*2*(t+offset))` is up to 0.26 rad out of phase. `grain` is the odd one: its seed is `round(clip_offset * SEAM_SEED_PER_SECOND)`, already discrete, so the error selects a different noise field rather than a discontinuous one — the property that seed exists for (two clips of one Shot never share it) is unaffected. A Shot that is never split shows nothing at all: its single clip's clock starts at zero and runs continuously, so there is no seam for the residue to appear at. Recorded rather than fixed because the repair is a change to *what the route hands the composer* rather than to any composer — the same shape as the `clip_offset`/`shot_seconds` work Story 9.7 landed (see the resolved entry for `spec-9-c1-the-stack-on-the-shot.md` below) — and the honest fix is to hand it the number `trim_args` will actually cut at, `round(clip.offset*ASSEMBLY_FPS)/ASSEMBLY_FPS` measured from the Shot's own first rendered frame, computed once in `app.py` where both halves are in scope. The one documentation consequence is already closed: `effects.py`'s module docstring said that, given the Shot's own length, "the second clip picks the ramp up where the first one left it", and now says it does — to within half a frame — with a pointer back to this entry.

- source_spec: `_bmad-output/implementation-artifacts/spec-9-d1-the-preview-render.md`
  summary: Nothing ever sweeps the preview cache. Every distinct fingerprint leaves a clip behind, so a long grading session accumulates one file per parameter value the Director paused on, and only a manual delete reclaims them.
  evidence: The cache is keyed by the preview fingerprint so that a stale entry is inert rather than wrong, and a fingerprint moves on any of its eight inputs — including every parameter value that produced a render. Dragging one slider through twenty resting points therefore leaves twenty clips. Each is small at preview geometry (roughly 30–60 KB for a 528x304 clip of a few seconds, measured during D1), so this is an accumulation rather than a hazard, and the architecture deferred eviction explicitly rather than by omission. It is safe by construction: the cache is never an input to an export, and deleting the whole directory costs one re-render and nothing else — which is also the workaround. Recorded because "disposable by construction" and "something disposes of it" are different claims, and only the first is currently true. The natural repair is an age or count bound swept when a preview is requested, which is the one moment the project's own directory is already being touched.

- source_spec: `_bmad-output/implementation-artifacts/spec-9-c3-reorder-and-copy.md`
  summary: `models.shot_label` numbers a Shot by its position in the manifest while the timeline numbers clips by their position in the song, so every refusal sentence in this application can name `SHOT 03` for a clip the Director is looking at as `SHOT 05`. The function's whole stated purpose is to "name a Shot the way the Director sees it on the timeline", and it does not.
  evidence: `shot_label` computes `position = next((index + 1 for index, item in enumerate(project.shots) if item.id == shot.id), 0)` (`src/music_video_producer/models.py:1113-1116`) — manifest order. `renderTimeline` builds `timeOrder` by sorting a copy of the shots on `a.start - b.start` and ranking that (`src/music_video_producer/web/assets/app.js:1670-1672`), under a comment added by `93f4c77` stating the reason: "Numbered by position in the SONG, not in the manifest: after a mid-timeline add, the clip at 0:10 must not read SHOT 34." The two agree only while manifest order is time order, which a single mid-timeline insert ends. The docstring at `:1103-1105` anticipates a divergence and has the client backwards — it asserts the clip "is drawn as `SHOT 01` from the Shot's position in the **manifest** — that is what `renderTimeline` numbers by", and believes it is the readiness report that differs. Found while wiring C3's copy report, which names refused Shots through this helper for consistency with every sibling refusal rather than inventing a third numbering. Not fixed there because it is repo-wide: `shot_label` is re-exported by `batch.py` and feeds `timeline.assistant_input`, so the model is told about `SHOT 03` under this scheme too, and changing the number changes what a refusal, a notice and a prompt all say at once. Mitigated but not repaired by the label carrying the bare id alongside the number, which is why nothing has surfaced as a support question. The repair is to number by `start` and to correct the docstring; the risk is that `assistant_input` and the notices it produces must move in the same commit or the model and the Director end up on two schemes again — which is the exact failure the docstring was written to prevent.

- source_spec: `_bmad-output/implementation-artifacts/spec-9-c1-review-fixes.md`
  summary: The stack-length cap is at the write route only, so the export path still has no bound of its own. A manifest that was hand-edited, or graded before the cap existed, can still build a command line longer than Windows accepts and come back as the false "ffmpeg is not installed or not on PATH".
  evidence: `SHOT_EFFECT_STACK_LIMIT = 32` is checked in `replace_shot_effects` before validation, which closes every path a client can reach today — the review measured the crossing at roughly 985 grain cards for a 32725-character argv, so 32 leaves three orders of magnitude of headroom. But the composition loop in `assemble_project` reads whatever the manifest holds and imposes no length of its own, so the bound is a property of one route rather than of the export. The implementer flagged this as the uncovered half of the fix rather than leaving it to be discovered. Not closed now because reaching it requires editing a manifest by hand and every realistic path is capped; recorded because a cap that lives at one of two doors is exactly the shape that reads as closed and is not. The false sentence itself is a separate entry below — `run_tool` maps every `FileNotFoundError` to a missing binary — and fixing that mapping would make this gap merely slow rather than misleading.

- source_spec: `_bmad-output/implementation-artifacts/spec-9-c1-the-stack-on-the-shot.md`
  summary: A time-dependent effect restarts at the seam when one Shot becomes two clips. `handheld_shake` snaps back to phase zero partway through the shot and `grain` replays an identical noise sequence, because the export splices one shot's stages into every clip that shot produced and each clip's filter-graph clock starts again at zero.
  evidence: `assembly_plan` resolves an overlap by subtracting every later-starting clip's window from the earlier one (`src/music_video_producer/assembly.py:294-333`), and a *nested* overlay leaves two surviving segments — each emitted as its own `ClipWindow` through `replace(clip, ...)` at `:324-331`, carrying the same `shot_id` with `offset` advanced by the skipped stretch. The export keys the composed chain by that id: `effect_stages[clip.shot_id] = build_effect_stages(...)` at `app.py:12137`, read back per clip as `effect_stages.get(clip.shot_id, EffectStages())` at `:12232` — so both clips receive byte-identical filter text. `trim_args` then prepends `trim=start_frame={skip},setpts=PTS-STARTPTS` to any clip whose offset is non-zero (`assembly.py:458-460`), and that `setpts` restarts `t` at 0. The review's recorded argv shows the pair: clip 1 carries no trim and starts at t=0, clip 3 carries `trim=start_frame=120,setpts=PTS-STARTPTS` and the same `sin(2*PI*2*t)` expression `tests/test_effects.py:568` pins. Exactly two composers read `t`, confirmed against all twenty in the catalogue: `_compose_handheld_shake`'s crop position (`effects.py:919-923`) and `_compose_grain`'s `allf=t+u` with its pinned seed (`effects.py:940`). No other stage string names `t` or `n` at all. Not fixed here because the composer cannot fix it — it is handed the effect's values and a `StageContext` describing the export's geometry, and knows nothing of where the clip sits inside the shot. The repair is a change to what `trim_args` is handed, which is the same shape of change the five absent PRD-minimum effects need (the slice-B entry below, on branched filtergraphs and the clip's own duration). These two should be solved together.
  resolved: Story 9.7, 2026-08-26 — and they were solved together, which is why that story exists. `StageContext` now carries `clip_offset` (the seconds from the Shot's first frame to this clip's first frame) and `shot_seconds` (the Shot's whole window), both handed to `build_effect_stages` by `app.py` from the `ClipWindow` the export is already iterating — `clip.start - clip.approved_start` and `clip.approved_duration`, two fields `replace(clip, ...)` leaves alone when it splits a clip, and which the staleness check already proved equal to the Shot's live window. `effects.py` still imports the standard library and nothing else; `assembly.py` still does not import it back; `trim_args` is unchanged. `ExportComposition.effect_stages` is keyed by the clip's index in `plan.clips` rather than by shot id, so the two survivors of a nested overlay are composed apart while refusals and provenance are still said once per Shot. `handheld_shake` reads `t + clip_offset` and always writes the offset, `+ 0` included; `grain`, which has no expression to offset, advances its seed by the offset in milliseconds. Measured through the route: a Shot covering an 8 s song with another laid over its middle two seconds produces three trims, and the third clip's `-vf` is the first's with the trim pair in front and the offset substituted — `(t+0)` become `(t+5)` and `all_seed=7` become `all_seed=5007` — with the export counting 192 frames at 192x108. Six mutations confirm the tests bite: forcing the offset to zero at the call site, keying the stages by shot id again, taking the offset out of the shake, taking it out of grain's seed, and taking it out of the zoom's ramp each fail a named test.

- source_spec: `_bmad-output/implementation-artifacts/spec-9-c1-the-stack-on-the-shot.md`
  summary: The export's job-record save is last-writer-wins over a pre-probe `Project`, so a Shot edit made while the route was probing is silently overwritten. `effects` is not a new hole — it is a new and newly likely victim, because grading is exactly what a Director does while an export churns.
  evidence: `assemble_project` re-reads the manifest at `app.py:12042`, then awaits an ffprobe per sourced shot — two for a shot with `mix_take_audio` — and finally appends the job and writes that same pre-probe object at `app.py:12166-12167` with a bare `store.save(project)`. `store.save`'s own docstring names what that costs: `if_generation` "turns this from 'last writer wins' into a compare-and-swap" (`store.py:194`), and this call passes none. The route already knows the rule and states it six lines further down, in the `settle` helper it uses for every write after this one (`app.py:12172-12180`): "Re-read, patch only this job on the fresh manifest, save. The house re-read rule: awaits happened, and shot edits made meanwhile must not be overwritten." The job-record save is the one write on the route that does not follow it. Not fixed in C1 because it predates the effects work and costs every `Shot` field the same way — the window has always been open on `prompt`, `trim_nudge` and the rest — so it is a `settle`-shaped repair of an existing route rather than part of the stack, and it belongs with the app-wide read-modify-write entries already logged under `spec-1-3c-song-replacement-safety.md` and `spec-2-2-story-expansion-into-prompted-shots.md`.
  resolved: 2026-08-26, with the other four sites of this mechanism. The job record is appended to a fresh read (`fresh = get_project(project_id)`) rather than to the pre-probe object, which is `settle`'s own rule applied to the one write on the route that was the exception to it and makes the route docstring's "re-read before every job write" true. Proved with a real race rather than by reading: `test_the_export_job_record_does_not_revert_a_shot_edited_while_it_probed` parks the route inside `probe_take_args`, lands a real `PUT /shots` through the TestClient, and asserts both halves -- the shot edit survives and the export still settles. Restoring `fresh = project` fails it. The entry's own claim that the export is "the one write on the route that does not follow it" was true of that route and false of the application: nine submission routes repeat the shape after `comfy.submit`, and all nine now write through `record_submission`.

- source_spec: `_bmad-output/implementation-artifacts/spec-9-c1-the-stack-on-the-shot.md`
  summary: `run_tool` maps every `FileNotFoundError` to "not installed or not on PATH", so a Director whose command line grew too long is told their ffmpeg installation is missing.
  evidence: `app.py:11930-11931` — `except FileNotFoundError: return 127, "", f"{args[0]} is not installed or not on PATH"` — is the only handler around `create_subprocess_exec`, and `args[0]` is always `ffmpeg` or `ffprobe`. On Windows an over-long command line raises `FileNotFoundError [WinError 206] The filename or extension is too long` through that same handler, so the export's 502 reads "Assembly failed at the trim stage: ffmpeg is not installed or not on PATH" on a machine whose ffmpeg is working perfectly. Measured in the C1 review and recorded there (`spec-9-c1-review-fixes.md:110-121`): 985 grain cards build a 32725-character argv and the assemble returns 200; 1200 cards build 40060 characters against Windows's 32767-character cap and return that 502. The docstring above the handler argues the mapping deliberately — "a missing binary reads as a failed run with the absence in stderr rather than a 500" — which is right for the cause it was written for and wrong for every other cause the same exception carries. The review's Fix 6 caps stack length with a named refusal at the write route, which closes this particular road to the sentence; the mapping is untouched by that and still misreports every other `FileNotFoundError`, which is why this is recorded rather than considered handled. Fixing the mapping means discriminating the causes — `winerror == 206` is the distinguishing attribute on Windows — rather than widening the sentence into something vague enough to be true.

- source_spec: `_bmad-output/implementation-artifacts/spec-9-b-the-effects-chain.md`
  summary: Five effects that story 9.3 names as its families' PRD minimums are absent from the 20-effect catalogue slice B shipped — slow zoom (Geometry), bloom/halation (Texture), and edge treatment, scanline/CRT and pixel shuffle/sort (Stylize). Adding any of them is a change to how `trim_args` is built, not an addition to the catalogue.
  evidence: Recorded twice already at the point of the decision, and the two agree: the Spec Change Log at `_bmad-output/implementation-artifacts/spec-9-b-the-effects-chain.md:133-139` and the comment above `_CATALOGUE` at `src/music_video_producer/effects.py:887-891`. Every one of the five needs either a branched filtergraph (`split`/`blend`) or the clip's own duration, and the chain `d8b8afb` built is a single comma-joined linear graph spliced into an argv that knows neither — so the work is a second splice shape, not five more `compose` functions. Logged here because neither existing record is where the next implementer will be standing: story 9.3 restates the PRD minimums verbatim (`epics-effects.md:341`, `:345`, `:350`), so the five *names* will be rediscovered on their own while the reason the chain's shape forbids them will not — and the rediscovery walks straight into `trim_args`. Twenty effects across all four families ship; the rest belong to story 9.3, which the epic already names as where the catalogue is extended.
  resolved: Story 9.7, 2026-08-26 — the diagnosis held exactly: it was a second splice shape, not five more `compose` functions. `-vf` takes a whole filtergraph description, so a branched stage is *one* string containing semicolons that `trim_args` joins with a comma like any other, and `_branch_stage` is the only writer of it: `split=2[fxNa][fxNb];[fxNb]<leg>[fxNc];[fxNa][fxNc]<join>`, the labels carrying the effect's position in the composed chain so two of one effect cannot collide. Four of the five are branches — Slow Zoom (`scale` with `eval=frame` recombined by `overlay`, whose output size is its main input's, because `crop` cannot vary its size in this build and `zoompan`'s output size is not an expression), Bloom (`lutyuv` highlights and `gblur`, screened back with the leg's chroma at 0, which is `screen`'s identity and not 128), Edge Treatment (`edgedetect=mode=colormix` crossfaded back) and Pixel Shuffle (`shufflepixels` crossfaded back, its seed always written). Scanlines is the fifth and is *not* a branch: `drawgrid` does it for the price of one filter. What the diagnosis could not have known is the cost: **any framesync filter makes the chain's closing `fps` stage emit one frame fewer**, exit code zero, because framesync reports EOF at the last frame's timestamp rather than a frame's duration past it — 24 frames in, 23 out, measured 2026-08-26 on ffmpeg 7.0. `BRANCH_FRAME_GUARD` (`tpad=stop=1:stop_mode=clone`) is prepended to the head of the whole chain when anything in it branched, and it has to be the head: `setpts=PTS-STARTPTS` zeroes frame durations, so a clone appended after the branch inherits the previous timestamp and is discarded as a duplicate. Proved as content, not only as a count — a branch at its identity values, guard and all, renders frames bit-identical by `framemd5` to a chain with no effects in it. All 25 catalogue entries render through the real ffmpeg at the right frame grid, and four mutations confirm the tests bite: dropping the guard, giving every branch one label, putting the scanline grid back at its default column, and taking the bloom leg's chroma to 128 each fail a named test.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-1-analyze-the-song-into-an-envelope.md`
  summary: A **generated** song is never analysed automatically. Only `upload_song` — the import route — triggers the measurement, so a song produced by `generate_music` or the Song Planner has no envelope until something else asks for one.
  evidence: The spec's Code Map names exactly one hook (`app.py:7491-7511`, `upload_song`, "where automatic analysis hooks in") while the acceptance criterion says "imported or generated". A generated song's audio path is not written by any route: `batch.apply_job_history` adopts it when the render completes, and the two callers of that in `app.py` are `read_job` and `read_render_status` — both `async def`, and the second is the two-second poll the spec's Never list explicitly rules out ("no polling endpoint for this work"). Hooking `read_job` alone would cover a path the browser does not actually drive, which is a half-measure rather than a fix. Wiring it is a one-liner when the Director rules on where: `analyze_project_song(store, project_id, project, source)` is module-level, takes its dependencies as arguments, and skips itself by fingerprint, so calling it from a completion path costs one file hash on every call after the first.
  resolved: Director ruling 2026-08-24 — both call sites hooked via `analyze_a_landed_song`, and the "no polling endpoint" Never amended in the spec's frozen block to distinguish *triggering* from *working*. The trigger is a `Song.path` captured before reconciliation and compared in memory afterwards, never a fingerprint, so an ordinary tick does a string comparison and reads no bytes of the song. `batch.reconcile_render_jobs` was not touched and stays pure. Five mutations confirm the tests bite: removing either hook, removing the path comparison, and disabling either read-time reason branch each fail a named test.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-1-analyze-the-song-into-an-envelope.md`
  summary: When an import's analysis fails, the named reason reaches the log and nothing else. The Director sees the envelope reported *absent* with a read-time reason ("No song analysis yet"), not the specific one ("ffmpeg was not found on PATH").
  evidence: `upload_song` returns a `Project`, and the story's own boundary says `SongAnalysis` is the only model entity it may add — so there is no field to carry a reason and no notice writer on that route (`replace_project`'s comment records that the chat, expansion and restore routes are the thread's only writers). The reasons exist as named constants with their remedies (`SONG_ANALYSIS_FFMPEG_MISSING`, `SONG_ANALYSIS_DECODE_FAILED`) and are logged at WARNING with the project id. What is missing is a surface: an "analysis failed because X" the interface can show. That is UX with a home already — Story 8.2's `No song analysis yet — bands need it. [Analyze song]` copy — and it wants a re-analysis route to offer alongside it, which this story's Never list ("read-only endpoint") keeps out.
  resolved: Director ruling 2026-08-24 — and the framing was wrong, which is the part worth keeping. The reason does not need a field to carry it; it needs to be *derived*, exactly as validity is. `analysis_absence_reason` recomputes it at read time from `shutil.which("ffmpeg")` and an ffprobe of the file, so the endpoint distinguishes nine causes without storing one byte — three of them `analysis_absence_reason`'s own (ffmpeg absent, a file ffmpeg will not decode, not measured yet), the other six decided by `song_envelope_report` around it, and the same manifest answers differently once ffmpeg is installed. `SongAnalysis` still has no reason field and no valid flag — asserted. The endpoint remains read-only: it computes no envelope and writes nothing, asserted by a test that stubs `analyze_song` to raise if it is ever called. The import-path WARNING logging is unchanged.

- source_spec: `_bmad-output/implementation-artifacts/spec-assistant-producerbot.md`
  summary: Assistant ProducerBot's system prompt has been written but **not iterated against real output**, which is the one acceptance the spec explicitly asks for ("iterated against real output, not written once").
  evidence: No model was loaded in LM Studio at implementation time (`GET /api/v0/models` on 2026-08-18 reported every model `not-loaded`), and none was loaded to probe, so the tool-calling path is verified against `httpx.MockTransport` and against LM Studio's own capability listing — every candidate model advertises `tool_use` — but never against a real generation. Three things can only be judged live and each has a named home for its fix: whether the model calls `fill_shots` at all rather than answering in prose (`tool_choice` is `"auto"`; one word changes it to `"required"`), whether it transcribes lyric lines into prompts as it did for expansion (deliberately not pre-empted, per the Design Notes; the fix belongs in `assistant_prompt.PROMPT_CRAFT`), and whether it cites real asset ids rather than inventing them (refused and reported either way, but a model that invents them constantly makes the feature useless rather than unsafe).

- source_spec: `_bmad-output/implementation-artifacts/spec-assistant-producerbot.md`
  summary: An assistant fill never re-checks that the shots it wrote to are still the ones the Director selected on screen; a bulk fill over thirty shots writes them all in one save with no per-shot undo.
  evidence: The route's own guarantees hold — every shot is judged individually, reported individually, and nothing is persisted until all of them are judged — but recovery is the whole-project one, not a per-shot slot like `treatment_previous`. A Director who dislikes what a bulk fill did has the reply naming every shot it changed and no single control that reverses it. Deliberate: single-slot recovery per shot is a data-model decision (a `Shot.previous` of some shape) that belongs to the Director, and the spec's Never list does not ask for it.

- source_spec: `_bmad-output/implementation-artifacts/spec-song-context-on-import.md`
  summary: A max-length song context adds ~12,200 characters (~3,050 tokens) to *every* Director call, not just the first, and it is re-sent whole on each turn. On an 8192-token model it cuts the usable conversation from ~27 turns to ~8.
  evidence: Measured 2026-08-17 against `snapshot.model_dump(exclude=DIRECTOR_CONTEXT_EXCLUDE)` as `director_chat` builds it, on a project with a 26-shot plan, a filled treatment and style bible, and both recovery slots occupied. The delta is exactly constant at 12,191 characters per call (8000 + 4000 plus JSON escaping) because the whole project is dumped fresh every turn and nothing windows or truncates `messages`. Whole-dump sizes with a max sheet: 26,698 chars (~6,700 tok) at turn 0, 33,136 (~8,300) at turn 10, 39,576 (~9,900) at turn 20, 52,456 (~13,100) at turn 40 — against 14,507 / 20,945 / 27,385 / 40,265 without it. The sheet is 46% of the turn-0 prompt and still 23% at turn 40. Adding `SYSTEM_PROMPT` (587 chars) and the `DirectorResult` json_schema (715 chars, sent every call), an 8192-token window is crossed at turn 8 with a max sheet versus turn 27 without; 16384 at turn 59 versus 78. Over a 20-turn conversation the same sheet is uploaded 20 times, ~61,000 tokens of identical repeated text, and the cumulative prompt cost rises 57%. **Not acted on, deliberately:** the project's recorded root cause of Director degradation is rich context, so this is a real tension with the feature's purpose — but capping or truncating the model's view of the lyrics is precisely the "change to how the Director is prompted" the spec marks Ask First, and a silent cap would recreate the failure this feature exists to fix (the model working from a partial song). Nothing is broken today: no path errors, and the 8k crossover is a degradation, not an exception. What the numbers argue for is a *prompt-budget* story — window or summarise `messages` (the only unbounded term), or send the lyric sheet once as a pinned system fact rather than re-serialising it per turn — which is Director-prompt design and belongs to the Director, not to this spec.

- source_spec: `_bmad-output/implementation-artifacts/spec-song-context-on-import.md`
  summary: The song-context edit route blanks whichever field a request omits, mirroring `PUT /documents`.
  evidence: The shipped client always sends both, so this is unreachable from the interface; a third-party client sending only `lyrics` would clear the style. Same shape as the documents route, and the same argument for and against making it tri-state.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-2-first-live-reference-render.md`
  summary: RESOLVED 2026-08-18 — the four-panel layout carry-over was undersampling, nothing more. It does not occur at the application's own default step count.
  evidence: A/B against the same Reference Sheet, same project, same prompt, same 640x384 window, one variable changed. At 4 steps (prompt `6dbe4ff7-08d2-468c-bd5b-8dce37bd68fd`, 136.5 s) the sheet's panel bands and face plate persisted in the composition. At 20 steps — the audited export's setting and `H3Request.steps`'s default (prompt `3f2a3658-4f52-45bb-87b9-2c09d4496a9a`, 497.6 s) — the frame is a coherent cinematic shot of the character in a scene, with the sheet's wardrobe carried faithfully: corset lacing, arm guards, layered fringed skirt, boots. No panel structure at all. `ref_image_size="match"` was unchanged between the two, so it was never a factor. The 4-step render was a cost-saving override in the smoke script against a graph designed for 20 with no LoRA to compensate.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-2-first-live-reference-render.md`
  summary: The reference path's live evidence covers exactly one shape: one picture reference plus the master song. Video references, paired video audio, multiple pictures, `ref_image_size="max"`, and windows near the frame ceiling have never been submitted.
  evidence: The 2026-08-18 run spent its two authorised jobs on the minimum window that proves the path. Everything else on that path remains schema-audited by `tests/preflight_h3_ultra.py` and unit-tested only, which is the same standing the whole path had before this run.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-1-h3-ultra-preflight-audit.md`
  summary: A reference marked `enabled: False` desynchronises the H3 payload — the adapter's index loop counts every reference, while the node's partitioning skips disabled ones, so every splitter output after a disabled item shifts down one while the condition wiring still points at the unshifted slot.
  evidence: Traced against the node source during Story 3.1 investigation. Unreachable today because `app.py` never sets `enabled`, but it is one line away, and the adapter deliberately preserves a caller-supplied flag it then ignores when indexing.
  resolved: `_bmad-output/implementation-artifacts/spec-h3-reference-partition.md`, 2026-08-26. The index loop is gone: `workflows.partition_h3_references` mirrors `MiniMaxH3MediaLoader._partition` once, and the conditioner inputs, the split output indices and the per-kind ceilings are all derived from that one partition, so the wiring cannot disagree with the grouping. A mirror rather than an import — the extension is GPL, outside this project and not a dependency — pinned by tests in `tests/test_workflows.py` that state the node's rules as their expectations, including that `enabled` is read by identity (`is False`) exactly as the node reads it, so `0` and `null` are not off. A plan whose every reference is switched off is now refused by name rather than building a payload wired to four empty groups. Verified offline against constructed payloads for six awkward shapes; **no render was submitted and none is needed** — the node's rules are readable and the payload is a dict. The mutation that reintroduces the defect (the `enabled` skip removed) fails four tests.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-1-h3-ultra-preflight-audit.md`
  summary: `audio_mode="standalone"` on a video with `has_audio` delivers the wrong audio reference — CONFIRMED mismatch, not theoretical.
  evidence: The adapter wires no `ref_video_audio` slot for that combination, but the node appends the video's extracted track into the `audios` group ahead of any later standalone audio, so `ref_audios.ref_audio_0` resolves to the video's track rather than the intended standalone file. Traced through the node's partitioning. Unreachable from the app today (nothing sets `audio_mode`), and no test covers `audio_mode` at all.
  resolved: `_bmad-output/implementation-artifacts/spec-h3-reference-partition.md`, 2026-08-26. Same root cause and same fix as the `enabled` entry above: the standalone video's track now takes its slot in the audio group where the video sits in the list, so a following `audio` reference is wired to the slot the loader actually fills. Two corrections to this entry's own description, both found by re-reading the node. `_partition` is a static method on **`MiniMaxH3MediaLoader`**, not on `MiniMaxH3ReferenceSplitter` — the splitter only pads each group to its fixed width and concatenates them. And the displacement is positional, not unconditional: an `audio` reference *before* the video keeps slot 0, and only what follows it moves. The ceilings moved with it, and became the larger of two counts, because the node counts its own media twice and the two disagree: `VALIDATE_INPUTS` counts every item of a kind including the switched-off ones and refuses a tenth picture before the graph runs, while `_partition` skips them and then truncates each group silently. The audio group is bounded by neither — nothing counts audios in `VALIDATE_INPUTS`, and a standalone video's soundtrack joins the partitioned group where no attached count sees it — so a standalone video plus three audios is four audios, and is now refused here rather than wired to a splitter output that does not exist. `audio_mode` is covered by tests at last, on the two values the node actually branches on; the pre-existing test passing `"separate"` was never evidence, since the node recognises neither branch for it and it matched the old adapter by accident.

- source_spec: `_bmad-output/implementation-artifacts/spec-h3-reference-partition.md`
  summary: A cited video's soundtrack is not conditioned, by decision. Turning it on needs two things this application does not have, and is the Director's call.
  evidence: The submit route sent neither `has_audio` nor `audio_mode` until 2026-08-26, so a cited video's audio was discarded on every submission in silence — reachable today, since an uploaded mp4 becomes an Asset of kind `video` from its MIME type and `citation_slot_kind` wires it through. Shipped: the omission became a declaration (`has_audio: False` on the wire, where the loader reads it), and `batch.video_soundtrack_note` says so to the Director in the readiness list under its own heading, muted rather than amber and with no badge on the clip, because nothing is wrong with the shot. Stating it on the wire moved seven pinned payload digests in `tests/test_api.py`; the delta was proved to be that one field and nothing else by stripping it back out and re-digesting. **Not** shipped, and deliberately: any behaviour change. A reference shot is performed against the master song, and a clip's own soundtrack in the same sampling pass is unrelated music; wiring it on by default would change what every existing citation renders, on a path where video references have never been rendered live at all. The two missing pieces, if the Director wants it: something that knows whether a video file carries an audio stream (`probe_streams_args` does this for takes in the export pre-flight, and no Asset field records it), and a control for the paired-versus-standalone choice the node reads — `paired` conditions the track as video N's own soundtrack, `standalone` moves it into the audio group where it displaces every following audio reference, and there is no defensible default between them. The partition mirror already models both, so only the product decision and its control are outstanding.
  resolved: **Director ruling, 2026-08-26 — leave it off, and it is honest now.** Asked directly, with the two missing pieces and the cost of each option named. The reasoning stands as written above: a reference shot is performed against the master song, so a cited clip's own track conditioned alongside it is unrelated music in the same sampling pass, and that is arguably the right default whatever the interface eventually offers. Nothing further is outstanding — this is a decision taken, not a gap left. Reopen it only if the Director asks for the control, and if they do, the two missing pieces named above are the work; the partition mirror already models both wiring shapes, so neither needs designing again.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-1-h3-ultra-preflight-audit.md`
  summary: `shift_audio` is 4 on the Director graph and 3 on the reference graph's shift node; both are in range so nothing catches the discrepancy.
  evidence: Raised as the spec's Ask First item and deliberately left unchanged — neither value is provably wrong, and picking one without the Director's judgement would be guessing at a creative/technical parameter.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-1-h3-ultra-preflight-audit.md`
  summary: `resolve_asset_path` and `resolve_song_path` are closures inside `create_app`, and `generate_multiview` re-implements the same containment logic inline a third time.
  evidence: Story 3.1 investigation. Means a standalone script cannot reuse the containment guard without instantiating the app, and the third copy can drift from the other two. Lifting them to module scope is a refactor this verification epic deliberately avoided.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-1-h3-ultra-preflight-audit.md`
  summary: Reference file paths are resolved on the application host but consumed by the ComfyUI process, so a remote ComfyUI would receive an unreachable local path and fail only at execution.
  evidence: Works today because both run on one machine (`comfy_root` on `J:`, `data_root` on `F:`). The node performs no pre-execution existence check of its own — `VALIDATE_INPUTS` only parses JSON and counts kinds — so a bad path fails after the job is queued and the model may already be loading.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-4-director-safety-notices-unmissable.md`
  summary: `document_restore_notice` is the one protective/audit statement still carried as a plain text convention — a bare `system` message with its own styling — while the story's scope sentence claims every refusal is now structural.
  evidence: Blind-hunter review. Either it belongs in the notice structure like the rest, or the scope claim should name it as the exception. Low risk because it is already visually distinct via `.message.system`, but the inconsistency will confuse the next person to add a notice.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-4-director-safety-notices-unmissable.md`
  summary: The prose-claims-Shots mismatch check is a heuristic over model prose and will produce false positives no tightening fully removes.
  evidence: It exists because the recorded 2026-08-16 defect said "a four-beat sequence" rather than "shot", so a literal-word match would have missed it. Review narrowed the obvious false positives (denials, replies about existing Shots, counted structures without shot context), but the check reports rather than blocks precisely because it cannot be exact. Revisit if the notice proves noisy in real use.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-4-director-safety-notices-unmissable.md`
  summary: Pre-2.4 manifests still carry the old inline `"Raw output: …"` text inside `content`, with no `notices` structure — so the model's degraded output remains in those saved projects and reaches the Director context on the next turn.
  evidence: The exclusion only strips the structured `notices` field; historical messages predate it. A migration would have to rewrite stored message content, which is a data-rewriting decision rather than a code fix. Affects only projects chatted with before 2026-08-17.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-2-story-expansion-into-prompted-shots.md`
  summary: Expansion overwrites an unlocked Shot's hand-written prompt with no recovery slot — the lock is the only protection, which is a real asymmetry with the document path that gained `*_previous` slots in Story 2.1.
  evidence: Flagged by the implementer. The spec did not ask for prompt recovery, but the same argument that justified single-slot document recovery (a plausible-looking rewrite nobody asked for is permanent) applies to a prompt the Director wrote by hand.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-2-story-expansion-into-prompted-shots.md`
  summary: Two expansion behaviours are reported rather than acted on, by design: a Shot the model omitted is not retried, and a returned Shot id matching no Shot is discarded rather than creating a Shot.
  evidence: Both were the spec's Ask First items. Guessing either way changes the design — retrying omissions costs another model call, and creating Shots from unknown ids lets the model add untimed Shots to the plan.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-2-story-expansion-into-prompted-shots.md`
  summary: `PUT /api/projects/{id}/shots` (`replace_shots`) is an unguarded whole-list save — no overlap check, no prompt check, no lock check, and no `updated_at` conflict check — so anything expansion writes can be silently clobbered by the next inspector change from a stale client.
  evidence: Investigation for Story 2.2. `saveShotsSilently` fires on any `change` in the shot inspector and on every drag, resize, drop, add, duplicate, delete and split. `replace_project` closes this class of hole with an `updated_at` 409; `replace_shots` has no equivalent. Same sibling-write-path pattern that left holes in the song and document stories.
  resolved: 2026-08-26. `replace_shots` gained the compare-and-swap to go with the `updated_at` token it already had, and so did `replace_project`: the token is compared at the top of the route and the manifest is written at the bottom, so anything landing between the two passed the token and was then reverted. A client that sends no token at all -- the wire has always allowed it -- now has the store's guard instead of nothing. The other gates this entry names (overlap, prompt, lock) are unchanged and still absent.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-2-story-expansion-into-prompted-shots.md`
  summary: Song structure analysis (BPM, sections) has no implementation, so the epic's "section boundaries when analysis exists" clause can never take its populated branch.
  evidence: The analyse-structure button is a disabled stub, `#bpm-value`/`#sections-value` are hardcoded "Not analyzed", `#section-track` exists in the timeline markup but is never populated, and no model carries section data. Listed as not-done in ROADMAP's Production editing section. Until an analyser ships, expansion's section slot is an empty branch.
  resolved: 2026-08-26, and this is the entry Epic 9's retrospective named as the seed of its ledger audit -- five of its six clauses were false when it was read, and it had buried the sixth. What is true now, clause by clause: `Project.sections` carries `SongSection` and `SongAnalysis` carries `bpm` (`models.py`); `POST .../song/align-lyrics` fills the sections from a Whisper transcription and `audio.py` measures BPM (shipped 2026-08-24, Epic 8); `#section-track` is drawn from `project.sections` and `#bpm-value` reads `songTempoCell` (`b8dc162`); and `timeline.song_section` is not an empty branch but the fix for the wrong-verse lipsync, sending `section: {label, prompt}` per Shot from `expansion_input`. **Story 9.5 falsified the last of it (2026-08-26):** the browser now targets a Section by name from `Project.shot_sections`, the served answer of `Project.section_of` -- so the sections are not merely modelled and drawn, they are something the Director acts on. The rule the audit drew from this entry, recorded here because it is the entry that taught it: an inventory table of what exists would have made three of its findings visible without an audit at all.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-reviewable-document-replacement.md`
  summary: Re-reading the project after the Director call means a shot added concurrently now participates in the `apply_shots` index merge, so `result.shots[i]` can align to a different shot than the model saw.
  evidence: Accepted consequence of closing the stale-snapshot window (review finding 6). Strictly better than the previous behaviour, which silently deleted the concurrent shot, but it is a real behaviour change on the `apply_shots=True` path and is untested. The UI hardcodes `apply_shots: false` today, which is why it is not urgent.
  resolved: 2026-08-26. The merge is keyed to the ids of the Shots the *model was shown* (`snapshot.shots`) rather than to positions in the list read after the call, which is `expand_shot_prompts`' rule arriving at the route that argued it was not needed. A Shot the model described that no longer exists is dropped rather than recreated -- recreating it would invent a window on a plan the Director has just changed -- and that is silent, because the browser still hardcodes `apply_shots: false` and a notice nobody can reach is a wire contract bought for nothing. `test_the_chat_merge_follows_the_shots_the_model_saw_not_their_positions` inserts a Shot at the front through a real `PUT /shots` while the model call is parked; keying off `project.shots` again fails it.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-reviewable-document-replacement.md`
  summary: No way to review a kept document version before restoring it — no preview, no diff, no length or date hint — and the recovery slot carries no provenance (no timestamp, no originating message id).
  evidence: Blind-hunter review. For a requirement named *reviewable* replacement, the review step is the missing half: the only way to see what is recoverable is to restore, read, and restore back. Would need slot metadata, which is a model change.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-reviewable-document-replacement.md`
  summary: The restore route has no optimistic-concurrency check, so two restores — or a restore racing a chat turn — can double-swap or lose an update.
  evidence: Both edge-case and blind-hunter review. Consistent with the app-wide absence of locking already deferred for the song routes; `replace_project`'s `updated_at` check is the only precedent.
  resolved: 2026-08-26. `restore_document` reads through `get_project_for_update` and saves with `if_generation`, so the loser is refused by the existing `handle_save_race` with `SAVE_RACE_REFUSAL` -- no new sentence and no client change, and the browser already re-reads on a 409. Refused rather than retried, on `RENDER_STATUS_SAVE_ATTEMPTS`' own rule that a retry belongs to a loop that re-derives its verdict. **And the twins were closed with it**: `restore_song_context`, whose docstring claims to match this route "exactly", and `replace_song_context`, which is the route that *fills* the single slot. `test_the_two_recovery_slot_restores_are_written_the_same_way` holds the three together so one cannot drift again.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3c-song-replacement-safety.md`
  summary: The app has no request-level locking anywhere, so a whole-manifest `store.save` can clobber a concurrent one — e.g. `remove_song` racing a `PUT /shots` could drop shot data despite the no-deletion guarantee.
  evidence: Edge-case review. Every route reads the project, mutates, and writes the entire manifest; `store.save` is atomic per write but there is no read-modify-write guard. Architecture-level concern affecting all routes, not specific to song replacement.
  resolved: 2026-08-26, and the framing was wrong. "No request-level locking anywhere" was false at the time it was written: `ProjectStore.save`'s `if_generation`, `read_for_update`, `_MANIFEST_LOCK` and `handle_save_race` all existed. What was true is that one route out of the fifty-four that write reached for any of it. The named example is closed from both sides -- `remove_song` and `replace_shots` both compare-and-swap, with a test for each direction -- and the general case is answered by `MANIFEST_WRITE_GUARDS` in `app.py`, an enumerated table of every writing route by the guard on its write, asserted against the source by `test_every_manifest_write_is_classified`: a new saving route fails the suite until somebody classifies it, and a classification that stops matching the code fails too. Thirty-four routes are still last-writer-wins and the table says so by name; that is the remaining work, and it is now visible without an audit.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3c-song-replacement-safety.md`
  summary: After a confirmed replacement with a shorter Song, Shots can sit past the end of the audio with no signal anywhere in the app.
  evidence: The spec forbids auto-adjusting Shot windows (correctly — that would be its own data loss), so the remedy is a notice surfacing orphaned Shots. Separate feature; relates to Epic 2's window flagging.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3c-song-replacement-safety.md`
  summary: A Song whose duration is 0 (ffprobe unavailable) is treated as both loaded and lengthless; downstream planning and UI have no notion of "present but unmeasured".
  evidence: Edge-case review. Would need a `needs_measurement` concept gating treatment and shot planning — a model change, which is Ask First territory.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3b-import-duration-regression-guard.md`
  summary: Browser coverage for the undecodable-import path, the song removal/replacement confirmations, the GPU-cost confirm dialogs, and the expansion button. NARROWED 2026-08-17: `tests/e2e_epic2_surfaces.py` now covers the notice blocks, document lock/restore, consent clearing on project change, the unprompted-clip flag, the readiness region and the Song preset rules. What remains needs either a live language model or a `window.confirm` interaction.
  evidence: Both specs claim only the executable contract for their UI halves, and say so in the docs. Browser QA remains a human release gate.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-live-song-generation-smoke.md`
  summary: Browser-level playback coverage for GENERATED songs, which requires ComfyUI started with `--enable-cors-header`.
  evidence: Generated songs play from a cross-origin `<comfy_url>/view` URL while `loadPersistedWaveform` uses fetch + decodeAudioData, which needs CORS headers ComfyUI does not send by default. The smoke script's urllib fetch ignores CORS entirely, so its evidence cannot generalize to a browser even in principle; `tests/e2e_audio_playback.py` covers only the same-origin imported path. Needs its own spec.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-live-song-generation-smoke.md`
  summary: Smoke script ergonomics — an overall time budget across variants (currently 2400 s per job, so two variants can burn 80 minutes) and an `--only <variant>` selector to retry just the failed half.
  evidence: Raised by blind-hunter review; not a correctness defect, but a real cost/iteration issue on a GPU-spending script.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-live-song-generation-smoke.md`
  summary: Bind the smoke script's Python `view_url()` to the frontend's `comfyOutputUrl()` with a contract test instead of maintaining two implementations.
  evidence: The repo already maintains `tests/test_frontend_contract.py` for exactly this class of duplication; the separator bug was fixed in the Python copy but nothing prevents the two drifting apart again.

- source_spec: `_bmad-output/implementation-artifacts/spec-ltx25-dimension-boundary-repair.md`
  summary: Implement the 8k+1 frame-count alignment the LTX boundary imposes (192 frames in, 185 out) so assembly can account for it alongside H3's 17k+5 grid.
  evidence: Measured in the 2026-08-17 live run. `timeline.py` encodes only `align_h3_frames` (17k+5); there is no 8k+1 helper. Assembly is unimplemented, so this is a recorded constraint awaiting the work that consumes it.

- source_spec: `_bmad-output/implementation-artifacts/spec-ltx25-dimension-boundary-repair.md`
  summary: `patch_ltx25_dimension_boundary` hardcodes divisor 32 for any template it accepts; a graph whose VAE needs only 4 or 16 would be over-cropped by up to 31 px per axis.
  evidence: Edge-case review. Low risk today because the function is LTX-2.5-specific by name and guards on LTX-2.5 node ids, but the divisor should be derived from the target VAE if the function is ever generalized.

- source_spec: `_bmad-output/implementation-artifacts/spec-ltx25-dimension-boundary-repair.md`
  summary: `test_ltx25_normalizer_divisor_makes_seedvr2_output_exact_at_every_vae_stage` recomputes KJNodes' rounding itself and asserts its own arithmetic, so it proves nothing about how `ImageResizeKJv2` actually rounds.
  evidence: Blind-hunter and verification-gap both noted it. The load-bearing protection is the direct `divisible_by == 32` assertion; verifying the node's real rounding would need a live single-node probe.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-generate-a-song-with-invented-lyrics.md`
  summary: Surface the SongPlanner-generated caption and lyrics to the Director after a run (Song.lyrics stays empty; the export's preview nodes were UI-only and never reach /history).
  evidence: Blind-hunter review confirmed nothing in the app shows what Gemma-3 actually wrote; the Director hears the song but never sees its lyrics.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-generate-a-song-with-invented-lyrics.md`
  summary: Guard against two concurrent song-generation jobs — the older job's refresh reconciliation can overwrite the newer song's path/metadata.
  evidence: Pre-existing pattern shared with the direct Music 3 route (all kind=music jobs target "song", last refresh wins); surfaced by edge-case review.
  resolved: 2026-08-26, and half of it was already stale. The reconciliation overwrite this entry describes cannot happen: `batch.apply_job_history` gates song adoption on `project.song.prompt_id == job.prompt_id`, so a late answer to the older job can never be pasted onto the newer Song. What was still open is worse than the entry claims -- both music routes wrote the project they had read *before* `comfy.submit`, so the loser of two concurrent generations lost its whole job record, leaving a prompt running on the card that `reconcilable_jobs` cannot count and nothing will ever adopt. Both routes now commit through `record_submission`, which re-reads and writes only the accepted prompt id and the Song. `test_a_second_music_generation_does_not_erase_the_first_ones_job_record` parks the first route inside `comfy.submit` and runs the second to completion through the TestClient.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-generate-a-song-with-invented-lyrics.md`
  summary: Generated Song.duration stores the requested duration; the model-resolved FLAC length is never measured, so the timing spine can diverge from the real audio.
  evidence: Node 49 seconds is model-resolved (["45",1]) by design; the generated path never calls ffprobe (pre-existing for the direct adapter too). Song is the timing spine for Shots/Assembly.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-generate-a-song-with-invented-lyrics.md`
  summary: Expose genre_hint in the Song workspace UI (currently API-only; the form never sends it).
  evidence: Two reviewers confirmed SongPlannerRequest accepts and threads genre_hint but app.js builds only {title, idea, duration, seed}.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-generate-a-song-with-invented-lyrics.md`
  summary: Consider a persisted marker distinguishing SongPlanner-generated songs from direct Music 3 songs (job and Song both record only kind=music).
  evidence: Story 1.3's live verification and future debugging cannot tell from persisted state which adapter produced a song; a new Song field is Ask-First per the spec.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-generate-a-cover-from-known-lyrics.md`
  summary: Song.lyrics is now populated for known-lyrics generations but permanently empty for invented ones, and nothing documents the asymmetry.
  evidence: Downstream treatment planning cannot distinguish "no lyrics" from "lyrics exist but were never captured"; DATA-MODEL.md lists the field with no semantics. Pairs with the 1.1 entry about surfacing planner-written lyrics.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-generate-a-cover-from-known-lyrics.md`
  summary: The API selects the SongPlanner variant implicitly by presence of the lyrics field; consider an explicit variant discriminator so a client cannot silently degrade a cover request into an invented generation.
  evidence: Verification-gap and blind-hunter both flagged that {"lyrics": null} takes the invented path with no signal; the frontend has an explicit preset concept the route lacks. Behavior is now pinned by test, but the design question stands.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-1-analyze-the-song-into-an-envelope.md`
  summary: The song fingerprint is byte identity, not audio identity — re-muxing a container or editing an ID3 tag invalidates a perfectly good measurement and tells the Director "the song changed after it was analyzed".
  evidence: Surfaced by review of Story 8.1. `effects.song_fingerprint` is size plus a digest of bytes, chosen over mtime for good reasons (AD-28), and the trade is real rather than wrong. Recorded so it is a documented trade rather than an unexamined claim; revisit if Directors hit it in practice.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-1-analyze-the-song-into-an-envelope.md`
  summary: `effects.song_fingerprints_match`'s refusal of two empty strings is unobservable from both of its call sites, which already guard non-empty above it.
  evidence: Found by the verification-gap review, which confirmed that replacing the body with `stored == current` would change nothing any route or test can see. Kept as defensive depth rather than removed, since the guard is the cheaper side of the mistake it prevents. Revisit if a third call site appears that does not pre-guard.

- source_spec: `_bmad-output/implementation-artifacts/deferred-work.md` (read-modify-write pass, 2026-08-26)
  summary: `assistant_fill` now refuses a save race with a 409, and the turn it refuses has already spent one model call per requested shot. The right answer is a re-apply, not a token, and that is a restructuring of the route.
  evidence: The route re-reads the manifest (`app.py`, after `director.assist`), then awaits `expand_shots` — one call to the expansion specialist per shot the model asked to expand — and saves at the end. This project's own measurements put a single local model call at up to 300 s (`populate-outruns-its-timeout`), so this is the widest read-modify-write window in the application by a wide margin, and until this pass the whole-manifest save at the end of it silently reverted anything committed during those minutes. The compare-and-swap added here closes that, and the cost is stated rather than hidden: a turn that loses the race is told `SAVE_RACE_REFUSAL` with every one of those calls already spent. That trade is the one this codebase has already made once — the 2026-08-19 revert of thirty-two prompts is what a silent win looks like — but it is not the best available answer. The best one is `record_submission`'s: re-read at the point of the save and re-apply only what this turn wrote (the staged fills by shot id, the expansion outcomes by shot id, the notices, the reference-map refresh), so the turn lands on whatever manifest is current instead of being thrown away. Not done here because it is a restructuring of one of the three largest routes in `app.py` rather than a token, and because the same shape would then be wanted for `director_chat` and `expand_shot_prompts`, whose windows are microseconds and do not justify it on their own.

- source_spec: `_bmad-output/implementation-artifacts/deferred-work.md` (read-modify-write pass, 2026-08-26)
  summary: Thirty-four of the fifty-four routes that write the manifest are still last-writer-wins, named individually in `MANIFEST_WRITE_GUARDS`. Two lines each, but each one needs a judgement first.
  evidence: `app.py`'s `MANIFEST_WRITE_GUARDS` is the inventory and `test_every_manifest_write_is_classified` keeps it honest, so this is no longer a hole that has to be rediscovered — it is a list. Every route in the bottom group is a plain read, mutate, save with no await between the read and the save, so the window is short; short is not zero, because FastAPI runs a sync endpoint in a threadpool and two of them genuinely overlap. The conversion is `remove_song`'s two lines (`get_project_for_update`, then `if_generation`), and what it costs is a 409 the caller has to be able to act on — which is why each route needs a judgement about what its own write owns before it can be told it lost. The ones worth doing first are the whole-manifest writers whose loss is undetectable afterwards: `replace_documents`, `replace_sections`, `replace_default_setting` and `replace_character_slot` all rewrite text or structure with no revision token of any kind. `approve_take`/`unapprove_take` and `select_shot_take` write editorial decisions about specific media and would want the same. The analysis and timeline routes (`analyze_asset`, `analyze_latest_take`, `populate_timeline`, `lay_out_timeline`) are the other kind — derived output, cheap to redo — and a 409 there is more annoyance than protection.

- source_spec: `_bmad-output/implementation-artifacts/spec-10-e1-sendcmd-compiler.md`
  summary: Grain and vignette cannot be driven by the music because `noise` and `vignette` accept no runtime commands, and the epic's flagship example is one of them. Re-composing grain as a driven `blend` of a noise leg would make it drivable; it is a composer change, deliberately not taken inside a UI epic.
  evidence: Measured 2026-08-27 on ffmpeg 7.0, and reproduced independently: `noise` (0 of 25 options), `vignette` (0 of 8), `unsharp` (0 of 18), `shufflepixels` (0 of 10) and `edgedetect` (0 of 4) expose no runtime-settable AVOption, and a `sendcmd` aimed at one leaves frames byte-identical at rc 0 with no warning even at `-v warning`. Ruled R-25: Epic 10 ships the drivable subset and refuses the rest by name. The route back is the shape `edge_treatment` and `pixel_shuffle` already use — both compose as a branch and write their Director-facing dial into `blend=all_mode=normal:all_opacity=`, whose options *are* runtime-settable, which is why `edge_treatment.strength` and `pixel_shuffle.amount` are drivable today while `grain.strength` is not. Grain would follow: compose the noise on its own leg and drive the join's opacity. What it costs, and why it is not in Epic 10: a branched chain on a texture effect changes the composed filter text, which is `preview_fingerprint`'s fourth input, so every cached preview of a grained Shot is invalidated; and the branch itself needs `BRANCH_FRAME_GUARD`, which is the mechanism that cost one frame per branched clip at exit code 0 until Story 9.7 caught it. The measurement to attach before doing it is the export-cost one, pooled and arm-alternated -- a single 25-run round on this machine manufactured a fake 30% regression once. `lut_look.lut` is a permanent exception either way: a `.cube`'s `file` has no timeline flag, so a LUT can never be swapped by `sendcmd` however the stage is composed.

- source_spec: `_bmad-output/implementation-artifacts/spec-10-e1-sendcmd-compiler.md`
  summary: The whole Geometry family is undrivable because a `sendcmd` moving both `crop` dimensions aborts ffmpeg outright. The fix is to compose a bound geometry stage's companion crop for the binding's whole reach rather than its resting value, so only one dimension ever moves at render time.
  evidence: Measured 2026-08-27, reproduced independently, and this is the finding that would have shipped a crash: driving `w` *and* `h` on `crop` gives `Assertion best_input >= 0 failed at fftools/ffmpeg_filter.c:1923`, **rc 3 and a 48-byte truncated output file**, at any pair of timestamps and with or without `-frames:v`. One dimension alone renders fine. A zoom is never one dimension alone, so `punch_in.zoom`, `slow_zoom.zoom`, `handheld_shake.amplitude` and `dutch_tilt.angle` are all out (R-29). **ffmpeg's `T` flag is not the test for drivability** -- `crop` and `scale` both carry it on `w` and `h`, and the first draft of the drive table was built from the flag: it would have shipped a punch-in that crashed every export it appeared in, with a written file rather than a clean failure. The way back is a composer change: size the companion crop for the binding's whole reach so the driven dimension moves inside a fixed frame. `dutch_tilt.angle` is the cheapest first case because it drives `rotate`'s `a` cleanly on its own and needs no crop arithmetic at all. Not taken inside Epic 10 for R-25's reason -- a composer change landing mid-UI-epic is how Epic 9 shipped a 26-pixel black bar -- and because this family owns the grid, which is what the frame rule depends on.

- source_spec: `_bmad-output/implementation-artifacts/spec-10-e1-sendcmd-compiler.md`
  summary: The `sendcmd` compiler is unreachable from any route: `EffectSpec.bindings` was deliberately left out of slice E1, so a hand-edited `bindings` key is dropped by pydantic and nothing can produce a binding. The next slice adds the field, and the nested adopt guard it needs is a real design problem rather than a copy of the existing one.
  evidence: Slice E1's whole point was the compiler and its pinned text, so the field was left for the slice that wires a control. The obligation it carries is not the ordinary `_adopt_*`: a binding lives *nested* inside `Shot.effects[i]`, so adopting it off the stored project means matching stack entries, and matching them positionally is exactly the `(index, parameter)` ambiguity that R-26 rejected because Story 9.4's reorder breaks it. `EffectSpec` has no id, entries are positional and duplicates of one effect are composable. The safe direction is the one that shipped -- the compiler is fully exercised by tests and cannot be reached by a client -- and `models.EffectSpec`'s docstring says so. The generic write path to guard is both routes, `replace_shots` as well as `replace_project`; the route's own ledger now counts fourteen findings of that hole.

- source_spec: `_bmad-output/implementation-artifacts/spec-10-e5-the-drive-readout.md` (Epic 10 audit, 2026-08-27)
  summary: Two browser harnesses fail at HEAD and both predate Epic 10 — `e2e_seed_and_asset_tabs.py:577`, where a hand-typed seed never reaches the manifest, and `e2e_shot_controls.py:375`, where the Render-again confirmation is still standing when the script starts waiting for a toast. Neither is environmental and neither is Epic 10's.
  evidence: Found by running all twenty-two `tests/e2e_*.py` scripts, which is the first time they have been run together: `pyproject.toml` sets `testpaths = ["tests"]` and pytest's default `python_files = test_*.py`, so none of them is in `uv run pytest -q`. Twenty passed. `e2e_seed_and_asset_tabs.py` fails at `assert typed == 4242` with the seed left at whatever the previous randomised render rolled — reproduced three times with three different residual values (16973, 39391, 9757), so it is not a flake: the select-and-retype into `#shot-seed` followed by a blur onto `#shot-prompt` does not land. `e2e_shot_controls.py` fails with `UnexpectedAlertPresentException: Alert Text: Queue one new take now (turbo, fresh seed)?` raised inside `wait_for_toast(driver, wait, "is open for another render")`. **Provenance established rather than assumed:** a `git worktree` at `0b0bb96` — `ad67a14^`, the last commit before Epic 10's first slice — with `PYTHONPATH=<worktree>/src` so `ManagedServer`'s "is this *this* checkout" proof binds to the worktree, reproduces both failures at the identical line with the identical message. Not environmental either: Edge, its driver, ffmpeg and the server all worked, and the other twenty harnesses passed in the same session on the same machine. Worth one more line, because it says something about how this class of defect survives: `e2e_shot_controls.py`'s most recent commit is `b217585`, whose subject is *"Correct a render-again assertion that had been failing at baseline"* — and it is failing again, on a render-again assertion. A gate outside the suite decays quietly, and a gate that was repaired once outside the suite decays again.
  resolved: 2026-08-31, Epic 11 retrospective item 86 — **and the two failures had two different causes, only one of which was the harness's.** `e2e_seed_and_asset_tabs.py` was reporting a real product defect and the diagnosis in the entry above is wrong about the mechanism: the retype *does* land, the `change` fires, `updateShotFromInspector` runs and `PUT /shots` goes out — and comes back **409 `PROJECT_CHANGED_REFUSAL`**. Section 4b's Render again submits to the dead port this script owns; `generate_h3` saves the job record *before* `comfy.submit` (the 2026-08-21 ruling), so the 502 leaves the manifest two revisions ahead of the tab, and `updated_at` is the token every shots write carries. Every edit in that tab was refused for the rest of the session while the panel went on showing what was typed — measured in a browser with a fetch log: `render-again` 200, `PUT /shots` 200, `generate/h3` 502, then `PUT /shots` 409 with the box reading 4242 and the manifest reading 47034. Fixed by `resyncAfterRefusedSubmission` in `app.js`, which re-reads the project on the failure path exactly as the success path already did, at all five single-submission buttons; pinned by `test_a_refused_submission_re_reads_the_project_it_has_already_moved` against the stub DOM, which two mutations kill. The harness was *also* racing the gesture it started — `wait_for_seed_change` returns one request before the submission is even sent — and now waits for the settled never-submitted record, but that alone does not make it pass: with the harness fixed and the product reverted it fails identically. `e2e_shot_controls.py` was the stale one: it never learned to answer the `confirm()` Render again gained on 2026-08-19 and now answers Cancel, keeping its no-GPU contract. Two further assertions in it had been superseded by the same day's other change — `9bd9447` gave `.toast` `pointer-events: auto` and a dismiss handler, so *nothing is on top of the control* is no longer the guarantee; it now asserts the one that is, that a toast takes one click and the control is reachable on the next. And the confirmation itself was wrong: it promised `(turbo, fresh seed)` from 2026-08-19 while `cd5c785` stopped sending a profile on 2026-08-23, so it named a 4-step bundle for a render that takes the project's own — 20 steps by default. `renderAgainQuestion` now composes it from `bundleSpend`, the same sentence both batch dialogs use, pinned per bundle by `test_the_render_again_question_names_the_bundle_it_will_actually_spend`. Both scripts pass end to end; the suite is 2814 collected, 2814 passed.

- source_spec: `_bmad-output/implementation-artifacts/spec-10-e5-the-drive-readout.md` (Epic 10 audit, 2026-08-27)
  summary: A Director learns that a binding cannot resolve only by opening each bound parameter's panel, or at export. The Drive readout vanishes without a sentence, the `ƒ` chip cannot tell a Shot whose bindings resolve from one whose do not, and with the panel closed the whole state is a colour and a tooltip. FX-22's third consequence — *"With no Song Envelope, no drive readout is offered **and the absence is explained**"* — is unmet.
  evidence: Raised by E5's implementer and characterised by the audit against the running application. **The trigger is an envelope that is absent, not one that was replaced, and the audit's first account of this got that wrong.** `upload_song` analyses the track it just wrote, so a replaced song comes back measured and the binding resolves against the new measurement immediately; the unresolvable state is reached when the measurement is *missing* — never taken (every pre-Epic-8 project), the sidecar deleted or unreadable, or **an import whose analysis failed**, which that route deliberately does not fail the import over and only writes to the log (`logger.warning("Song analysis skipped for %s: %s", ...)`). Nothing on screen says that happened. What the Director then has, in the order they meet it: (1) `GET .../shots/{id}/drive` answers `200` with `bindings: []` — the route's docstring is explicit that every absence answers alike and that *which* absence it is "is the band panel's sentence" — so `driveReadoutView` finds nothing drawable and `syncDriveReadout` leaves `<figure id="drive-readout">` `hidden`; the envelope that was on screen a moment ago is simply gone, which is FX-22's third consequence unmet, and it fell between the criteria because story 10.3's own AC asks for *absent, not empty* only for **a Shot with no bindings**. (2) `api.clipEffectsChip` computes from `shotEffectStack(shot).length` alone and says `"Carries 1 effect"` — it cannot distinguish unbound, bound-and-resolving and bound-and-unresolvable, and never mentions bindings at all, so a Director scanning the timeline for the Shots that will refuse has nothing to scan. (3) With the Effects tab open and the panel closed, the whole state is the `〜` glyph turning `--blue` in state `unresolvable` with the explanation in its accessible name. Only opening the panel gives the full account — `EFFECT_BAND_UNRESOLVABLE_NOTE`, which says nothing is dropped or zeroed and offers `[Analyze song]` — and only an export gives the complete list. The facts are all computed already (`song_envelope_report.reason` rides on `snap-targets`; `stack_is_driven` answers cheaply per Shot), so what is missing is a presentation at the two surfaces a Director looks at: a chip state that separates bound-and-resolving from bound-and-unresolvable, and something where the readout used to be that says why it left. Not built here because it is a UX decision about two shared surfaces — the timeline chip column is 82 px and full at four chips (`e2e_chip_column`), and a note under the Monitor competes with `PREVIEW_FAILED_NOTE` and `PREVIEW_STALE_NOTE` for one line over the picture. **The fourth item of that list is not deferred and is fixed:** the Monitor could show a Preview Clip driven by a song the project no longer had, because `preview_fingerprint` hashed `song_fingerprint` unconditionally while `api.previewInputKey` carried no song at all — closed 2026-08-28 by gating both sides on one rule, with `tests/e2e_preview_song_change.py` as its gate.

- source_spec: `_bmad-output/implementation-artifacts/spec-10-e1-sendcmd-compiler.md` (Epic 10 audit, 2026-08-27)
  summary: Four parameters are refused as undrivable for a reason that appears in no ruling, no acceptance criterion and no PRD consequence — `handheld_shake.frequency`, `bloom.threshold`, `posterize.levels` and `scanlines.lines`, all carrying `DRIVE_EXPRESSION`. Eighteen of the catalogue's thirty-eight declared parameters are drivable; the artifacts imply many more.
  evidence: Read off `effects.EFFECT_CATALOGUE` by enumerating every parameter's `drive` on 2026-08-27. R-25 names nine non-drivable parameters and R-29 adds the Geometry family, which together account for everything except these four: each reaches its filter *inside an expression rather than as a value of its own* (`DRIVE_EXPRESSION`), so a `sendcmd` cannot address it even though the filter takes commands. The exclusions look right, and each was read off the composed text rather than off the catalogue's word for it: `posterize.levels` becomes the *step* inside `lutyuv=y=trunc(val/42.666667)*42.666667`, `bloom.threshold` a comparand inside `lutyuv=y=if(gt(val\,147)\,val\,0)` on the branch's treated leg, `scanlines.lines` the divisor in every one of `drawgrid`'s `x`, `w`, `h` and `t` expressions, and `handheld_shake.frequency` a term inside `crop`'s `x=...sin(2*PI*3*(t+0))` — none of which is an option a command can set — but "looks right" is the state R-29 was in about the `T` flag before it was measured, and that draft would have shipped a punch-in that crashed every export. So this is recorded as **unmeasured rather than wrong**: nothing has run a `sendcmd` at any of the four to confirm the refusal is the truth rather than a conservative guess, and the epic's own record is silent on all four. Two things follow. The refusal sentences reach Directors already (`BINDING_UNDRIVABLE_REFUSAL` quotes them verbatim in the band panel), so the catalogue is stating a fact about ffmpeg on the Director's behalf that no ruling stands behind. And the same recomposition route that R-25 sketched for grain applies to at least `posterize` and `bloom.threshold` — spend the dial on a branch's `blend=all_opacity` rather than on an expression — which would make them drivable with no compiler change at all. Both are follow-ups with a measurement attached, for R-25's own reason: a composer change landing mid-epic is how Epic 9 shipped a 26-pixel black bar.

- source_spec: `_bmad-output/implementation-artifacts/spec-10-f1-the-card-gets-an-id.md`
  summary: Duplicating or splitting a **bound** Shot fires a console 404 -- the Drive readout asks `GET .../shots/{new id}/drive` for a Shot the server has never heard of, because the new Shot is selected and the inspector redrawn before the save lands.
  evidence: Reproduced in `tests/e2e_band_panel.py`'s step 10 browser QA during the card-id work (`ae28e8f`) and left named in that harness's `console_gate(expected=...)` with a comment marking it a reproduced defect rather than a refusal driven on purpose -- so it is visible rather than filtered away. Reachable only since Epic 10 shipped bindings: an unbound Shot asks for no readout at all. The one-line shape of the fix, from the implementer who found it: **`driveReadoutWanted` should answer `wanted: false` while `state.shotsDirty` is true** -- a Shot whose creation has not landed cannot have a drive -- **with the inspector redrawn when the settling save's reply arrives, since nothing redraws it there today.** That second clause is the part that is not free: `renderShotInspector` is not called from the settled branch, so gating alone would leave the readout absent until the next unrelated repaint. Not urgent -- the console entry is the whole symptom, the readout appears correctly a moment later, and no write is at risk -- but it is a 404 a Director's browser logs on an ordinary gesture, and this project's own standing rule is that absence is never an error.

- source_spec: `_bmad-output/implementation-artifacts/spec-11-f3-the-band-and-the-rows.md`
  summary: When an Overlap is removed, only the **outgoing** half of the pair renders. A stored `transition_in` on the incoming Shot composes nothing, so a Director who set a Dissolve and then dragged the clips apart gets one treated edge where R-36's own premise said two.
  evidence: Measured through the shipped routes during F3 (`3322ace`): `transition_in: fade_black` on a Shot answers 200, mirrors nothing, and exports `ExportLook.transitions == []`; the identical type as `transition_out` on the same Shot exports `['shot_a=fade_black one-sided over 12 frames']`. The cause is structural and documented in the code: `_compose_one_sided_transitions` reads `subject.transitions`, which `assemble_project` builds from `transition_out` alone, because AD-30 makes `transition_in` the mirror and *"at export only one of the two is read"* -- which is right for the **paired** case it was written for and simply carries over to the one-sided case nobody wrote a criterion for. **R-36 was ruled on the premise "A's tail fades, B's head fades" and that premise was false**; the ruling is amended with this measurement and the shipped toast now promises only what happens (`Shot 04's transition now treats its own last frames, then cuts`). So the product is honest and smaller than the ruling imagined. **What it would take to close:** the export would compose a one-sided treatment from the *incoming* Shot's `transition_in` on a boundary with no Overlap -- a second insertion point on the head of a clip rather than its tail, and a decision about what a `transition_in` means on the **first** Shot of a plan, where F3 found it inert and said so in a new row state. Both are boundaries Epic 11 has no acceptance criterion for. Not urgent: nothing is lost or silently wrong, and a Director sees exactly one edge treated and is told so.
  rechecked: 2026-08-30 -- still open, and now stated in the requirement itself. FX-18 reads *"the Shot's own final or opening frames"*; only the final ones ship. Epic 11's retrospective amended FX-18's own line in `epics-effects.md`, its row in the FR coverage map, and Epic 11's **FRs covered** list, which flatly claimed FX-18 -- three places that each read as confirmation of the other two, which is why a gap this size survived a slice, a review pass and a closing audit. The mechanism is unchanged at HEAD: `assemble_project` builds `subject.transitions` from `transition_out` alone.
  closed: 2026-08-31 by slice 11.f8, in the shape R-45 decided -- which is **not** the shape this entry proposed. *"The export would compose a one-sided treatment from the incoming Shot's `transition_in` on a boundary with no Overlap"* was refused: AD-30's mirror writes that field on the neighbour whenever `transition_out` is set, so composing it at every unoverlapped boundary is what **one** gesture produces -- a Dissolve faded out and then faded in, which is the picture `Fade through black` is named for and the substitution FX-18 exists to forbid. Measured over 19,440 plans: the rejected composer treats a boundary twice on 5,544 of them and what shipped on none. What did land is the second half of the question this entry raised and could not answer -- *"a decision about what a `transition_in` means on the first Shot of a plan"* -- and it is the whole of where the opening half composes: `app._compose_opening_transition` at the entry the plan lays first, and only where that Shot is also the first in song order. F3's inert row state is now `opening`, and `headless` narrows to the Shot whose own head another covers.
  resolved: story 11.f8, 2026-08-31 — built in the shape **R-45** decided, which is not the shape this entry proposed. The entry's own remedy read *"compose a one-sided treatment from the incoming Shot's `transition_in` on a boundary with no Overlap"*, and that design was **rejected**: AD-30's mirror writes `transition_in` on the neighbour whenever `transition_out` is set, so it is what one gesture produces, and a Dissolve faded out then faded in is the picture `Fade through black` is named for. Measured over the same lattice the shipped rule was proved on: **5,544 plans of 19,440 would have treated a boundary twice** under it, and that count is in the suite as `test_the_one_treatment_sweep_can_see_the_design_r_45_rejected` — a sweep with no demonstrated failure mode rules nothing out. What shipped instead: `transition_in` composes on a Shot's opening frames at the plan's **first entry** and only where that entry's Shot is also first in song order, four mirror forms (`fade_in`, `rise_black`, `rise_white`, `blur_settle`), and 19,440 plans / 18,009 treatments / **0 boundaries treated twice**.

- source_spec: `_bmad-output/implementation-artifacts/spec-11-f3-the-band-and-the-rows.md`
  summary: A transition's type label does not fit its band at ordinary zoom -- `DISSOLVE` needs about 3.1 seconds of Overlap to letter at the default 16.6 px/s, and a typical 0.50s Overlap is 8.3 px wide.
  evidence: Measured in a real browser during F3. Label advance is 5.668 px/char and the 5.7 budget bounds all twelve catalogue entries. The band withholds its label rather than squeezing it, and reserves `.clip-prompt`'s own 33 px inset before lettering so it cannot land on the `ƒ` chip -- both behaviours found by looking, the second only after a real effect stack was put in the fixture. So an overlap at a working zoom is a coloured region whose type the Director must read from the transition row instead. **The band's state is not colour-alone** even then (UX-DR15 holds: the row carries the type as text, and an untyped overlap draws a distinct hatch rather than a fill), so this is a legibility limit rather than a correctness one. Options if it is ever worth closing: an abbreviation table, a hover title, or drawing the label above the band in the track's own margin -- each of which is a UX decision rather than a fix.
  rechecked: 2026-08-30 -- still open and unchanged at HEAD. `.overlap-band .overlap-label` and the 33 px `[data-chips]` inset are as measured; `overlapBands` still withholds rather than squeezing. UX-DR8's line in `epics-effects.md` was amended on 2026-08-30 to say the label is conditional, because it had described it as unconditional since planning.

- source_spec: `_bmad-output/implementation-artifacts/spec-11-f4-preview-a-transition.md`
  summary: The boundary preview plays in the transition row, not in the Monitor. `EXPERIENCE.md` asked for it *in place* -- the Monitor's window spanning the boundary whenever the playhead is inside or approaching an Overlap -- and that is the better product. It was not taken, and the reason is scope rather than disagreement.
  evidence: R-35 makes a boundary its own subject with its own route and its own fingerprint, because `preview_fingerprint` takes one take and one window and asserts as an invariant that a preview is *"never one half of a resolved overlap"*. The Monitor is built on the other side of that line: `monitorPreviews` is a Map keyed by Shot id, `monitorPreviewView` layers a clip onto a per-Shot view, and `previewTime` maps the playhead into **that Shot's** window by subtracting its take offset. A boundary clip's axis runs from `overlap_start - lead` to `overlap_end + tail`, which is a second mapping in the same function. And *"approaching"* is an automatic request: a 0.50 s Overlap in an 8 s song would swap the Monitor's source twice per pass, against a measured 144.6 ms per cold boundary render at the house 1056x608 delivery (88.4 ms at the test fixtures' size, 2.8 ms on a cache hit). **What shipped satisfies FX-21 and story 11.5's own acceptance criterion** -- one clip spanning the boundary, both Shots and the blend continuous -- at the moment a Director is choosing the type, with the twelve names in the select above it. **What it would take to close:** give `monitorPreviewView` a second subject and a second time mapping, decide when a boundary clip supersedes a Shot clip on the same two layers (the supersede registry is already shared, so the render side needs nothing), and choose a rule for *approaching* that does not re-render on every playhead tick. `EXPERIENCE.md`'s bullet is amended in place with this rather than deleted.
  rechecked: 2026-08-30 -- still open and unchanged at HEAD -- `app.js` draws it from `boundaryPreviewWanted` inside the row, and nothing routes it to the Monitor.

- source_spec: `_bmad-output/implementation-artifacts/spec-11-f5-the-frame-rule-and-what-it-let-through.md`
  summary: `assembly_plan`'s resolution loop can lose one frame. A window no longer than `BOUNDARY_TOLERANCE_SECONDS` is discarded whole and its neighbours do not close the hole, so the plan is a frame short of the song while every check that could notice reports clean.
  evidence: Pre-existing, found while building F5's one rule and deliberately left out of scope -- it is a defect in the layers loop, not in the transition split, and F5's non-negotiable was the split. Measured 2026-08-30: `a[0,0.104167] b[0.09375,0.114583] c[0.114583,1.114583]` lays **26 frames for a 27-frame plan**. It is invisible to everything that watches: the three windows tile contiguously, so `tiling_refusals` reports no gap; and one frame short is exactly the bound `EXPORT_DURATION_PROBLEM` accepts, so the export is not refused either. Named in the sweep test's own comment rather than asserted away, so the next reader of that test finds it. **What it would take to close:** the loop would extend a surviving neighbour across a discarded window rather than dropping the seconds, which is a change to how overlaps resolve into layers -- the oldest arithmetic in this module and the one AD-18's frame rule telescopes over. Not urgent: it needs a window under half a frame between two others, and it costs one frame of a song, not a wrong picture.

- source_spec: `_bmad-output/implementation-artifacts/spec-11-f5-the-frame-rule-and-what-it-let-through.md`
  summary: A one-frame clip's one-sided transition renders nothing and records that it ran. `dissolve`, `fade_black` and `fade_white` are byte-identical to untreated at `clip_frames == 1`, while `blur_wipe` snaps to full sigma -- four catalogue types, two contradictory answers, and `ExportLook.transitions` records `one-sided over 1 frames` for all four.
  evidence: Reported in F5's spec and absent from its 'What to build' list, so the fix pass correctly left it: F5's rule is about the **paired** split and this is the one-sided composer. The two answers are both defensible in isolation -- a ramp across one frame has nowhere to ramp, and a blur at t=1 is its endpoint value -- which is exactly why it needs a decision rather than an implementation. **Three things it touches:** what `ExportLook.transitions` should say when a treatment composed nothing (today it claims the treatment ran, which is the past-tense-about-nothing idiom this application has a rule against); whether a one-frame treatment should be refused by name the way a degenerate blend now is; and whether the four types should agree. The boundary preview shows the same thing the export does, so a Director who looks will see the truth -- the record is the part that lies.

- source_spec: `_bmad-output/implementation-artifacts/spec-11-f7-the-timeline-stops-promising-a-blend.md`
  summary: The split rule now exists in two engines. `assembly._paired_transitions` and `api.boundaryBlendVerdicts` each carry the layers loop, the position search, the three stretches and the splice, and nothing but a test keeps them the same.
  evidence: Deliberate and Director-ruled (2026-08-30): the timeline re-renders on every pointermove during a drag, so a server round trip per frame was rejected and the client was given the rule instead. The duplication is real and is held by `test_the_client_and_the_server_answer_one_split_rule`, which asks both engines about one table and compares **answers including the three frame counts** -- two engines agreeing on `False` for different reasons is two engines -- plus `test_the_split_table_contains_what_the_two_engines_disagreed_about`, which fails if the table stops containing a row the two would have answered differently before the port. Verified 2026-08-31 over 12,127 plans and 18,418 transition-carrying boundaries with up to four simultaneous transitions: zero disagreements. **What it would take to close:** the server would put a per-boundary verdict on the wire and the client would draw what it was told, which needs a shape that survives a drag -- the plan is not saved until the gesture ends, so the verdict would have to be computed from an unsaved geometry. That is a route that takes a plan rather than a project id, and it is a bigger change than the port was. The hazard to watch is not drift in the rule -- the table catches that -- but a **third** consumer asking the question a fourth way: this slice found `boundaryPreviewWanted` doing exactly that, offering a blend the preview route refuses at 422, after a spec that named only two functions.

- source_spec: `_bmad-output/implementation-artifacts/epic-11-retro-2026-08-30.md` (action item 83)
  summary: The five record guards each enumerate what they cannot see, honestly and in their own docstrings, and nothing reads those enumerations. Every one of the thirty false records Epic 11 corrected sat in a declared blind spot; the guards caught none of them.
  evidence: Extracted 2026-08-31 from the five module docstrings, so this entry is the thing that reads them. **The two that matter most, because Epic 11 proved both:** (1) `test_stale_claims.py` reads `#` comments, docstrings and markdown in `src/` and the doc roots -- **not test files and not either JavaScript asset**. The false comment in `api.js`'s `overlapRemovalToasts`, which contradicted the code eight lines below it with a test in the same commit asserting the code, was invisible to every guard by construction. (2) It only fires on claims phrased as a **horizon** (*yet*, *planned*, *Epic N*); a flatly false statement with no horizon is not caught, and its own docstring says *those are the larger class*. AD-18's *"asserted at the split now"* -- the most expensive false record of the epic -- is exactly that shape. **The rest, named so they are not re-derived:** `test_spec_frontmatter.py` cannot see a spec that names neither a story nor an epic and carries no task list, never checks `done` in the other direction (a `done` spec whose work was abandoned passes, and eight `done` specs have unticked tasks), and does not guard `type`, which has drifted to `story`/`slice`/`fix`/`remediation` against a template declaring four other values. `test_line_citations.py` **counts** unanchored citations rather than failing them, which is the shape that goes stale. `test_enumeration_claims.py` names itself the one most likely to become noise. `test_sprint_tracker.py` reads `git_evidence.py`'s attribution, which matches a story id **in the subject line only** -- so the two commits that repaired Epic 11 are attributed to no story at all. **What it would take to close, and it is not one job:** the JavaScript half is the largest and most valuable, and it is a different parser rather than a wider glob. The horizon-only limit is a deliberate trade against false positives and closing it means accepting some. Neither is urgent; what was missing was anybody knowing the list existed.
- source_spec: `_bmad-output/implementation-artifacts/epic-11-retro-2026-08-30.md` (item 86, 2026-08-31)
  summary: The two-second render poll moves the project's revision and tells no one, so **a shot edit made while a render is live can be refused 409 and lost, and the tab stays refused until something reloads it**. Same mechanism as item 86's fixed defect, different writer — and this one has no client-side call site to put a re-read in.
  evidence: Measured 2026-08-31, both halves executed rather than read. **Server:** with a Flux job in flight through the shipped routes, `GET /render-status` moves `updated_at` on every tick whose reconciliation changes something (`ProjectStore.save` inside `read_render_status`) — running→settled moved it twice in one probe — and a `PUT /shots` carrying the pre-tick revision comes back **409 `PROJECT_CHANGED_REFUSAL`** with the seed unchanged in the manifest. **Client:** driven in the stub DOM, `pollRenderStatus` applies the report and leaves `state.project.updated_at` at `rev-1`, and the very next `#shot-seed` change sends `rev-1` as its token. `RenderStatusReport` carries no revision at all (`active`, `comfy_online`, `jobs`, `shots`, `assets`, `song`, `progress`), so there is nothing for the client to adopt even if it wanted to. The tab never recovers on its own: `updated_at` only advances on a shots write that *lands* or on `loadProject`, and while stale no write lands.
  Why it was not fixed with item 86: the obvious remedy is wrong. Adopting a revision off the poll answer would declare the tab current over a **foreign** write it has never seen — another tab's prompt edit that the tick read and saved back — and its next silent save would revert that work, which is the 2026-08-19 incident `saveShotsSilently`'s revision exists to prevent. A reload every two seconds is the editor-wiping defect this file keeps re-fixing. The shape that is actually safe is for the tick to report **both** revisions — the one it read at and the one its save produced — and for the client to adopt the new one only when the first equals what it holds; that is a change to a response model, the route, and the most frequent writer in the application, and `docs/BUILD-HANDOFF.md` §4 says work like that gets its own review pass rather than riding along with a harness repair. The sharper alternative worth pricing first: `replace_shots` refuses on a *project*-wide revision while it writes only `shots`, so job bookkeeping invalidating a plan edit is a false conflict by construction.
  What a Director sees today: an error toast reading *"Project changed since it was loaded; refresh before replacing it"*, and the panel still showing the number they typed. Not silent, but the screen and the manifest disagree and the remedy is a page reload.

- source_spec: `_bmad-output/implementation-artifacts/epic-11-retro-2026-08-30.md` (AV-6)
  summary: There is no state in `sprint-status.yaml` for a slice opened **after** its epic's stories are done, and the two guards that police the record contradict each other about it. Third occurrence: F6, F7, and now F8.
  evidence: `test_spec_frontmatter.py::test_no_spec_whose_work_has_shipped_still_says_it_is_waiting` treats *the tracker calls this epic done* as evidence that a spec naming that epic has shipped. For a slice written after the epic closed that inference is simply false, and the spec is red from the moment the file exists until its work lands -- which is precisely when an implementer is running the suite. Setting `epic-11: in-progress` instead trips two guards in `test_sprint_tracker.py` (an epic whose stories are all done must be done; an epic with a finished retrospective must not be unfinished), so **no assignment of the three keys is consistent.** Worked around each time by deferring the tracker write (F6) or by carrying one expected failure through the build (F8), neither of which is a fix. **What it would take to close:** the epic signal is a proxy for *this spec's work shipped* and a bad one; the honest discriminators are the spec's own ticked task list and a story id it names. Dropping the epic signal for specs that name no story would stop the misfire and would also widen the blind spot that guard's own docstring already enumerates, so it is a trade rather than a repair. **Do not weaken the guard to make a slice green** -- that is the failure mode this whole family of guards exists against.
- source_spec: `_bmad-output/implementation-artifacts/spec-13-e2-a-pass-you-can-watch-and-walk-away-from.md`
  summary: **A pass asks before it discards document edits, then discards the ones typed while it runs without asking again.** The workspace staying usable during a pass is E2's constraint 4, so typing into it during a pass is the expected behaviour, and the reply silently overwrites what was typed.
  evidence: Read rather than measured — recorded so it is not re-derived, and it wants a browser run before it is fixed. `suggestVideo` calls `confirmDiscardingDocumentEdits(SUGGEST_VIDEO_QUESTION)` **before** `beginSuggestPass`, which is the right place for edits that already existed. On the reply it does `state.project = reply.project; markDocumentsSaved(); renderAll()` with no second question, so anything typed between those two moments is gone — and `markDocumentsSaved()` clears the dirty flag in the same breath, so the guard that would have caught it on the *next* action is wiped too. The window is the length of the pass, which AD-39 sizes at up to 300 s, and E2 deliberately left the workspace open for exactly that long. The Brief is recoverable through its slot; the Treatment and Style Bible are not, and they are the two documents a Director is most likely to be working in while waiting.
  **What it would take to close, and it is a ruling first.** Re-asking on arrival is the obvious remedy and is wrong alone: the question would fire after a 300 s wait, when the Director has forgotten what they typed and the only honest answer needs both texts side by side. The sharper reading is that the pass writes **only** `creative_brief` — asserted by `test_a_pass_writes_the_brief_and_nothing_else` — so a Treatment edit is in no conflict with it and the discard is gratuitous. That makes the real defect `state.project = reply.project` adopting a whole project when the route changed one document, which is a narrower and safer fix than any confirmation.

- source_spec: `_bmad-output/implementation-artifacts/spec-13-e2-a-pass-you-can-watch-and-walk-away-from.md`
  summary: **`AGENTS.md` tells every new session to run a gate that cannot fail**, and the line sits inside the `bmad:context` managed block, so correcting it in place is not durable.
  evidence: `AGENTS.md`, inside `<!-- bmad:context -->`, reads *"`node --check src/music_video_producer/web/assets/app.js` is a required gate and appears in no config file."* Node parses a `.js` file as CommonJS and falls silent at ESM syntax, and both assets have been modules for months: measured 2026-09-03, broken syntax appended to `app.js` exits **0** as `.js` and **1** as `.mjs`. The required gate this file names therefore exits 0 over a broken asset. The check no longer needs a session to run it at all — `test_frontend_contract.py::test_node_check_is_only_a_gate_when_the_asset_is_read_as_a_module` holds the real one inside the suite — which makes the line not merely inert but redundant *and* actively misleading, because a session that runs it believes it has checked something.
  **Why it is deferred rather than fixed:** the managed block is regenerated by the BMad context tooling, so an edit here is overwritten on the next regeneration and the false instruction returns. The fix belongs wherever that block's source lives, and finding it is the work. Recorded because a session that reads `AGENTS.md` and believes it has a working JS gate is precisely the failure the two-inert-gates finding is about.

- source_spec: `_bmad-output/implementation-artifacts/spec-14-c1-editing-a-range-clears-its-mark.md`
  summary: **`Project` has no field-classification gate**, so a new `Project` field enters every Director prompt silently. `Song`, `Shot` and `Asset` each have one; the model whose fields are dumped at the top level does not.
  evidence: Measured by Slice C1 rather than reasoned: adding `Project.brief_attribution` raised nothing at import and the application started fine, with a list of integer character spans in every Director prompt, until the implementer added a hand-typed `DIRECTOR_CONTEXT_EXCLUDE["brief_attribution"] = True`. `_withheld_fields(...)` is applied to `Song`, `Asset` and `Shot` and produces an import-time failure for an unclassified field; `Project`'s own fields have no such call and are excluded one literal at a time. `app.py`'s own comment beside the exclude map already names the argument — *"the same argument `_withheld_fields` makes one level up"* — which makes this a known gap that nobody had written down.
  **What it would take to close:** a `PROJECT_DIRECTOR_VISIBLE`/`PROJECT_DIRECTOR_WITHHELD` pair and one `_withheld_fields` call, in the three existing families' exact shape. The work is not the mechanism, it is classifying the fields `Project` already has — every one of them is currently visible by default, so the pair has to be written to preserve today's prompt byte-for-byte or every Director call changes at once. That is the same bar the `brief_attribution` entry cleared and it is why this is a job rather than a line.

- source_spec: `_bmad-output/implementation-artifacts/spec-14-c1-editing-a-range-clears-its-mark.md`
  summary: **A machine write marks the whole document as the assistant's**, so a planning turn that echoes the Director's own paragraphs back inside a full rewrite marks those paragraphs as written by the assistant. Needs a Director ruling before it needs code.
  evidence: `written_attribution`'s two arms are *a machine write that changed the text marks the whole document* and *everything else reconciles and never marks*. Stated in its docstring rather than left implicit, and correct for the case the slice was built for — Suggest Video composing a Brief from nothing. The case it is wrong for is the planning turn: AD-43 makes a turn an ordinary message and the Brief it writes can be a revision of the Director's text rather than a replacement of it, and a model asked to revise commonly returns unchanged paragraphs verbatim. Those come back marked.
  resolved: **Ruled and built the same day (R-20).** A machine write now marks only the stretches it added; the Director's runs that survive verbatim stay unmarked. Annotated here rather than moved below `## Resolved`, on this file's own rule — a shipped entry left above the heading over-reports open work and misleads nobody, where a *moved* one risks the positional lie this ledger was corrected for. Two things the build found that the ruling did not anticipate are in R-20: the unit is a **line** and presence is a **substring** test, and the two mark sets are **not disjoint** — a mark can survive verbatim inside a line that is itself new, and the writing turn wins that collision.
  **Why it was not obviously a defect:** marking only what is new needs a diff, which the spec did not ask for and which has its own wrong answers — a diff at the wrong granularity marks a re-flowed paragraph as wholly new. And the failure is in the *safe* direction only for the reconciliation, not for this: the reconciliation drops a true mark rather than invent one, whereas this **invents** a mark on the Director's own words, which is the direction AD-45 calls worse than none. **The ruling needed is whether over-marking a full rewrite is acceptable until Slice D**, or whether a machine write should mark nothing until the marks can be placed by comparison.

- source_spec: `_bmad-output/implementation-artifacts/spec-14-c2-the-brief-shows-which-half-came-from-where.md`
  summary: **The palette's own two records disagree on how many accents it has**, six against seven, while agreeing on the closed set and on the rule. A spec that cites the count will be contradicted by whichever document it did not read.
  evidence: `docs/BUILD-HANDOFF.md` §7 standing law 7: *"The palette is closed at **six** accents. `--acid` complete/action · `--amber` running/caution · `--red` error · `--cyan` approved · `--blue` transitions and reactive bindings · `--dim`/`--muted` inert. A **seventh** needs the argument made from scratch."* `styles.css`'s own comment above `--blue` calls it *"the palette's **seventh** and last accent"* and closes with *"an **eighth** accent needs DESIGN 1's argument made again from scratch."* Both are internally consistent and neither is wrong about the set: the handoff counts `--dim`/`--muted` as one entry, the stylesheet counts them as two. Nothing is unallocated either way and the rule — *a new accent needs the argument re-made from scratch* — is identical in both.
  **Why it is deferred rather than fixed:** picking a number means deciding whether `--dim` and `--muted` are one accent or two, which is a design call rather than a typo, and the wrong choice makes a *third* record. Recorded now because it is exactly the shape that goes stale silently: both sentences are guarded by nothing, and a slice that quotes *"the sixth accent"* while adding one would read as compliant against one document and as a violation against the other.

- source_spec: `_bmad-output/implementation-artifacts/spec-14-c2-the-brief-shows-which-half-came-from-where.md`
  summary: **A gate fails at random: `test_healing_an_orphaned_export_at_boot_stamps_the_record_it_settles` asserts a strict `>` between two wall-clock stamps taken microseconds apart.** Found during Slice C2's full runs; unrelated to that slice.
  evidence: `tests/test_assembly_route.py` seeds an orphan whose `updated_at == created_at` — the fixture asserts that as its precondition — settles it, and then asserts `healed.updated_at > healed.created_at`. The settle happens in the same call, so the two stamps are whatever the platform clock returned microseconds apart, and on a coarse clock they are **equal**. Observed failing once in a full run and passing in eight consecutive isolated runs, which is the signature of a resolution race rather than of a defect in the code under test.
  **The obvious fix is wrong.** Relaxing to `>=` makes the assertion vacuous: it would pass on a settle that never stamped anything, which is precisely what the test exists to catch. The shape that is both deterministic and strict is to seed `created_at` in the **past** rather than at `now` — the fixture already controls both stamps, since it asserts they start equal — so the healed stamp has real distance to clear and the inequality means what it says. **What it would take to close:** that fixture change plus a sweep for siblings, because a strict comparison between two stamps taken in one call is a shape rather than an instance: **10 of them across 3 files** (`tests/test_api.py`, `tests/test_assembly_route.py`, `tests/test_batch.py`), counted rather than estimated. Only one has been seen to fail, which is what a resolution race looks like — the other 9 are the same wager at different odds.

## Resolved

Kept for the audit trail: these were logged as deferred and have since shipped. **Nothing belongs here without a `resolved:` line**, and a guard enforces it.

- source_spec: `_bmad-output/implementation-artifacts/spec-song-duration-headroom.md`
  summary: Expose `duration_headroom` in the Song workspace UI (currently API-only; the form never sends it, so it always takes the 1.5 default).
  evidence: The spec's own reasoning is that the creator's documentation and their audited export disagree, so the Director should be able to see the choice and settle it by ear — which a form field would serve and an API-only field does not. Same shape as the `genre_hint` entry above. Two further UI consequences of the default: a duration above 240 s now takes a 422 unless the headroom is lowered, and the duration field's 30-300 s `max` no longer describes what the form can actually submit; a headroom control would need `musicFormFieldUpdate` to relate the two.
  resolved: `_bmad-output/implementation-artifacts/spec-song-duration-headroom.md` change log, 2026-08-18 -- the Song workspace carries an Encoder headroom box on both SongPlanner presets, seeded from `SongPlannerRequest.duration_headroom`'s own default and bounded by its own `ge`/`le`, with every one of those numbers asserted equal to the model rather than typed into the markup. `musicFormFieldUpdate` does not relate the two fields the way this entry guessed it would have to: bounding either against the other makes the followed field a trap, since raising one slides the other's `max` under a value already in its box. The product is shown instead -- a live readout of duration x headroom against the encoder's 360 s ceiling, from the builder's own constant -- and a product outside the schema is refused before the submit in the readout's own words, naming both ways out. Neither number is ever clamped. The duration field's 30-300 s `max` again describes what the form can submit, because the form now says what the second number does to it.

- source_spec: `_bmad-output/implementation-artifacts/spec-song-context-on-import.md`
  summary: Song context has no recovery slot. `PUT /song/context` assigns both fields from the body, so one save can delete an 8000-character lyric sheet with nothing to restore it from — while the treatment and the style bible each kept a `*_previous` copy since Story 2.1.
  evidence: Raised in review as "extend the same recovery pattern to song context". Not done, for three reasons, in order of weight. (1) The spec's frozen intent says **"Never: No new model fields"**, and a slot is a new model field — `Song.lyrics_previous` cannot be added without a human renegotiating the block, which is not the implementer's to do. (2) The threat models differ. The document slots exist because the *model* rewrote a document nobody asked it to; song context is only ever written by the Director's own explicit save click, and the Director sees the text they are about to save on screen while they click. (3) A slot alone is not recovery — it needs a restore route, a restore control, and a `documentRestoreAvailable`-style enabled state, all of which the document pattern has and none of which this story scoped. The alternative, if it is wanted: mirror Story 2.1 exactly — `Song.lyrics_previous`/`Song.caption_previous`, captured by `replace_song_context` only when it overwrites non-empty text with different text, a `POST /song/context/restore` beside the document restore route, a control in the Song workspace, and an entry in `DIRECTOR_CONTEXT_EXCLUDE` so the kept copy is not echoed into every prompt. That last part needs care that the document pattern did not: the exclusion is a top-level `{field}_previous` mapping derived from `DOCUMENT_LABELS`, and a slot nested inside `song` would need `{"song": {"lyrics_previous"}}` — the nested-path shape `app.py`'s own comment warns against, because it stops covering a field renamed or added beside it. Shipped instead as a stopgap: `songContextClearing` asks before a save empties stored text, which covers the unrecoverable accident without pretending to be recovery. It is a client-side guard only — an API client still deletes silently.
  resolved: `_bmad-output/implementation-artifacts/spec-song-context-recovery.md` — the Director renegotiated the "No new model fields" constraint on 2026-08-18, which is reason (1) withdrawn rather than worked around; `spec-song-context-on-import.md` is deliberately left as it shipped, so the record of *why* the gap existed survives. Reason (3) is built out in full: `Song.lyrics_previous`/`caption_previous`, a per-field `POST /song/context/{field}/restore` that swaps rather than pops, two restore controls in the Song workspace enabled from their own slots, and the slots cleared by all five song-changing routes. Reason (2) stands as stated and is not the justification used — the case made is the size and irreplaceability of a pasted lyric sheet, not protection from the model. Two departures from the suggested alternative, both deliberate. The capture rule is "different from stored", not "overwrites non-empty text with different text": a blank is a real previous version and a Director who pasted over an empty field may want it back, so the slots are `str | None` (`None` = nothing kept, `""` = a kept blank) and the restore refuses only on `None`. And the exclusion is not the nested path this entry warned about — `SONG_DIRECTOR_VISIBLE`/`SONG_DIRECTOR_WITHHELD` classify every field `Song` declares, and `_withheld_fields` raises at import if any field is unclassified, so the drift this objection named fails the whole suite on collection instead of leaking silently.

- source_spec: `_bmad-output/implementation-artifacts/spec-song-context-on-import.md`
  summary: Unsaved song-context typing is not covered by `state.dirty`, so switching projects discards a lyric sheet mid-edit without the discard prompt.
  evidence: Deliberate — folding it into `dirty` risks a sticky discard prompt, and the import title behaves the same way today. But it is a real loss path for text a Director may have pasted rather than typed, and the feature exists precisely because that text is worth having. Review found it applied to the `beforeunload` guard as well, and that `loadProject` cleared the flag on refreshes of the project already on screen — so a queue refresh wiped a sheet mid-paste with no question at all.
  resolved: song-context review patch — `unsavedWorkPending` now gates both the project switch and `beforeunload`, `unsavedWorkQuestion` states why the Song workspace is involved, and `songContextSeedClearedOnLoad` clears the flag only when the project actually changes. The flag stays out of `state.dirty`: it also decides whether an incidental render may re-seed the boxes, which `state.dirty` must never do. All three are executed against a stub DOM in `tests/test_frontend_contract.py`.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-reviewable-document-replacement.md`
  summary: Document replacement still has no opt-in, while shot application does (`apply_shots`). Both documents are replaced on every chat turn regardless of what the Director asked for.
  evidence: Blind-hunter review. Recovery, locks and a notice make an unrequested rewrite visible and reversible without making it consented. A genuine product decision — raised with the Director rather than settled here.
  resolved: 78d1424 — apply_documents shipped, off by default

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-generate-a-song-with-invented-lyrics.md`
  summary: Decide policy on the reference export embedding full real-song explicit lyrics as immutable checksummed evidence (sanitized capture vs. as-is).
  evidence: Blind-hunter review noted songplanner-invented-user-export.json nodes 55/63 carry complete real-song lyric text; the spec's Ask-First SHA pinned this exact file, so changing it requires renegotiating the audit convention.
  resolved: 22e1eae — lyrics scrubbed from both exports and from history

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-generate-a-cover-from-known-lyrics.md`
  summary: The explicit-lyrics reference-export policy question now applies to a second checksummed file (songplanner-known-lyrics-user-export.json node 63 carries verbatim third-party lyric text).
  evidence: Blind-hunter review noted the open 1.1 policy item gained a second instance; both exports are pinned by Ask-First SHAs, so any sanitization requires renegotiating the audit convention.
  resolved: 22e1eae — same scrub covered both exports

- source_spec: none
  summary: FR-12 import regression guard — prove an undecodable WAV's ffprobe duration survives application restart, and fix the stale `state.audioBuffer` bug that bypasses the fallback entirely.
  evidence: Split from Story 1.3 (three independently shippable goals; live smoke taken first). Investigation found `app.js` never clears `state.audioBuffer` on a failed decode, so importing an undecodable file into a project that already has a decodable song sends the PREVIOUS song's duration and ffprobe is never consulted. Existing coverage (`tests/test_api.py:105`) tests the route fallback only, never a store round-trip.
  resolved: b1a2d0c — guard shipped with the stale-audioBuffer fix

- source_spec: none
  summary: Song replacement/removal safety — require explicit confirmation naming that Shot windows and Assembly synchronization depend on the Song, and guarantee no Shot data is deleted.
  evidence: Split from Story 1.3. Investigation found every song-mutating path (upload_song, generate_music, generate_songplanner, replace_project) overwrites `project.song` unconditionally with zero confirmation; there is no removal route at all, and `PUT /api/projects/{id}` can null the song and wipe shots in one body. The "no Shot data deleted" AC currently holds by accident, not by design or test.
  resolved: b1a2d0c — gate shipped on all five Song-changing routes

- source_spec: `_bmad-output/implementation-artifacts/spec-8-1-analyze-the-song-into-an-envelope.md`
  summary: Two independent click-track generators exist, `click_wav_bytes` in tests/test_api.py (pure-Python, peak 20000) and `click_track` in tests/test_audio.py (numpy, peak 32767); they can drift apart and the pure-Python one re-runs per test.
  evidence: Found by the blind review. Both suites measure tempo recovery, and two generators with different amplitudes means the two suites are not measuring the same signal. One shared vectorised fixture would be cheaper and would keep them honest.
  resolved: 8e7b46c — retrospective action item A12. `click_wav_bytes` now lives in `tests/test_audio.py:78`, beside `click_track` at `:40`, and delegates the arithmetic instead of restating it: it calls `click_track(bpm, seconds=seconds, rate=rate)` and hands the samples to that module's own `write_wav`, so the decaying-burst formula exists in exactly one place and both suites measure one signal at one amplitude — the 20000-versus-32767 difference this entry named is gone because there is no second peak to differ. `tests/test_api.py` defines no generator of its own; it imports this one (`from test_audio import click_wav_bytes`), the way `test_frontend_contract` already imports `click_track` from the same module. The third generator the retrospective's duplication map named, `synthesize_song` in `tests/e2e_timeline_edit.py:349`, is deliberately left alone: it is a different design rather than a transcription — a full-scale square burst at 8 kHz, stdlib-only because it is a browser QA script rather than a pytest module.

- source_spec: `_bmad-output/implementation-artifacts/spec-13-e2-a-pass-you-can-watch-and-walk-away-from.md`
  summary: `suggest_video_notice` tells a Director "It was blank" about a Brief that was not blank, whenever a pass answers exactly what is already stored.
  evidence: Found while building E2's browser harness, by running two identical passes back to back. `restorable = write_document(...) and bool(displaced.strip())` collapses two different causes of `False` into one: a genuinely blank Brief, and a **byte-identical** write that `write_document` skipped. The notice then picks `SUGGEST_VIDEO_FIRST_DRAFT_NOTICE` — *"It was blank, so there is no previous version to restore"* — which is false in the second case, and it is false in the same sentence that is otherwise this route's most careful one. The `restorable` field itself is correct either way (there really is nothing to restore, because the slot was not spent), so only the *wording* is wrong; a third arm needs a Director-facing sentence and therefore a ruling, and it is E1's route rather than E2's browser half. Reachable in practice on a low-temperature roll, and certain on a re-press with an unchanged song.
  resolved: no

---
topic: Treatment Planning
updated: 2026-08-22T16:01
---

- (event) Forwarded activation from Mary; discovery complete in treatment-planning-findings-and-rulings-2026-08-22.md (F-1..F-6, R-1..R-10, all open questions closed). No re-elicitation.
- (override) Run folder named prd-MusicVideoProducer-treatment-2026-08-22, deviating from run_folder_pattern, for the same reason as the effects PRD: three folders named prd-MusicVideoProducer-<date> invite picking the wrong document.
- (decision) Requirement prefix TP- so IDs cannot collide with FR-1..FR-26 (base product) or FX-1..FX-25 (effects).
- (decision) Two analyst premises were WRONG and are corrected in the findings: the Brief already reaches the Director (timeline.py, 3 call sites), and the multiview character-only gate was already widened to character/prop/setting.
- (decision) R-8 refines the Director's own 'yes' on slots: recurring non-singing characters get reference-sheet consistency, NOT character slots, because a slot is H3's speaker id and would declare them a voice.
- (decision) R-3 knowingly builds a long unattended pass inside a documented-unreliable envelope; the PRD expresses the obligations (bounded timeout, retry, untouched-on-failure, partial-reported-as-partial, never required) rather than assuming them.
- (event) PRD finalized: 19 TP requirements, 5 TP-NFRs, 3 user journeys, 6 open questions (1 blocking for architecture: the Suggest Video timeout bound), 4 assumptions. Addendum carries the existing-code inventory, the tool-schema mechanism, the reliability envelope, and the declined alternatives.
- (decision) R-11: timeout set by live measurement during build; raising the director timeout authorised. The Director's actual requirement is the in-progress indicator. Suggest Video's workload is smaller than populate's, so an overrun is variance not work - longer timeout buys the tail, retry re-rolls it.
- (decision) R-12: LM Studio context stays at 75k. Suggest Video does not need more; the planning conversation's accumulating thread is what will creep, and the fix is bounding what each turn sends, not a bigger context that costs VRAM the eject coordination must reclaim.
- (decision) New open question: planning makes Briefs richer -> Treatments richer -> populate's input larger. Planning succeeding increases populate's job; re-measure after it ships.
- (change) R-13/14/15 folded in: TP-11 gains recorded origins, new TP-13 flags stale proposals, TP-3 uses song sections when present. Renumbered to TP-20. Open questions cut from 8 to 3 (1 UX, 2 architecture); populate re-measurement moved out as a follow-up because it was never a decision.
- (decision) R-16: the Analyze structure control ALREADY sits on the Song page beside Build treatment and already proposes section boxes from timed [Tag] blocks. The Director's premise (timeline-only) was wrong; the ask stands as a prompting change, now TP-18.
- (decision) R-17 cross-PRD dependency: effects FX-1 (beats/onsets/bands) and this PRD's structure analysis are two different computations wanting the same trigger moment. Whichever feature ships second owns sharing one trigger and one indicator.

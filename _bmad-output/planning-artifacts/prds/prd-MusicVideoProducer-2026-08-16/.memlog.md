---
topic: Music Video Producer PRD
updated: 2026-08-16T20:48
---

- (event) Inputs: brief + addendum (2026-08-16), docs/ARCHITECTURE.md, ROADMAP.md, WORKFLOW-MAP.md, DATA-MODEL.md, LLM-DIRECTOR.md, and live verification evidence from today's H3 render.
- (decision) Working mode: fast path, carried forward from the brief run at the user's earlier choice.
- (decision) Stakes: solo passion project, shareable later. Rigor calibrated to internal-tool level, not launch/investor grade.
- (decision) Wizard = guided path composing the five existing workspaces; current step derived from project state, no stored progress, skippable to full editor. Reconciles with the Operate/Command-Inspect decision in ARCHITECTURE.md.
- (override) REVERSAL of ROADMAP.md 'Multiple takes and approval': user rejects multi-take comparison. Rendering several takes per shot costs too much time for too little value. Model is instead one-shot the whole video after reference setup, then selectively REGENERATE individual bad shots in place.
- (override) REVERSAL of brief proposal: user keeps the finishing chain (LTX/SeedVR2/FILM/RTX VSR) IN the MVP rather than deferring it if unproven.
- (decision) MVP must include final FFmpeg assembly, reference-driven H3 Ultra shots, and the finishing chain. Multi-take comparison explicitly excluded.
- (decision) Scale/warm-batching: write the FR but make the target conditional on first measuring whether ComfyUI keeps the model stack resident between queued prompts.
- (decision) NEW REQUIREMENT: during a batch render, each finished clip must take its place on the timeline LIVE. Director can watch the video up to the currently-generating clip and flag clips for regeneration while the batch is still running. Changes batch rendering from fire-and-wait to a progressive, reviewable stream.
- (decision) NEW: VRAM contention. LM Studio holds the Director/vision model in VRAM outside ComfyUI's control. It must be unloaded before video generation. Corroborated: verified H3 render left only 1.1 GB free of 32 GB.
- (event) DEFECT reported by user: the LLM Director errors when used. Smoke test tests/smoke_director_lmstudio.py produced no output. Under investigation.
- (event) PathchSageAttentionKJ is set to sage_attention=disabled in build_h3_reference_payload(). It was disabled due to an earlier error; enabling it would likely speed up H3/LTX workflows. Candidate performance work, needs a verified sageattention install.
- (decision) NEW: MiniMax Music 3 + SongPlanner has been split by the user into two workflow variants -- one that invents lyrics, one for known lyrics (e.g. a cover). The Song feature must support both.
- (event) DIAGNOSED Director defect: with full project context the LLM returns style_bible as serialized JSON ('[{"style":"moody"...') instead of prose. Reproduced 3/3. Schema is NOT the cause -- flattened vs Pydantic $defs schema both scored 3/3 clean on thin context.
- (event) APP-LEVEL DEFECT (data loss): app.py director_chat assigns project.treatment and project.style_bible unconditionally from unvalidated LLM output. One bad call destroys existing creative documents with no undo. Same class of failure yields an empty shots array while the model's prose claims several shots.
- (decision) Director requirements must cover: validate LLM output before persisting, never overwrite a non-empty creative document with degraded output, and make document replacement reviewable/reversible.
- (event) PRD drafted: 25 FRs across 8 features, 8 success metrics with 3 counter-metrics, 11 open questions. Status left draft pending user review; reviewer gate not yet run.
- (event) Reviewer gate run (rubric walker, sequential). Verdict: strong on 5 of 7 dimensions; done-ness thin; 3 high findings. Written to review-rubric.md.
- (decision) FR-15 rule set: reject replacement that parses as JSON, or that is under 40% of the length of the non-empty document it would replace. Checks skipped when target is empty.
- (decision) FR-11 changed from a VRAM threshold to detecting and naming a loaded LM Studio model. Threshold rejected as needing per-model/per-machine retuning.
- (decision) Finishing sequenced last with an explicit drop condition: if the standalone approved-take adapter is not working when all other MVP items are done, the first video ships without Finishing and it moves to v2.
- (decision) Cross-cutting NFRs added, scoped to the two the Director selected: NFR-1 UI responsiveness during a Batch, NFR-2 project-state integrity. Render economics and storage growth deliberately left as open questions -- no measurement supports a number yet.
- (change) Autofixes applied: FR-17 window stated as 4-15s; SM-3 restated as observable; non-goal added bounding live population vs live preview; Assumptions Index now round-trips 7/7.
- (event) Q1 ANSWERED by measurement: two identical 107-frame H3 renders back to back took 438s cold and 288s warm. ComfyUI keeps the model stack resident; second render saved 150s (34%). FR-9 rewritten to 'do not defeat it' -- no batching machinery needed.
- (event) Q8 ANSWERED: sageattention is NOT installed in ComfyUI's embedded python (triton and torch 2.7.0+cu128 are). The PathchSageAttentionKJ 'disabled' pin is correct, not a leftover. Remaining work is an install-and-measure spike.
- (event) Aligned-frames fix verified live: two 4.0s shots (96 requested, off-grid) rendered at exactly 107 frames, 4.458s. Confirms grid rounding works end to end.
- (decision) NEW consequence added to FR-22: Assembly MUST trim each Approved Output to its Shot window. A 4.0s shot renders 4.458s, so untrimmed joins would drift ~11% -- roughly 20s over a three-minute video.
- (event) Seven defects fixed and verified: data loss guard (2/2 live), running-vs-queued (live), silent empty shot apply, aligned frames (live), path separators (live), H3 window flagging, ruff gate. Suite 52 -> 62.
- (decision) NEW REQUIREMENT (user, 2026-08-16): story expansion. Before batch rendering, every Shot's prompt text must be fully planned out -- the treatment/overall structure is expanded into appropriately prompted scenes with deliberate shot-to-shot variance matched to the music's structure. Becomes FR-26 in Creative Direction.
- (decision) User confirmed: proceed to epics and stories from the reviewed PRD.
- (change) FR-26 (story expansion) added to PRD Creative Direction with SM-9; MVP scope updated. 26 FRs total.
- (event) Epics and stories complete: 7 epics, 23 stories, 28/28 FR+NFR coverage validated, user approved epic structure. Written to _bmad-output/planning-artifacts/epics.md.
- (event) Adversarial review of epics.md (subagent, 3 lenses): fix-first verdict, 12 findings + 7 coverage-claim gaps. All 21 fixes applied: guard regression ACs, assembly stale-window/gap checks, restart-mid-batch, H3 kind grouping (text vs Ultra UNETs), automatic unload w/ skip, testable rephrasings.
- (decision) UX contract settled in-session (4 decisions, all recommended options): rail-as-wizard + guidance banner; clip state via border+corner chips in existing palette; on-clip hover actions with F/A keys; pre-flight modal for batch confirmation. Written as DESIGN.md + EXPERIENCE.md under ux-designs/ux-mvp-2026-08-16.

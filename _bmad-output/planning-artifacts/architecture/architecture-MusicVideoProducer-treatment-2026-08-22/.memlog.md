---
scope: Treatment Planning
purpose: Feature-slice spine extending the MVP spine (AD-1..AD-15) and the effects spine (AD-16..AD-31), both binding read-only
altitude: feature
updated: 2026-08-22T16:27
---

- (constraint) Two parent spines binding read-only: MVP AD-1..AD-15 and effects AD-16..AD-31. Numbering continues at AD-32. AD-11 (derived not persisted) and AD-14 (guarded persistence boundary) are the two most load-bearing here.
- (constraint) Found: _promoted() already exists in director.py and RAISES on an unknown field name - it is the machinery built after 'shots' was asked for in words for three runs while the grammar never mentioned it. Every planning tool's required list goes through it.
- (constraint) Found: TreatmentMessage carries role/content/notices, and MessageNotice is the structured 'what this reply reports about itself' mechanism. A planning turn's change announcement is a notice, not a new concept.
- (decision) AD-32: attribution is a MIRROR OVERLAY (styled read-only div behind a transparent textarea, shared font metrics), never a hand-rolled contenteditable. Keeps native selection, paste, undo, IME and spellcheck that a contenteditable must reimplement.
- (decision) AD-33: attribution ranges stored as offsets on Project and RECONCILED by diff on every Brief write - an untouched range shifts, a range whose text changed is dropped. Pure function, testable, and it is what makes 'editing clears the mark' true rather than aspirational.
- (decision) AD-34: session undo lives in the FRONTEND, bounded, never persisted - it is session state, and AD-11's derived-not-stored discipline applies. The recovery slot is the durable floor; a reload loses the stack, which the UX already states.
- (decision) AD-35: Planning Mode session consent is a CLIENT affordance only; every request still carries explicit consent on the wire. The server never holds ambient consent, so a stored flag cannot outlive its condition.
- (decision) AD-36/37: proposals live on Project, narrow-gated by the _adopt_* idiom, each recording its origin text; staleness is DERIVED at read time by checking whether the origin still appears in the Brief. Never a stored stale flag.
- (decision) AD-38: ask and write are SEPARATE tools, not one tool with an optional document field. On a model documented to drop fields silently, an optional field is indistinguishable from a dropped one - separate tools make a question-only turn representable.
- (decision) AD-40: R-17's shared trigger composes the two analyses without merging them - align-lyrics and audio.py stay separate computations behind one job and one progress state.
- (event) Adversarial lens run inline: four incompatibility holes found in the AD-32..AD-43 draft.
- (decision) AD-44: undo restores a snapshot verbatim and bypasses reconciliation - routing undo through the ordinary write path would strip the marks it meant to restore.
- (decision) AD-45: the client never sends ranges; the server is the sole writer of brief_attribution. Provenance with two authorities is provenance with none.
- (decision) AD-46: an accepted proposal is marked with the Asset id it produced, not removed - otherwise a second Accept either duplicates an asset or silently does nothing depending on which builder wrote which half.
- (decision) AD-47: each half of the combined analysis is skipped when already current. Effects FX-1 analyses automatically on song import, so the common case is that one half is already done.
- (event) BUILD-ORDER.md written: eight slices, A/B/H independent, G split so plain navigation ships alone and its combined-analysis half waits on effects Story 8.1.
- (event) spine finalized

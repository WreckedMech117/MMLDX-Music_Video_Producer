---
scope: Shot Effects and Transitions
purpose: Feature-slice spine extending architecture-MusicVideoProducer-2026-08-16 (AD-1..AD-15 inherited, read-only)
altitude: feature
updated: 2026-08-21T16:13
---

- (constraint) Parent spine AD-1..AD-15 are binding and read-only; AD numbering continues at AD-16. AD-9 (assembly is local ffmpeg, trim-then-concat) and AD-13 (approval is approved_output) are the two most load-bearing inherited constraints.
- (decision) MEASURED: texture stages go BEFORE pad. Vignette+grain after pad leaves pillarbox bars at RGB(1,1,5); before pad they are RGB(0,0,0). Closes the PRD's open question 1 and confirms its FX-9 assumption.
- (decision) MEASURED: Song Envelope must be a sidecar, not a manifest field. project.json is 110-190KB today; a 3-min envelope at 30Hz with 8 bands is ~750KB and would ride the 2s poll (~22MB/min of re-serialization).
- (direction) Fast path chosen. Deliverables: spine + a build-order view. Spectrum strip draws the whole-song average (stable across shots so copied bindings keep meaning).
- (decision) AD-16: effects/transition fields live on Shot, but replace_project adopts them server-side via the established _adopt_* idiom (the route's own comments record this hole found six times). Prevents a whole-manifest save clearing every stack.
- (decision) AD-17: one pure chain builder in a new effects.py; family order fixed geometry->scale->pad-preceded texture; texture BEFORE pad per measurement. Prevents each effect choosing its own insertion point.
- (decision) AD-18/19: transitions bake into their own concat intermediate so the join keeps -c:v copy; the overlap is the only transition geometry and one-sided treats the clip's own frames. Prevents a whole-timeline re-encode and any change to the cumulative grid.
- (decision) AD-20/21/26: Song Envelope is a sidecar under the project media dir; the manifest carries only a pointer plus the song fingerprint it was computed from. Validity is DERIVED by comparing fingerprints, never a stored flag - follows AD-11's read-time discipline. Whole-song band averages stored once in the envelope for the spectrum strip.
- (decision) AD-22: reactive drive is a generated sendcmd script written beside the render and passed as a cwd-relative filename; an absolute Windows path's drive colon breaks filter parsing and reports the wrong filter.
- (decision) AD-23/27: preview renders live in a project-scoped cache keyed by a stack fingerprint; staleness is derived by comparing fingerprints; a superseded render is cancelled, never queued.
- (decision) AD-24/25: two new modules, audio.py (envelope extraction, ffmpeg decode only) and effects.py (pure chain + sendcmd construction), both under the standing rule that they never import app.py and stay pinned by string comparison.
- (event) Adversarial reviewer lens run inline. Four incompatibility holes found in the AD-16..AD-27 draft, each closed by a new AD.
- (decision) AD-28: one fingerprint function with enumerated ordered inputs; song fingerprint is content-derived (size+bytes hash), never mtime, which changes on a copy and not on an edit.
- (decision) AD-29: preview geometry is half the PROJECT export geometry, not half the take's, or a shot whose aspect differs previews without the letterbox it will ship with.
- (decision) AD-30: the outgoing shot's transition_out is authoritative for a paired transition; a disagreeing pair reports the divergence and uses the outgoing value rather than refusing.
- (decision) AD-31: the chain builder sorts the stack by family on read, so storage order is never load-bearing and a copied or hand-edited stack cannot silently produce a different chain.
- (event) BUILD-ORDER.md written: six slices, A+B parallel and independent, F (transitions) isolated as the only slice touching the frame grid. Registered as a companion.
- (event) spine finalized

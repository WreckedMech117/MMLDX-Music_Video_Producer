# Roadmap and Verification Status

## Implemented now

- [x] Standalone project root and runtime
- [x] Atomic project manifests
- [x] WAV/FLAC/MP3 upload
- [x] Browser waveform and automatic imported-song duration
- [x] Lyrics and style on an imported song — offline-verified 2026-08-18. `POST .../songs/upload` takes optional `lyrics` and `caption` form fields, and `PUT /api/projects/{id}/song/context` sets them on a song that already exists, so a track imported before anyone typed its words does not have to be re-imported to gain them. Both fields already existed on `Song` and already reached the Director; **no model field was added by this story** — two recovery slots were added by the separate story below, after the Director renegotiated that constraint. Stored as supplied apart from edge whitespace, bounded at the generation routes' own ceilings (8000 characters of lyrics, 4000 of style) and refused with a 422 before any audio is written. A test asserts both actually appear in the dump handed to the Director, against a recording double rather than against the stored project. **Nothing parses or sections the lyrics** — no verse detection, no structure inference, no BPM: `timeline.song_section` stays the empty slot below. The import's two fields are deliberately separate controls from the Music 3 form's lyrics textarea, asserted on the markup and on both handlers. **Not browser-verified**: the Song workspace's new import fields and context editor have offline coverage only
- [x] Direct MiniMax Music 3 generation
- [x] SongPlanner invented-lyrics generation (FR-13) — **verified live 2026-08-17**: a 30 s request produced a 29.989 s FLAC through the running application; see the verification section below
- [x] SongPlanner known-lyrics cover generation (FR-14) — **verified live 2026-08-17**: a 30 s request with a lyric sheet produced a 29.989 s FLAC through the running application; see the verification section below
- [x] OpenAI-compatible conversational treatment planner
- [x] Flux character/setting/prop/style generation
- [x] Media asset library
- [x] Krea QuadView multiview promotion — **first live promotion from the application 2026-08-18**: an uploaded character produced a 1536×1024 four-view sheet in 56.1 s and the child Asset's `path` was populated by the ordinary job reconciliation; see the 2026-08-18 verification section below
- [x] Direct shot add/drag/resize/split/duplicate/delete — **duplicate and split now copy the plan and no take, offline-verified 2026-08-20.** Both cloned the whole Shot and reset `status` alone, so the new Shot owned the original's `latest_output`, approval, review and flags and *displayed that take as its own*. The copy is built from `models.SHOT_PLAN_CONTENT_FIELDS` instead, with the classification partitioned against `Shot.model_fields` by a contract test, so an unclassified new field fails the suite rather than riding into every copy. `locked` and `flagged` are deliberately not inherited; `latest_output`/`approved_output` semantics are untouched, and the original keeps everything it had. Not seen in a browser
- [x] Asset-to-shot references
- [x] Director timeline JSON and H3 frame alignment
- [x] Text-only H3 Director shot submission with explicit model dependencies
- [x] H3 reference sampling profiles — **three** profiles, of which two were rendered live 2026-08-18 on the same sheet, shot, seed and window, one variable changed: turbo is not degraded, and it is **23% faster, not 5×** (182.7 s against 237.5 s), because sampling is not what dominates this graph. `H3Request.profile` picks one of two evidenced bundles: `default` reproduces the audited export (`simple`/`res_multistep`, 20 steps, no LoRA — the export's own `Power Lora Loader` holds a turbo LoRA with `"on": false`, which is why), and `turbo` reproduces the Director's saved H3 stage (`minimax_h3_ref2v_turbo_4step_v0.1_comfyui_bf16.safetensors` at 0.7, `beta`/`euler`, 4 steps). Each carries the step count its own evidence used; an explicit `steps` overrides it. **The default profile's payload is byte-identical to what shipped**, pinned by a digest taken at commit `7e25ad0`. An unknown profile is a 422 before any payload is built, and the pre-flight audits both — `LoraLoaderModelOnly` and the LoRA file are confirmed against live `/object_info`. The LoRA in isolation, and either scheduler paired with the other profile's sampler, still have no evidence at all — only the two bundles do. A non-default profile named on a text-only Director shot is refused with a 422 rather than silently ignored
- [x] Persistent Comfy prompt IDs, job refresh, outputs, and errors
- [x] Live render percentages on the asset card and the timeline Shot box — **socket and message shapes verified live 2026-08-20** against ComfyUI 0.33.1, from one authorised Flux render (512×512, 10 steps) whose every non-monitor frame is kept verbatim at `tests/fixtures/comfy_progress_frames.jsonl`. This build emits **both** `progress_state` (the node map, with `value`/`max` per node) and the older single-node `progress`; `GET /internal/progress` is a 404 and the WebSocket is the only per-step source there is. A backend listener (`comfy.ComfyProgressListener`, a dependency-free RFC 6455 client) folds them into an in-memory `prompt_id → percent` map that rides the **existing** AD-1 poll — no new route, no browser socket, no second timer, and an idle project still makes zero requests. **Only step-counting nodes are counted** (`max > 1`, which is the sampler): averaging the whole node map was measured wrong live, reading 100% for twenty-five of the render's twenty-six seconds. **Nothing is persisted** — a percentage in the manifest would move `Project.updated_at`, which `PUT /api/projects/{id}` compares. The percentage is text on both surfaces, composed with the existing `RENDERING` word and appended to the clip's accessible name; **unknown draws nothing at all**, and is a different state from a reported `0%`. **Model loading is unmeasured**: in the verified Flux render that was ~24 s of the ~26 s, so the card showed the plain word for most of it — an H3 video render is the opposite ratio. **Not verified**: the derived percentages after the parser was corrected were re-obtained by replaying the captured live frames through the shipped listener and route, not by a second GPU render; no browser QA
- [x] Purpose-built local editor UI
- [x] Song replacement/removal confirmation gate on all five Song-changing routes, and first-class song removal (`DELETE /api/projects/{id}/song`) — offline-verified 2026-08-17; the generic full-project `PUT` can still wipe Shots while carrying an unchanged Song, which is a separate concern
- [x] Reviewable Director document replacement (FR-16): single-slot recovery per document, per-document locks mirroring `Shot.locked`, a Director-free restore route, and a chat reply that names which documents changed — offline-verified 2026-08-17. **Single-slot is a decision, not a shortfall**: exactly one previous version per document, no history stack, and a restore swaps rather than pops. The Treatment workspace's restore buttons and lock toggles are **browser-verified 2026-08-17** by `tests/e2e_epic2_surfaces.py`: both reflect the stored project, and restore is disabled for a document with nothing kept
- [x] Document replacement is **opt-in per turn** (`apply_documents`, off by default, mirroring `apply_shots`) — offline-verified 2026-08-17. Asking the Director a question no longer rewrites the Treatment or Style bible; a declined turn writes nothing, touches no recovery slot, and the reply names which documents it proposed changing. **A declined proposal's text is not stored anywhere** — there is no proposal slot, so the proposed wording is gone exactly as a declined shot list is. Consent is per turn and is deliberately not remembered: the control is cleared when a turn finishes and when a project loads, so a Director who wants every turn applied must tick the box every turn. The composer checkbox is **browser-verified 2026-08-17**: unticked on load, and cleared by a project change. Clearing after a completed turn still has offline coverage only, because it needs a live model

- [x] Story expansion into fully prompted shots (FR-26): `POST /api/projects/{id}/director/expand` writes one prompt per unlocked Shot in a single whole-plan Director call, from the Treatment, the Style Bible and each Shot's position in the song — offline-verified 2026-08-17. Results are keyed by **Shot id**, never by position, so a Shot added, split or deleted while the model is thinking cannot receive another Shot's prompt; a locked Shot is never rewritten; no window is retimed; and **no render is queued**, asserted. **Song-section input is an explicitly empty branch**: FR-26 asks for section boundaries "when analysis exists" and nothing in this project produces them — there is no BPM or section field on any model, the analyse-structure button is a disabled stub, and `#section-track` is never populated — so `timeline.song_section` is a named empty slot, the key is omitted rather than sent as `""`, and a test asserts both the absence and that no analyser data source exists. The Treatment workspace's "Expand shots into prompts" button has **not** been driven in a real browser

- [x] Shot Plan readiness gate (FR-26): `batch.readiness_report(project)` is the one derived answer to "is this plan fit to submit", exposed at `GET /api/projects/{id}/readiness` and enforced on every path that can submit a Shot — offline-verified 2026-08-17. **Only emptiness blocks**: a Shot whose prompt is empty, whitespace-only, or still carrying the `"New shot"` placeholder every new Shot is created with is refused with a 422 naming it, checked at the route before any payload is built and before the reference branch's `Reference map:` interpolation turns an empty prompt into a populated string. **Sameness only warns** — two prompts identical after lowercase and whitespace collapse, or sharing more than 90% of their tokens, are reported as lacking variance and the plan stays submittable, because a repeated beat can be deliberate. The gate is deliberately **not** keyed to `Shot.status`: nothing in the shipped UI ever writes `ready`. Readiness is derived on every call and **never stored** (AD-5); no new `Shot` field. The queue-ready button checks readiness once before its loop, so a batch is never half-submitted, and an unprompted clip says `NO PROMPT` on the timeline instead of borrowing the `Untitled shot` fallback. **The pre-flight surface is Epic 4 (Story 4.2)** — this ships the backend gate it will consume, not a modal. The timeline flag and the readiness region are **browser-verified 2026-08-17**: the unprompted clip reads `NO PROMPT` with a dashed border while the written clip does not, and the region names the blocking Shot in the server's own words. The batch refusal is still offline-only — `#queue-ready` cannot be enabled from the shipped UI

## Accepted by the Director, specced, not yet built

> **Keyframes inside references mode shipped 2026-08-18** after the Director corrected the ceiling - a references shot now cites `first`/`last` pictures alongside its identity references and keeps the windowed song, with the guide's own declaration wording in both prompt channels (the winning specialist rule measured 12/12 guide-form anchoring against a 0/16 baseline; prescriptions beat prohibitions three-for-three on this model). **Live-proven the same day**: one references render (137 s on turbo) with the pin declared in the submitted prompt exactly as the guide words it, the master song trimmed 30.0-33.75 s in the media state, frame 0 visibly the cited picture, and the performer mid-phrase by frame 45. Frames under `test-artifacts/2026-08-18-render-comparisons/singing_keyframe_*`. **Still Ask First:** `middle` on references, and the builder's composition/storyboard/attribute-transfer picture roles - each a taxonomy addition of its own.

> **The H3 keyframe adapter is built, schema-audited and live-verified (2026-08-18).** The Director's own flow ran end to end — Flux first frame → cited in the `first` role → mark-ready → render — and every prediction held: **1056×608, 124 frames** (17×7+5, the node's own grid for 5 s → 5.167 s), 24 fps, generated aac audio present, `fl2va` in the submitted graph with `ref2va` nowhere, `first_frame` wired and `last_frame` genuinely absent. **Frame 0 of the take is visibly the cited image**, and by frame 100 the wolf has stepped from the treeline toward camera — the prompt's motion. Comparison frames under `test-artifacts/2026-08-18-render-comparisons/keyframe_*`. Render: 344 s at 20 steps. `first_last` (two frames) rendered the same path but is not separately live-verified. The lip-sync fact, learned from the schema rather than a silent render: `MiniMaxH3ImageToVideo` has **no audio input of any kind**, so keyframe shots cannot take the master song and a singing shot belongs in `references` mode; the pre-flight re-reads that fact live so an upgrade adding audio surfaces as a check failure. The LTX 2.5 I2V evidence (`ltx25-i2v-user-export.json`) stays imported as the alternative image-to-video path if fidelity ever argues for it. A `singing="singing"` keyframe shot deserves a readiness warning once readiness is mode-aware.

> **FR-21 done, live-verified in a browser (2026-08-18).** A take streams by id with real 206 Range service (scrubbing observed in Edge), and approval finally exists: one writer, written together with the status, reversible only by un-approve, every downstream refusal proven against a route-approved Shot. ~~Deferred to Assembly by design: the AD-13 window snapshot and a disk-existence check~~ **both landed with Assembly the same day** — see below. ~~**Open for a future ruling:** the generic `PUT` routes can still write `approved_output`/`status` from a body — the third appearance of the sibling-write-path pattern; making approval server-owned there would break the test-seeding idiom, so it is recorded rather than done. The snapshot fields inherit the same stance~~ — **ruled on and closed, offline-verified 2026-08-20.** Both generic writes now refuse a body that moves a Shot out of `queued`/`running`, and refuse a body that changes any of `approved_output`/`approved_start`/`approved_duration`, in the narrow form that preserves ordinary saves: the incoming Shot is compared against the stored one, so an unchanged round-trip saves every field byte-identically and `draft → ready` arming still works. The test-seeding cost measured **two tests, both about supersession and neither about approval** — the approval half broke nothing, so the recorded reason for deferring it turned out not to hold. The two were migrated to pin the refusal where the hole used to be. **Still open, deliberately:** a body may still write the `approved` *status* on a settled Shot — a restrictive fabrication that un-approve is documented to rescue, not a way to put an unapproved file into Assembly — and neither route gates a Shot *deleted* while its render is live.

> **The Director's user workflow, written down 2026-08-19** (`_bmad-output/planning-artifacts/user/UserWorkflow.txt` — that directory is unversioned, so the operative content is recorded here). Six stages for the first video, mapped against what exists:
>
> 1. **Song** — provide or generate, with lyrics + style. *Exists.* The first video's material is on disk and verified: "Harder Faster (Female Cover)" with the prepared lyric sheet, the raw character (`characters/HarderFaster.png`, red boots need prompting — not visible in the source), and the location (`locations/DaskWarehouseBed.png`), all under the ComfyUI input tree. Noted for later: **instrumental songs lean on the Treatment much harder** — environments/instruments carry the video when there is no singer to associate.
> 2. **Vibe (Treatment)** — one instruction seeds the concept. *Exists* (ProducerBot chat). The Director's own example is the first video's brief: verses at a standing microphone with action shots, choruses on a black-draped canopy bed with glamour angles and posed singing, in a darker moonlit empty warehouse.
> 3. **Assets** — starter assets + character templates. *Mostly exists* (upload, Flux generation, multiview promotion). **Two new asks:** ~~an **"AI Mod" button** per image asset~~ **AI Mod built and live-proven the same day** — the evidence existed ("MiniMaxH3 ImageEditing - API" + Turbo twin, imported and SHA-pinned): image editing is a `length: 5` reference render through `ref2va` with the dedicated image VAE, the base picture 2 MP-scaled into `ref_image_0`, pictures 2–9 raw (the storyboard/characters-into-scenes ceiling, Ask First). Instructions travel in the workflow's own bundled prompting guide (clothing/pose deliberately not in the preserve preamble — "change the outfit" is the point); `subject_definitions:` prompts go verbatim. Live proof was the Director's own example: **red boots on `HarderFaster.png`**, turbo, 1920×1056 — boots bright red leather, framing widened to show them, identity/stage/lettering preserved, child asset adopted beside its untouched source, re-edit accepted. ~~Still open from stage 3: the **"Asset Fill" button**~~ **Asset Fill built 2026-08-19** (route + Stage Manager persona + UI; count entry default 8, the Director's number): one model pass proposes the supporting assets the library lacks, each proposal queues an ordinary Flux render (keep/delete/AI Mod once landed), refused while any render is open (FR-9's eviction number in the sentence), GPU acknowledgement server-enforced. **Persona not yet live-iterated** — built during the first-video H3 batch, which owned the GPU; the first real pass is the natural iteration run.
> 4. **Timeline Population** — ~~the one-button orchestration does not exist~~ **built 2026-08-19**: the Populate Timeline button in Assets, destructive-warned in the workflow's own words and server-enforced (`confirm_replace`), refuses protected shots by name before the model is ever asked, and treats the model's layout as *shape, never arithmetic* — `populate_windows` repairs it into a contiguous 0-to-song-end tiling inside H3's 4–15 s band (the exact geometry assembly later demands), each window drawing its prompt from the proposal whose proportional span contains it. Shots land as drafts; assistant fill and expansion remain the next acts, per the workflow's own text. **The reason populate kept returning nothing was found and fixed 2026-08-20**: see "Populate's empty-`shots` failure was the response schema" in the timeline list below.
> 5. **Video Generation** — individual renders for testing (*exists*), then the main generation with a time/GPU warning and the Replace Existing checkbox — ~~Generate All / AD-5 batch submission still unbuilt~~ **built 2026-08-19**: one server-side POST, one server-enforced confirmation naming the count, per-shot skip-and-report in the single-shot routes' own sentences (FR-4 verbatim), `batch_id` minted per batch, Replace Existing re-opening settled unprotected shots, and AD-5's `flagged` mark with flagged-set resubmission clearing flags only on success. Every submission rides the identical single-shot handlers, and FR-9 holds by construction (all `h3`, consecutive, nothing evicting the resident stack). **Audio ruling renegotiated by the Director in this document**: the video line and audio line should be mutable and **each clip individually mutable, so "only the main music track and accepted audio from videos would come through"** — per-clip opt-in mixing, which the 2026-08-16 song-only ruling had explicitly parked as the v2 refinement. Now in scope.
> 6. **Final Render** — export the composition as a playable file. *Exists* (FR-22 assembly), pending the stage-5 audio mix extension.
>
> **The Monitor and the over-render margin: built and live-proven (2026-08-19).** Two Director rulings from watching the first export, delivered together because they interlock. The **Monitor** — the timeline's program viewer — plays the shot under the playhead against the master song: the master audio element is the clock, the video a muted view of it, gaps and unrendered shots an honest placeholder. The **margin**: every take now renders at least half a second longer than its window (107 frames for 3.75 s, never 90 again), song-audio takes carry a quarter-second sync lead recorded on the shot at submission (`latest_take_lead` — recorded because it cannot be derived later), and the inspector's **Trim nudge** slides which slice fills the window, frame-stepped, previewed by the Monitor and cut by assembly through *one* offset rule (`lead + nudge`, contract-tested across the pair). Assembly probes take lengths up front, so an off-the-take cut is a plan refusal with the numbers, not a mid-pipeline failure — and the cut is a frame-exact `trim=start_frame`, proven in pixels (a green-lead/blue-body take assembles blue at the lead, green when nudged to zero; same file, same approval). Every H3 payload digest was re-pinned deliberately for the margin, each moved literal verified by eye first. Live proof: the singing shot at 12 s resubmitted on turbo carried `length: 107` and trim `11.75–16.208` in the queued graph, lead 0.25 on the manifest. **The band's short end was decoupled from the render length 2026-08-20, offline only.** The Director ruled that a sub-4-second window is legitimate music-video editing — "some videos even use micro-cuts for fast paced stuff … Shots still generated to the 4 second minimum and they just have a bigger invisible 'buffer' while still being lined up so the exposed part matches" — and, the same day, that the buffer is **centred**: "if a shot is below the minimum typical then the math would just center around that clip … spanning equally both directions." `over_render_frames` now floors every take at `H3_MIN_RENDER_FRAMES` (107 frames, 4.4583 s, H3's own 4 s minimum on the grid) and `over_render_lead` answers half the buffer, snapped to a whole frame, whenever that floor fired. A 2.083 s window at 12 s is submitted as `length: 107` with the master trimmed `10.7917–15.25` and `latest_take_lead` 1.2083 — assembly cuts frame 29 and takes 50, and frame 29 of that take is song second 12.000 exactly. Windows at or above ~3.2917 s are unchanged frame for frame and byte for byte, pinned against the pre-floor expression. Clamps at 0 s and at the song's end move the buffer and never the exposure. **Windows outside 4–15 s are now visible in `readiness_report`** as a third list, `window_warnings`, with a machine-readable kind per note — never blocking, because `generate_h3` accepts them and a pre-flight stricter than its own route is a pre-flight that gets worked around. **Over 15 s warns rather than blocks**, per the Director ("it should turn yellow but we arent dead yet"), and the note says plainly that extension is not a way out today: `build_ltx25_extend_payload` exists and is audited, but no route submits it and `SHOT_MODE_SPECS["extend"]` carries no adapter. **Nothing was rendered against any of this** — the claim is the arithmetic and the submitted graph, not a take. **Follow-ups recorded:** restore-song-audio still windows by the bare shot window (misses the lead on post-margin takes; Ask First, and larger now on short shots); `audio_replace_lengths` still uses the pre-margin frame formula for its reported picture length; the text-only Director graph does not centre (lead 0, no song window to align); the browser draws `window_warnings` as of 2026-08-21 — the long end takes the Director's amber on the clip with its reason in the accessible name, the short end deliberately draws nothing, and both are listed in the readiness report under their own labels; `extend` likely forward-only per the Director; ~~a browser e2e for the Monitor~~ — **built 2026-08-19** (`tests/e2e_monitor.py`, seven sections, all passing in a real Edge: take-on-load, honest placeholder, seek accuracy, acceptance un-mute, frame-stepped nudge, both line mutes, clean console). **A re-rendering shot now says so on the Monitor, the takes strip and the clip — offline-verified 2026-08-20, not seen in a browser.** The Monitor decided purely from `latest_output` and `.showing-take` hides its only text layer, so a displaced take played in sync and framed exactly like a settled one; the strip labelled that take `Current`, which is a wrong claim rather than a missing signal; the clip carried the state as a border hue with nothing in `aria-label`. `monitorState` now returns a `previous-take` kind carrying the server's own in-flight sentence, drawn in a `#monitor-note` band that survives a picture; the strip reads `Previous` and appends a pending row; the clip carries the word `RENDERING` and the sentence in its accessible name. **The take keeps playing and `latest_output` is untouched** — the pointer's provenance lesson below is exactly why the fix states the state instead of clearing it.
>
> **FR-22 Assembly: built and live-proven (2026-08-18).** The finishing lane's main act, AD-9 verbatim: local ffmpeg only (`comfy.prompts` empty on every path, refusal or success), every approved take trimmed to its window on one cumulative 24 fps grid (per-clip rounding cannot accumulate; the trim defeats the ~11 % grid-overrun drift), joined in shot order, master song as the sole audio track, export verified within one frame of the song before it reaches its public name under `media/exports/`. Refusals are **comprehensive**: one 422 carries every unapproved/stale/legacy/missing-take shot and every gap/overlap against the song at once. AD-13's window snapshot landed with it — approve writes `approved_start`/`approved_duration`, un-approve clears them, assembly refuses a moved window by shot ID (proven live: a 0.25 s window nudge refused with both windows *and* the resulting gap in one report; restore reassembled clean). Recorded as a `RenderJob` kind `post` with **empty `prompt_id`** — the local-work marker `reconcilable_jobs` and the frontend poll already key on — carrying the consumed takes in the new `inputs` field (FR-24 adapted); an orphaned `running` local job (app restarted mid-run) is healed to `error` **at application startup and again at the next assemble** — one rule (`app.heal_orphaned_local_jobs`), called at boot with the empty registry a just-started process actually has, so a project reopened after a crash no longer shows an export nothing will ever finish (widened from assemble-only on 2026-08-20; offline-verified, no crash reproduced live). A job carrying a `prompt_id` is never healed by our own restart: ComfyUI is user-managed and may still be rendering, so those stay the reconciler's. The Assembly bar sits at the foot of the timeline: cheap readiness client-side, the server's refusal report verbatim, the export playable in place. **Live proof on `project_21e5a260c3a7` ("Assembly Live Proof")**: the two real singing takes over a 7.5 s excerpt cut from exactly the windows they lip-synced to — 180 frames, 7.500 s against a 7.500 s song, 1056×608, both takes byte-identical afterwards. Mutation pass on the pure half: 18/18 killed. **SM-1's remaining gap is now content, not machinery**: a full plan of real renders, approved and assembled.

> **The driving milestone, set 2026-08-18: a 60-second music video, end to end.** In the Director's words: *"have it go through and set up a 60 second music video all the way up to being ready to run the video generation chain. Its own generated track, characters, scene, b-roll, and shots linked together appropriately with the music. It should utilize the lypsinc when the shot calls for it and have good video composition and assembly for a music video."* Everything in this section should now be read against that milestone. Both analyses are delivered under `_bmad-output/planning-artifacts/`: `sixty-second-video-gap-analysis.md` (the adapter truth table, the finishing gap — nothing writes `approved_output` and no assembly code exists — and a dependency-ordered sequence) and `timeline-centric-layout-analysis.md` (Premiere-style mapping; **zero wizard code exists**, so superseding FR-2/FR-3 abandons nothing built, while FR-1's derived-never-stored rule survives as an invariant).
>
> **Found live by the Director the same day: completed jobs never reach the UI.** There is no automatic polling anywhere in the frontend — the only timer in `app.js` is toast dismissal, and `refreshJobs` is bound solely to the manual queue-panel button. A Flux render completed in ComfyUI while the asset card showed "Rendering" indefinitely; the silence then invited a second click, and the form's fixed seed made ComfyUI render the identical image twice. The architecture spine's transport decision (2 s polling while a batch is active, one `/queue` fetch per tick) was recorded on 2026-08-16 and never implemented. **Fixed the same day, live-verified in a browser against a scripted ComfyUI double**: the AD-1 reconciliation endpoint and 2 s conditional polling are in (idle project = zero requests, completion reaches every surface with no click, polling stands down when the last job settles), the generation submits disable while in flight, and expansion gained up to 3 automatic retries with corrective feedback (worst case: a 30-shot sweep is 120 calls). Still unbuilt from AD-5/Epic 4: server-side batch submission with FR-9 kind grouping, `batch_id`/`flagged`, and flagged-set resubmission.


> **ProducerBot writes the wrong shape of prompt, found 2026-08-18 on its first live run.** H3 does not want a casual sentence: MiniMax publishes a 20-page format guide, and a rewriting model (`H3-Context-IR`) was supposed to turn an idea into it - **that model was never open-sourced**, which is exactly the gap the assistant now fills. The format is three named fields (`integrated_multimodal_description`, `overall_soundscape`, `non_diegetic_music`), with `[Shot N] At MM:SS.mmm` cut markers, `(S1)` speaker ids, `<d>[English] ...</d>` dialogue, `<Picture 1>` tags, and camera motion written as type + amplitude + speed. The assistant currently emits one descriptive sentence. It is also **checkable** - shot numbering, monotonic cut times, balanced dialogue tags and undefined references are all verifiable before a render. See the planning artifact for the derived rules; the guide itself is third-party and is referenced rather than copied.


> **Resolved 2026-08-18 by a three-way comparison — see the development log.** The audio window bought *coherence* (a properly formed face and real mouth shapes); the resolution bought *detail* (teeth, eyeliner, makeup, hair strands). A control render at the old 640×384 with the new window isolated the two, and it mattered: either fix alone would have supported a confident wrong verdict about H3.
>
> **Every quality verdict recorded before that date remains void, though.** The 4-step versus 20-step comparison, the turbo profile assessment, the "coherent cinematic frame" verdict — all were made from live renders at **640×384, 0.25 MP**, a size chosen only to make a smoke run cheap. The Director's own pipeline selects 0.6 MP. On a full-body framing the face occupies the same *fraction* of frame either way, so ours landed on tens of pixels of height, which is the reported "insanely poor quality details in the face". Two further faults compounded it: the test shot sat at 0 s where that track has no lyrics (singing starts at 8–10 s), and the reference audio was never windowed at all, so a 3.75 s shot was conditioned on the whole 154 s song. **No conclusion about H3 fidelity should be carried forward from any render made before this date.**
>
> Worth naming, because it is now three for three: **4-step sampling, `start = 0.0`, and 640×384 were each chosen to make a test cheap, and each became the value every conclusion was drawn from.** Nothing in a passing test says "this sample is not representative."


> **Built and verified live 2026-08-18.** `POST .../shots/{id}/restore-song-audio` puts the master's own window back over a finished picture. Measured: **+0.9945 correlation at lag 0.000 s** against the master's 12–15.75 s extracted independently, with 90 frames and 24 fps preserved and the take untouched as a sibling file. H3 still generates its own output audio by design — that has not changed and should not. The graph names **zero model files**, so this costs no GPU time. Not yet wired into assembly (FR-22) or take comparison, and there is no UI.
>
> **Provenance lesson, hit live by the Director 2026-08-18:** the stage follows `latest_output`, and any re-render displaces that pointer — so `song-audio_00001` on the comparison project silently carried the picture of the 640×384 *control* take, not the good 1056×608 one, and the Director saw "really bad facial details" with no clue why. Once approval exists on a shot, finishing stages (restore, enhancer) should prefer `approved_output` — that is AD-12's own rule ("the standalone adapter takes the approved file as input") and belongs to the take-comparison/finishing story.


> **Resolved 2026-08-18, live-verified.** A reference shot with `use_song_audio` was handed the entire song file with no offset, so H3 lip-synced to the track's opening wherever the shot sat — masked because every live render had used `start = 0.0`, the one value where the bug and correct behaviour coincide. Fixed: the master song now carries a `trim` of the shot's own window (`song_audio_window`), confirmed in the submitted graph read back from `/history` (`{"start": 12.0, "end": 15.75}`), and the three-way face comparison in the development log shows what the correct window buys — a coherent mouth making phoneme shapes. The restore-song-audio stage shares the same function structurally and measured **+0.9945 correlation at lag 0.000 s** against the master's own seconds. The stale half of the old warning stands corrected too: post-fix renders are trustworthy evidence; pre-fix ones remain void.


Each of these was decided on 2026-08-18 and has a written spec under `_bmad-output/implementation-artifacts/`. They are listed here because that directory is not versioned — only `deferred-work.md` is — so this is the record that survives a clone.

- **LTX 2.5 enhancer adapter** (`spec-ltx25-enhancer-adapter.md`) — submit an existing take to the standalone enhancement graph instead of regenerating H3, the long-standing gap in the render pipeline. Evidence imported and hash-pinned as `ltx25-enhancer-user-export.json`. Note before building: `LatentUpscaleModelLoader` and the audio VAE in that export are **orphaned**, so dependencies must come from the reachable subgraph — there is no latent upscale in the executed path despite the node's presence.
- ~~Browser QA for four controls~~ **Done 2026-08-18.** `tests/e2e_shot_controls.py` and `tests/e2e_song_context.py` drive mark-ready/mark-draft, render-again, multiview promote, the song-context editor and the VRAM eject toggle in a real browser. Both **start and prove their own server**: a bound port is refused by name, the listener must be the run's own descendant, and the responder must write into a data root created seconds earlier — because a health check that only proves *something* is listening proves nothing about *what*. **Three defects found that the stub-DOM harness structurally could not catch** and are not yet fixed: a toast overlays the shot inspector's own buttons for 4.2 s (a scripted click landed on it); three controls are hidden by media queries below ~1180 px and ~860 px, including the eject *note* disappearing while its toggle stays live; and the app rebuilds the inspector after replies it does not await, so elements go stale mid-interaction.
- **Arm a whole plan** (`spec-arm-a-plan.md`) — report what would be armed and skipped with reasons, arm only on confirm. **No longer on hold; the tension was a misreading of my own.** Arming and generating are two acts and the PRD already separated them: FR-4 is Generate All and always queued, FR-5 is per-clip regeneration, and this story is neither — it is bulk *arming*, moving shots from `draft` to `ready`. The frozen "never queue" is the boundary between them rather than a disagreement with the Director, and collapsing the two into one control is exactly what it prevents. **Replace Existing belongs to Generate All**, since it answers a question about spending GPU rather than about eligibility. The report step survives on its own merits: "22 armed, 3 skipped" is the moment a Director notices that three is wrong, which holds however earned the confidence is. Now that `mode_specification_problems` exists, it should report a first/middle/last shot missing its middle image as unarmable *with that reason*, not merely as "no prompt".
- **A live check of the VRAM eject control** — with a model loaded, submit with the control off and confirm nothing was ejected, then on and confirm the release names the model. Costs no GPU beyond one cheap render.
- **Render `turbo-references2v`** against the shipped `turbo`, one variable changed — open item 15 above.
- ~~Multiview for objects~~ **Built and verified live 2026-08-18.** A character and a cargo ship were both promoted through the shipped path: the character sheet holds identity, wardrobe and hair across a face detail plus front, profile and back; the ship sheet holds hull, panel lines, turret and boarding ladder across four angles. The child asset inherits its source's kind, so a promoted ship stays a `prop`. **The two extra sampling passes were reverted, not shipped** — see the development log: `Music-Video.md` is wrong about that graph in five ways, and our adapter already matched the creator's active sheet path exactly.
- ~~LTX 2.5 enhancer adapter~~ **Built and verified live 2026-08-18.** 640×384/90 frames → 1920×1152/89 frames, audio surviving, the take untouched. **89 = 8×11+1 confirms the 8k+1 load grid**, against H3's 17k+5 input. **But it is not an upscale**: sampled at three frames the source is mid-vowel while the enhanced clip has the mouth closed, because `ManualSigmas` starts at 0.909375 — substantial re-generation. **It breaks lip-sync and must not be used on singing shots** until the starting sigma is tuned, which is an Ask First decision. No UI, and take comparison — what would make a second output meaningful on screen — is still unbuilt.
- ~~LTX 2.5 video-extension adapter~~ **Built and schema-audited 2026-08-20. Nothing was rendered.** `build_ltx25_extend_payload` reproduces the whole reachable subgraph of `ltx25-videoextender-user-export.json` (61 nodes, **all 61 reachable**) as 49 API-format nodes, 46 with `include_audio=False`. `tests/preflight_ltx25_extend.py` validated 43 classes and all five model files against live `/object_info`; both variants clean. The claim is that the payload validates, **not that it renders** — no LTX render was submitted and no frame from this graph has been measured. **No route and no UI**, and `SHOT_MODE_SPECS["extend"]` still carries `adapter=""`: when extension is offered, and to which takes, is a separate editorial decision. Three findings worth carrying forward. (1) **`LatentUpscaleModelLoader` is genuinely reached here**, where it is an orphan in the enhancer and audio-replacer exports — the reachability walk gives opposite answers for the same class name, and the remembered rule of thumb would have dropped a model this graph loads. (2) **`ManualSigmas` runs `1.0, …, 0.421875, 0.0` on the base pass and `0.85, 0.7250, 0.4219, 0.0` on the refine pass** — heavier re-generation than the enhancer's 0.909375, applied to the reference tail as well as the new seconds, so the seam second comes back out of the model rather than being spliced. Per the Director this is a **characteristic, not a blocker**: the realistic use is a long b-roll cut, lip-sync will not survive an extended singing section, a shot singing continuously past 15 s is an unusual ask, and the strategic value is short-film production rather than music video. Nothing in the code refuses a shot on those grounds. (3) The **no-audio export is not built**: 11 of its 65 nodes are orphaned (its abandoned audio tail) and it mis-wires `LTXVEmptyLatentAudio.frame_rate` to the extend-seconds constant on both instances, so transcribing reproduces a defect and correcting it invents a graph. It saves no models and no classes; recorded as available and audited-second.
- **Shot mode and asset roles** (`spec-shot-mode-foundation.md`) — the keystone for the whole shot-mode direction. A shot's kind is currently *inferred* from whether `asset_ids` is empty, so it cannot declare what it is or be wrong before a render. Roles move onto the shot's citation of an asset rather than onto the asset itself, because the same wolf is a middle frame in one shot and a reference in another. Load-bearing test: a **byte-identical payload assertion** across the change, since it touches the branch that decides what every render is.
  - **Whether the performer is singing is part of this**, added 2026-08-18 after the enhancer was measured to move lip position. The Director's constraint is per shot, not global: a character lying on a bed has no lip-sync to protect and the enhancer is pure gain there. It is a property of the *performance*, not of the mode — a references shot may or may not be a singing shot — so it is its own declared attribute and **nothing infers it**. A shot whose singing state was never set is *unknown*, which is not the same as "not singing"; what an unknown state should do at the enhancer is an open question the Director has not been asked.
- **SongPlanner `max_duration` headroom as a bounded request field**, defaulting to the creator's documented 1.5×. `duration_seconds` tells the planner how long a song to write; `max_duration` caps the encoder's latent length, and passing the same value to both leaves no room for lyrics that run long. Both live runs were at the 30 s floor and returned 29.989 s — exactly where this would never show. Exposed rather than hardcoded because `Music-Video.md` states the rule while the audited export sets both to 200, and that contradiction should be visible.
- ~~Assistant ProducerBot~~ **Built 2026-08-18, persona not yet iterated.** `POST .../assistant/fill` gives the Director's own model one tool, `fill_shots`, whose wire schema is **generated from `ShotMode`/`AssetRole`/`SingingState`** rather than transcribed, so the enums the model may say cannot drift from the ones `models.py` declares. **The selection is the consent** — `shot_ids` is required and decides both what the model sees and what it may write to; a call naming any other shot is refused. That is stronger than chat's `apply_*` boolean because it cannot be left ticked. Guards are shared with expansion (`shot_write_refusal`, same wordings verbatim), so **marking a shot `ready` takes it out of the assistant's reach too**. No path spends GPU. Nothing infers `singing`. A single terminal `store.save` is what prevents a half-applied manifest.
  - **The two open questions from the planning artifact are now answered**: tools inherit the existing refusals *plus* a selection scope, and a bulk fill writes shot by shot with a per-shot report rather than one reviewable change-set.
  - **Standing item: the system prompt is written, not iterated.** It lives in `src/music_video_producer/assistant_prompt.py` in its own module so rewording it touches no transport, route or behavioural test, and it is meant to be edited against real output. No model was loaded when it was built, so the one acceptance this change cannot claim is that the persona produces good one-shot setups. `tests/smoke_assistant_lmstudio.py` is the cheapest way to check — no app, no manifest, nothing written.
- **Vocal transition points between shots — the Director ruled, and the ruling shipped 2026-08-20.** Flagged 2026-08-18 after watching the first two windowed singing takes ("vocal transition points will have to be worked on if we want them done kinda automatically and tuned by the user"). Each references shot performs its own song window, so at a cut the mouth of shot A's last frame and shot B's first frame are uncoordinated. Three levers were in the box — keyframe pinning, a crossfade, cut placement — and **the ruling is cut placement snapped to phrase boundaries**: it removes the problem rather than masking it, since there is no mouth to mismatch if nobody is singing across the cut, and it costs no GPU and no re-render. **Built**: `timeline.snap_cut_plan` — pure, I/O-free — proposes a new position for every cut against the track's measured voiceless stretches, and `POST .../timeline/snap-cuts` reports what would move and what would stay before writing anything, applying only on `confirm_apply` (`populate`'s shape, server-enforced). A cut is treated as *shared*: the plan is carried as a boundary list, only interior boundaries are ever assigned, the two outer edges are copied through untouched, and a shot no cut of which moved keeps its stored floats to the bit — so contiguity is structural and an untouched approved shot cannot be nudged into assembly's staleness refusal by an ulp. The **tolerance is the tunable** the Director asked for (default 0.75 s, pinned to `merge_vocal_spans`' own rest, bounded at 3 s by the request schema); **0 is the feature off** and is a genuine no-op that reads nothing. A move that would push either neighbour out of H3's 4–15 s band is refused *for that cut*, with the length and the bound in the sentence. **Locked, approved and rendering shots are never moved**, each refused by name — an approval is refused rather than silently un-approved, since AD-13 snapshots the window and a protection that vanishes with what it protected was never a protection. A song with no Whisper alignment is an explicitly empty branch (**neither** `lyric_words` nor `vocal_spans` is *unmeasured, not silent*), refused with a sentence pointing at Analyze structure. **Corrected 2026-08-20, measured the same day**: the gaps came from `Song.vocal_spans`, which is `merge_vocal_spans`' output and bridges every rest under 0.75 s so the vocal *band* does not flicker — a display threshold that was deciding where cuts go. `timeline.vocal_gaps` now reads `Song.lyric_words` when there are words and falls back to the spans when there are not, and on the Director's own song (154.6 s, 262 aligned words) that is **3 interior gaps of 0.6 s or more before, 7 after** — 5 usable gaps in total before, 12 after. `merge_vocal_spans`, the band and `shot_vocal_overlap` are untouched; only cut placement changed. Rests too short to hold the 0.15 s clearance at both ends are not gaps at all (`SNAP_MINIMUM_GAP_SECONDS`), which is what keeps the 21 sub-0.30 s pauses *inside* a phrase on that same song from being offered as cuts. Each move in the report now states **how long** the gap it landed in was, on the Director's framing that a 1 s gap is an extended shot where a 4 s gap is room for something else — the length only; **no shot-type suggestion or b-roll planning is built**, and that remains PRD material. The claim is only that **cuts can now be placed where nobody is singing** — nothing has been rendered against a snapped plan, so whether that actually reads as a clean transition on screen is unmeasured. The other two levers stay in the box, and assembly's master-track mux still makes the *audio* continuous regardless
- **Timeline viewport: scrolling and zoom — browser-verified 2026-08-21.** The Director's report was *"I cant scroll left or right and i see what i think is a zoom slider that isnt functional"*, and neither half was what it looked like. **There was no zoom slider**: the only range input in the panel was `#master-volume` in the transport row, a working volume control. One was added beside the `−`/`+` buttons, logarithmic over the same 6–64 px/s the buttons clamp to, with `renderTimeline` writing the thumb back so the three controls cannot disagree. **And the timeline could already scroll** — `overflow: auto` was set, the canvas was already sized to `duration × pixelsPerSecond`, `scrollWidth` really was 2564 against a 1125 px viewport, and all four tracks moved together when `scrollLeft` was set from script. Its **horizontal scrollbar was laid out 61 px below the bottom of the window**, behind `overflow: hidden` on both `.timeline-panel` and `.workspace`, because `.timeline-layout` claimed `height: calc(100% - 65px)` — a hardcoded heading allowance that counted neither the snap bar nor the assembly bar — and `min-height: 500px` refused to give the shortfall back. The **Assembly bar was 150 px off the bottom for the same reason**, which nobody had reported. The panel is now a grid whose timeline row takes the real leftover, and `.timeline-main` shares its height between the Monitor and the tracks in proportion rather than giving the Monitor a fixed 46 vh (measured: 442 px of picture against 127 px for four tracks that occupy 313 px). Zoom now holds the **playhead when it is on screen and the centre of the visible band when it is not** — never zero — held to 0.08 px and 0.04 s respectively in the browser; the plain wheel scrolls along the song, Shift+wheel scrolls down, Ctrl+wheel still zooms about the pointer. **Nothing new is persisted**: zoom already rode the browser session, the scroll offset is stored nowhere, and no manifest field was added. The **coordinate maths needed no change and that was established, not assumed** — the drag handlers work on `clientX` deltas and the absolute conversions read the canvas's own rect, which already carries the offset — and it is guarded by a browser drag *and* resize at a 540 px scroll offset asserting the **stored** window. **The Director's follow-up the same day moved the edit tools and the zoom out of the panel heading and down into the bar under the Monitor** — "next to the thing they are actually manipulating" — which puts fifteen controls and two sliders in one row, so the bar **wraps** rather than hiding anything (42 px at 1600 px wide, 72/94/107/76 at 1280/1024/900/820, every control hit-tested at all five). Split, Duplicate and Delete became **icon buttons** on the Director's ruling ("use button icons with tooltips when needed"), each carrying an `aria-label` identical to its tooltip because a glyph is not a label, with Delete keeping its destructive styling (asserted on painted colour, not on the class list) and ＋ Shot keeping its word because a bare ＋ is what the zoom-in button beside it already says. **Both sliders now carry a visible label** — `ZOOM` and `VOL`, real `<label for>` elements — since two unlabelled sliders in one row is the confusion the original report came from; `#master-volume`'s behaviour is unchanged and still session-only. The width sweep also found a **pre-existing** overflow: `.timeline-layout`'s 620 px first-column floor needed 912 px in the 750 px a 900 px window leaves, so between the 860 px breakpoint and ~1006 px the panel clipped controls out of reach (measured: the song mute at 900 px). Floor lowered to 320 px. `tests/e2e_timeline_scroll.py` is the gate; nine executed contract cases cover the pure halves and the markup; **19/19 mutations killed**, six of them catchable only in a browser. **Found and not fixed**: a clip dragged over its later neighbour has its right resize handle covered by that neighbour, a consequence of overlaps-as-layers that was not reconsidered when overlaps became legal; and `tests/e2e_shot_controls.py:364` pins a `render-again` tooltip that `RENDER_AGAIN_HELP` does not carry and did not carry at `7218c06` either, so that script fails at baseline
- **Switching a shot between its takes — browser-verified 2026-08-21.** The Director: *"i can see the takes in the shot info window but clicking on either does nothing instead of hot swapping between available shots."* Two candidate causes, separated in a browser before anything changed: clicking the **row** left `latest_output` untouched, clicking the **chip** switched it correctly — so it was the **affordance**, and the **stale-handler** cause (this panel's recorded defect, where a reply nobody awaited rebuilds the inspector and elements go stale) was **not** what was wrong and nothing was changed for it. The row was a `<span>` of text with a 40 px live button at its right end; the whole row is a real `<button>` now, so focus/Enter/Space come from the platform, and the current and pending rows are the same button `disabled` — dimmed, unfocusable, no pointer — which is *deliberately closed* rather than the silently inert it was. Each row also states **which render it came from** (the seed and when the take landed, from the job record), because `Take 1 · …-h3-reference_00001-audio.mp4` truncates to an ellipsis exactly where its serial is and two takes read as the same line twice. `tests/e2e_take_swap.py` proves the swap end to end on two real ffmpeg-synthesized takes: the row click, the Monitor following, the keyboard, the closed current row, and the swap **surviving a full inspector rebuild**. **Not built**: a poster-frame thumbnail per row, which would make the strip usable for judging rather than only for identifying. **Found and not fixed**: the Clips library draws each take as a `<video>` pointed straight at ComfyUI's `/view` rather than through this application's take route, so every card 404s whenever ComfyUI is down
- [x] Undo/redo over the shot list — **browser-verified 2026-08-21**, and deliberately **not** a command history. The Director's report was *"I accidentally hit split on a clip, but there is no Undo button, so the first shot is split in 2 now."* What shipped is a bounded snapshot stack (40 deep) over `project.shots`, replayed through the same `PUT /shots` every other timeline edit goes through — the manifest is one atomic document and the shot list is one field of it, so a snapshot of that field *is* a complete description of the plan, where a command history would need an inverse per gesture and each inverse would be a second implementation of the rule its gesture already encodes. **Every landed shots save is recorded**, with the six destructive gestures named on the button ("Undo the split", "Undo moving a shot") and the inspector's own writes falling back to a generic edit. **Three safety properties, none of them a new rule**: an entry is created only when the server confirms the write, so an undo of something that was never applied cannot exist; the undo write carries the revision the stack is valid against as `PUT /shots`'s own concurrency token, so a writer this client never saw is refused by the route in its own words (`PROJECT_CHANGED_REFUSAL`, now a named constant used by both manifest writes and pinned into `api.js`); and a snapshot taken before a render carries that shot's pre-render `status`, which `_require_in_flight_status_kept` refuses. **The server-moved rule is refuse-and-drop, never revert**: a change this client adopted discards the history on the next render and re-takes the baseline, so the next gesture is undoable against the plan that writer left; a change it did not see is refused at the route. **Redo shipped** (Ctrl+Shift+Z / Ctrl+Y), cleared by any new gesture. Ctrl+Z is guarded off editable elements, so it is still the browser's own text undo inside a prompt box. `tests/e2e_timeline_edit.py` proves each of the six gestures restores the exact stored plan, a six-deep chain back and forward, and the refusal path against a real outside writer; 23/23 mutants killed, two of them only by the browser. **Not built**: undo across a reload (the history is session state), and undo of anything that is not the shot list — approvals, takes and documents each have their own route and their own recovery
- [x] Snapping modes: cut edges to the playhead — **browser-verified 2026-08-21**. *"Snap to timeline play marker also good so i can line up to beats in the music then snap the Shot edges to it."* A magnet on the existing edge drag rather than a new button, measured in screen pixels (8 px) so it feels the same at every zoom, off while the song is playing, and switchable from the tool bar (session-only, announced in words as well as `aria-pressed`). **It moves the shared boundary**: the neighbour that meets the edge within `BOUNDARY_TOLERANCE_SECONDS` follows it, so the plan stays contiguous — which the freehand drag does *not* do, and is how the Director's live plan came to hold four sub-frame gaps. Freehand dragging is deliberately unchanged. The only length refusal is a zero-length window; nothing here forbids a short one. **Ripple editing is still not built**
- [x] Double-click a shot edge beside a gap to close it — **browser-verified 2026-08-21**. *"when i shorten one clip and leave a gap … double-click on a shot edge next to that gap … extend over to fill that gap."* Both edges, the song's own head and tail when there is no neighbour, and **any gap above a microsecond** — the Director's live plan carries 0.0017 s, 0.004 s, 0.014 s and 0.015 s gaps, all well inside assembly's tolerance and all of them gaps they asked to close. The edge lands exactly on the neighbour rather than being re-quantised to the frame grid. The neighbour is the *adjacent* clip in song order, so an already-overlapping edge is declined rather than being stretched over the clip beside it. Both shots at the resulting cut are asked, in `timeline.cut_move_refusal`'s three sentences verbatim and in its order. **Found in a browser and not visible anywhere else**: a `dblclick` listener on a clip is dead in this panel — `pointerdown` re-renders the track and replaces every clip node, so the browser has no common target left to dispatch at — and the gesture is a hand-rolled press-pair instead
- [ ] Ripple editing
- [ ] Beat/BPM analysis and multiresolution waveform peaks
- [ ] Automatic verse/chorus/bridge candidates — no analyser exists, so nothing in the project can name the section a Shot sits in. `timeline.song_section` is the one empty slot waiting for it; the shot-expansion input omits the key entirely rather than sending a fabricated boundary. A song carrying a lyric sheet does **not** change this: section tags in a supplied sheet carry no timestamps, and nothing reads them
- [ ] Lyrics alignment lane — **the Director named the method 2026-08-19: Whisper.** Transcribe the master song with word-level timestamps (faster-whisper runs fine on CPU for a 2.5-minute track, so it costs no render VRAM), then align the transcription against the lyric sheet's blocks to (a) *propose* section boundaries automatically instead of the Director marking them by ear, (b) hand each shot the exact line(s) sung over its window instead of `clip_position`'s within-block estimate, and (c) warn when a shot boundary cuts a sung line mid-word — the Director's cut-off concern. Whisper on singing is imperfect (hard-rock vocals especially), but the sheet is known text: alignment is matching known words to heard time, far easier than open transcription. Sections (shipped the same day) are the manual fallback and the schema Whisper would fill.
- [ ] Director of Photography pass — the Director's note from live review: camera/motion/focus/effects laid out as a separate agent pass over the whole plan, so movement is composed ("there doesn't always have to be crazy camera movement") rather than each shot inventing its own. Natural home: a specialist like the H3 expansion's, writing/editing the camera clauses across all expansions with the section map in view
- [x] Populate's empty-`shots` failure was the response schema — **root cause found and fixed 2026-08-20, measured live the same day (run 4)**. `DirectorResult.model_json_schema()["required"]` was `["message", "treatment", "style_bible"]`: `shots` and `sections` both carry `default_factory=list`, so Pydantic never marked them required, and that schema is what rides `response_format: json_schema strict` into LM Studio's **constrained decoder**. A reply containing no `shots` was legal under the grammar it was decoded with, which is why three runs of prompt wording, count enforcement, a two-stage split and a model swap moved nothing. `director.director_result_schema(require=…, min_shots=…)` builds the required set per call and `DirectorClient.plan` takes a `response_schema`; populate's shots call requires `shots` — plus `sections` exactly when the instruction asks for them — and the structure pass requires `sections` and pointedly leaves `shots` optional, because it is told to return an empty list. **`DirectorResult.shots` was deliberately *not* made required globally**: the model is shared with the Director chat, where a reply that answers a question without inventing a shot list is correct, and a test pins the chat schema so a future edit cannot harden it. **Measured, 60 s song / 12 required shots / temperature 0.7 / N=3 per arm, arms interleaved, one warm-up call discarded, the count-enforcement wording left in place on every arm:** empty `shots` on **2 of 3 answered control rolls** (old schema) against **0 of 3** with `shots` required, 0 of 3 with both required, and 0 of 3 with a `minItems` floor — twelve of twelve shots on all nine treatment rolls. Requiring both fields also delivered shots *and* sections on **3 of 3**, where the combined ask had managed that on 0 of 9 rolls across runs 1–2. **N=3 settles little** and one control call died on an unexplained provider 400 whose body was not captured; a replacement roll was run and is in the table. `minItems` **is honoured** by LM Studio's decoder (a request asking in words for two shots against `minItems: 12` returned twelve) , 4 of 4 floored calls, and is **not shipped**: the same probe shows the decoder does not enforce numeric bounds, so the forced entries arrived with `duration: 0` and `duration: 1200` and **3 of those 4 replies failed `PlannedShot` validation outright** — an unparseable reply is a 502 where a short one is a guided retry. LM Studio also accepts a partial `required` list and a missing `additionalProperties: false`, which is why nothing ever refused the broken schema. Evidence: `test-artifacts/2026-08-20-lmstudio-live/run4-*`, produced by `tests/smoke_populate_schema_live.py`. **Not exercised through the route live** — these are `plan`-shaped calls carrying the route's own assembled instruction — and the Gemma failure (run 3, 8 of 8 empty) has not been re-run under the fix
- [ ] Shot-length variance inside sections — populate currently tiles near-uniform windows; the Director wants variance ("the shots all look the exact same length"). The section tiling is the place: vary durations within the 4–6 s band per section, biased by the section's energy. Partially improved 2026-08-19 (the instruction now demands a deliberate mix; run 2's plan came back with a 4.0–5.7 s spread). **Count enforcement (2026-08-20) narrows where the variance can now come from, and does not add any**: the shot count is computed as `song / 5.2 s` and held to, so the *mean* window is pinned at the target and the only remaining source of spread is the relative shape of the model's own proposal durations inside the 4–6 s band. Whether that shape survives a model that has just been told a hard number has not been measured — it is a live-run question, and enforcing the count could plausibly flatten the spread rather than preserve it. **Revised 2026-08-20 by the schema fix below**: the count-enforcement wording's value is now *unknown* rather than unmeasured-but-plausible. Runs 1–3 attributed populate's failures to wording, but the reply the wording was being judged on frequently contained **no `shots` at all** — the strict schema never marked the field required, so there was nothing for a count constraint to grip. Every measurement of the count wording to date was taken through that hole. The wording was deliberately left in place on all four arms of run 4, so run 4 says nothing about it either; whether it does anything now needs its own one-variable run
- [ ] Two-stage populate — measured on run 2: the local model will not emit `sections` alongside a 32-shot layout in one call (three rolls, zero sections each), while it happily volunteers them in smaller replies. **Built 2026-08-20 and shipped off by default** (`two_stage` on the populate request body): a sections-first call asks for structure only and nothing else (`POPULATE_SECTIONS_INSTRUCTION`), and its repaired boxes are then *handed* to the shots call, which drops the structure request and its hard constraint entirely — the second ask genuinely shrinks rather than merely being sent twice. The structure pass gets one call and no retry, and an empty one falls back to the single-call wording, so the worst case costs one wasted call rather than a failure. **Still open because nothing has run it against a live model**: the default stays `false` (byte-for-byte the old behaviour) until a run says the split is worth a second local-model call, and until then the `sections` claim above remains the only measurement anyone has. `performance` was deliberately *not* made a retry trigger: an all-false answer is a legitimate reading of an instrumental and cannot be told from a dropout. **The premise is now evidence against itself (2026-08-20, run 4).** "The model will not emit `sections` alongside a shot layout in one call" was measured entirely against a schema that marked `sections` optional — the decoder was free to close the object after the shots, and it did, on 0-of-9 rolls across runs 1–2 and 0 of 3 again in run 4's control. With **both fields required in the schema**, the single combined call delivered shots *and* sections on **3 of 3** rolls (60 s song, 12 shots, N=3), at no cost in shots or wall clock. The split may well have been answering a schema bug rather than a model limitation. It stays shipped and stays `false`, and it has still never been run against a live model *through the route*; what has changed is that the measurement it was built on no longer supports it
- [ ] Markers and shot-range loop playback
- [ ] Thumbnail filmstrips
- [ ] Multi-select and batch operations
- [ ] Locked-field LLM change review — the lock and the single-slot recovery shipped 2026-08-17 (FR-16); what remains open is the *review* workflow: previewing or diffing a kept version before restoring it, and provenance on the slot

## Rendering and delivery

- [x] H3 text-only shot submission from a corrected selected-path adapter
- [x] H3 image/reference/audio shot submission — **verified live 2026-08-18** with one picture reference (a promoted Reference Sheet) and the project master song as the audio reference. Video references have never been submitted, and neither has a multi-picture shot
- [ ] Audio range extraction per shot
- [ ] Multiple takes and approval
- [ ] LTX 2.3/2.5 enhancement
- [ ] LTX 2.5 extension offered from the app — the adapter exists and is audited; no route submits it
- [ ] SeedVR2 and RTX VSR
- [ ] FILM interpolation
- [ ] FFmpeg song-synchronized assembly
- [x] Draft/master export presets — offline-verified 2026-08-20, plus real local ffmpeg on synthetic clips (no GPU, no ComfyUI: `comfy.prompts` asserted empty on every new path). `POST .../assemble` now takes one optional field, `preset`, a `Literal` so an unknown value is a 422 from request validation before any process exists. **`draft` is the default because `draft` is what this application has always exported** — x264 veryfast at CRF 18 on the trims, `-movflags +faststart` and no audio filter on the join — and the argv is pinned longhand three ways (no preset, the default name, the preset object), so a body-less Assemble click produces the byte-identical command it always produced. `master` moves the picture at the trim stage (x264 `slow`, CRF 16 — the join is `-c:v copy`, so a CRF there would encode nothing) and adds `aresample=48000` to the audio, riding the tail of the mix graph after `amix=…:normalize=0` when accepted take audio is present. **`master` normalizes no loudness — the Director's ruling of 2026-08-20, revised 2026-08-20 from the `loudnorm=I=-16:TP=-1.5:LRA=11` this line originally described.** The export's audio track *is* the Director's master song, so a delivery build must not re-master it; measured on synthetic media, the old pass lifted a -37.95 LUFS song to -15.96 LUFS and pulled a -8.50 LUFS one down to -16.20, and the export now measures within 0.00 LU of the song that went in. The 48 kHz conform stays on new reasoning, measured rather than inherited: `loudnorm`'s 192 kHz internal rate is no longer the cause (without it a 44.1 kHz song delivers at 44.1 kHz), but a Director-supplied 192 kHz master still lands at 96 kHz through AAC's ceiling, and the conform is level-transparent — a tone measures -27.15 LUFS on both sides of it. **The existing verification was not weakened**: the master export ffprobes at 8.000000 s against an 8.000 s song with exactly one video and one audio stream, and the loudness claim is now made on the written file against the song file, at 0.5 LU tolerance. Assembly also reports progress now — both ffmpeg stages run under `-progress pipe:1` with a **concurrent stderr drain**, and the percentage is written onto the `post` job (new defaulted `RenderJob.progress`; a manifest written before it loads unchanged, asserted through the real store). The deadlock the drain guards against **was not reproduced** — it is asserted structurally and by proving ffmpeg's stderr still reaches a failed stage's 502. Mutation pass: 34/34 killed when the presets shipped, plus 4/4 on the loudness removal (loudness re-added to `master`, the mix graph's filter separator left dangling, an empty `-af` emitted, a filter applied to `draft`), files restored byte-identically each time. **Not browser-verified**: the Assembly bar's preset select, the running percentage and the two-second progress tick have offline coverage only — the select's options are held against the route's `Literal` by a contract test, the rest is asserted on `app.js` source. Nothing records which preset made a given export beyond the response
- [ ] ffprobe verification and contact sheet

## Verification status on 2026-08-16

- First-run project creation: repaired and verified in headless Edge against an empty isolated data root; all five workspaces opened with zero severe console errors.
- Python suite: 84 passing as of this section's date. Historical — see the 2026-08-17 section for the current count.
- Music 3: prior real eight-second integrated FLAC successful in ComfyUI.
- SongPlanner invented lyrics (FR-13): adapter, route, and Song-workspace variant selector implemented 2026-08-16. `tests/preflight_songplanner.py` confirmed all 10 node classes and every model/combo value against live ComfyUI 0.33.1 `/object_info` and recorded `tests/fixtures/object_info.json`; unit tests validate payloads offline against that fixture. **Live generation verified 2026-08-17** — see the SongPlanner live smoke entry below.
- SongPlanner known lyrics (FR-14): second thin builder over the shared core, optional `lyrics` on the songplanner route, and a third Song-workspace preset implemented 2026-08-16. The audited Known_Lyrics export (SHA-256 verified) differs from the invented export only in node `45.lyrics`/preview `58.source`; unit tests assert payload equality except lyric handling and pin the lyric-handling contract exactly — the route strips leading/trailing whitespace only, and interior blank lines and indentation reach the payload unchanged. The offline `/object_info` fixture validates the known variant via the public builder. **Live generation verified 2026-08-17** — see the SongPlanner live smoke entry below.
- Flux API adapter: structurally matched to live `/object_info`; live smoke pending in this app.
- Krea: prior one-step character-sheet generation successful; current multiview adapter unit-tested; live app smoke pending. **Superseded — the first live promotion from the application ran 2026-08-18; see that section below.**
- Director compiler: `MiniMaxH3DirectorCS` is registered on 8188; app timing now uses `start`/`length`, while full media/reference conversion remains pending.
- H3 text-only adapter: **verified live end to end on 2026-08-16.** A 3.75 s / 90-frame shot at 640×384 and 4 steps was submitted from the application to ComfyUI 0.33.1, accepted with HTTP 202, executed on the RTX 5090, and returned `music-video-producer/<project>/shots/<shot>-h3_00001-audio.mp4`. `ffprobe` confirms h264 640×384, 90 frames, 3.750 s, with synchronized AAC audio at 32 kHz. Wall clock was roughly twelve minutes, dominated by loading the ~31 GB model stack; free VRAM fell from 32 GB to 1.1 GB during sampling. The job reconciled to `complete`, `latest_output` was written, and `approved_output` correctly stayed empty.
- H3 frame grid: **off-grid alignment verified live 2026-08-16.** Two 4.0 s shots (96 requested frames, off the 17k+5 grid) were submitted after the payload was changed to send `DirectorTimeline.aligned_frames`. Both rendered at exactly 107 frames, `ffprobe` duration 4.458 s. Note the consequence: an aligned clip is longer than its shot window, so assembly must trim.
- Model residency: **measured 2026-08-16.** Two identical 107-frame H3 renders submitted back to back completed in 438 s and 288 s. ComfyUI keeps the model stack resident between consecutive prompts, saving roughly 150 s per subsequent render. No warm-batching machinery is required; the requirement is only to avoid interleaving other workflow kinds into a shot batch.
- SageAttention: `sageattention` is **not installed** in ComfyUI's embedded Python, so the `PathchSageAttentionKJ` pin to `disabled` is correct rather than a leftover error. `triton` and torch 2.7.0+cu128 are present, so it is installable; whether a Blackwell-compatible build helps remains an unmeasured spike.
- Director defect sweep: seven defects found and fixed on 2026-08-16 — creative-document data loss, `queued`-while-running, silent empty shot application, unaligned frames, mixed path separators, unbounded planned shot durations, and a `ruff check .` gate broken by vendored tooling. See `docs/DEVELOPMENT-LOG.md`.
- SeedVR2: real 192-frame upscale completed at 1250×720.
- LTX: downstream VAE encode failed on width 1250. Fixed and verified live on 2026-08-17 — see the next section.

## Verification status on 2026-08-17

- Browser QA: **the Epic 2 surfaces were driven in a real browser on 2026-08-17** by `tests/e2e_epic2_surfaces.py` (headless Edge, isolated data root), closing the largest standing gap in this project's verification. Confirmed against the real DOM: the notice blocks render as elements rather than escaped text, refusal and change carry different labels and different left-edge colours, `role="note"` is present, the raw model output sits behind a disclosure that starts closed and is readable once opened, and the Director's prose survives intact beside the notices. Also confirmed: the document lock and restore controls reflect stored state and restore is disabled with nothing kept; the consent checkbox loads unticked and is cleared by a project change; the unprompted clip reads `NO PROMPT` with a dashed border while the written clip does not; the readiness region names the blocking Shot in the server's own sentence; and the Song preset drives lyrics visibility and the 30–300 s duration bounds. Zero `SEVERE` console entries. `tests/e2e_first_run.py` and `tests/e2e_audio_playback.py` also pass against the current tree.
- Browser QA, still uncovered: the expansion button and consent-clearing after a completed turn (both need a live language model), the GPU-cost confirm dialogs, song removal and replacement confirmations, the batch refusal (unreachable while nothing writes `status = "ready"`), and playback of a *generated* song (cross-origin; needs ComfyUI started with `--enable-cors-header`).
- Browser QA harness defect found and fixed: `tests/e2e_audio_playback.py` waited unconditionally for the new-project dialog, which the app opens only on an empty data root. Run in the order `docs/OPERATIONS.md` prescribes — immediately after `e2e_first_run.py`, which leaves a project behind — it timed out, so the audio gate had been silently not running.

- H3 Ultra reference pre-flight (Story 3.1): **audited live on 2026-08-17, not rendered.** `tests/preflight_h3_ultra.py` validated eight payload variants — one picture; the per-kind limits filled (9 pictures, 3 videos, 3 audios — 16 of the 18 autogrow slots, since the single paired-audio video leaves two `ref_video_audio` slots unexercised); a video with its paired soundtrack; `ref_image_size="max"`; the longest window the node's frame ceiling allows; the exact minimum and maximum of every request bound so those checks are not exercised only at a safe midpoint; and **two text-only Director variants**, which is how the one H3 path with live render evidence finally came under the same guard — against live ComfyUI 0.33.1 `/object_info` at `127.0.0.1:8188`, reporting `OK 138 nodes across 8 variants (18 classes)`. All 18 node classes are registered and all five model files (`minimax_h3_ref2va_pruned_int8_convrot`, `minimax_h3_fl2va_pruned_int8_convrot`, `qwen3vl_32b_minimax_h3_int8_convrot`, `minimax_h3_video_vae_fp16`, `minimax_h3_audio_vae_fp32`) are installed, the filenames derived from the payloads rather than restated, so a renamed loader cannot leave the check affirming a file the graph no longer loads. The adapter's own constants were checked against the schema that produced them: the 9/3/3 per-kind limits equal the `ref_images`/`ref_videos`/`ref_audios` autogrow maxima, every `mvp:split` index lands on the splitter output the schema names for that kind, the 3600-frame ceiling equals `length`'s declared maximum, and every `H3Request` bound sits inside the node's own (containment, not equality — the route is deliberately narrower). Review's central finding was that the audit could be gutted and still print `OK`: blanking the vacuous-pass guard, dropping the adapter checks, or moving the record step above the failure guard each left the suite green and the audit reporting success. All three are now driven by tests against a stubbed server, and `validate` additionally checks every link's target node and output index — the wrong-slot risk the split-offset comment names, now caught generically for every adapter. **This is a schema audit and nothing else** — no graph was submitted, so the reference path still has no live evidence and `- [ ] H3 image/reference/audio shot submission` in the Rendering and delivery section above stays unticked. **That last clause was true on the day and is now superseded**: the path rendered live on 2026-08-18 and the box is ticked. The audit itself is unchanged and re-ran clean immediately before that render.
- H3 `shift_audio` discrepancy, resolved by the schema rather than left open: the Director graph hardcodes `4` while the reference graph uses `3`. Live `/object_info` gives `MiniMaxH3DirectorCS.shift_audio` a **default of 3.0** with the tooltip "Audio flow sigma shift (H3 default 3.0)", so the reference graph matches the node's documented default and the Director graph's `4` is the deviation. It is deliberately left as-is: the text-only path is the one H3 path with live render evidence, it rendered correctly at `4`, and changing a sampling parameter on a proven path is the Director's call rather than a cleanup. The audit reads neither defaults nor tooltips, so it will never surface this by itself.
- Pre-flight validator, two corrected false failures: measured against live `/object_info`, the previous validator reported **eight** problems on a correct reference graph (three pictures and the master song) and **four** on the Director graph, plus one unchecked bound each — `crf` resolved no `min`/`max`, so its 0–100 range was never enforced. It understood neither ComfyUI's `COMFY_AUTOGROW_V3` group inputs nor `VHS_VideoCombine`'s format-conditional inputs. Both are now expanded in `tests/preflight.py`, the shared module both audits and the offline tests use; `tests/preflight_songplanner.py` still reports `OK 20 nodes across 2 variants (10 classes)`, unchanged by the extraction. Verified by mutation: disabling either expansion makes the live audit and the offline tests fail again.
- Offline schema coverage: `tests/fixtures/object_info.json` went from 10 recorded classes to 23 by **merge** — the SongPlanner classes are all still there, byte-identical — and `UNRECORDED_CLASSES` in `tests/test_workflows.py` shrank by exactly the 13 newly recorded names. `MiniMaxH3DirectorCS` deliberately stays unrecorded: no audited payload builds it.
- `shift_audio` discrepancy, reported not resolved: the Director graph sends `shift_audio: 4` and the reference graph's `MiniMaxH3SigmaShift` sends `3`. Both are inside the node's range and neither is provably wrong, so nothing was changed.

**Current suite: 1367 passing**, `ruff check .` clean, `node --check` clean on `app.js` and `api.js`, re-measured on this tree on 2026-08-21 after the timeline editing work (undo/redo, gap fill, snap to playhead, Expand All Prompts on the cuts bar, the window band on the clip) landed alongside the concurrent short-window render work (the value here read **1332** at that work's own measurement, **1226** at the LTX 2.5 extension adapter's baseline, **1205** at the H3 format checker's cut-time casing fix before it, and **1112** at the live-render-progress change before that; the tree gained concurrent work from two other streams in between). This tree is shared with concurrent work, so this figure is a measurement of the tree at one moment and not of any one change. The previous value sat here reading **545 passing** and dated 2026-08-18 while six stories landed on top of it, which is exactly the staleness the next sentence warns about. This is the single authoritative count, and it is only true as of the last edit to this line — the previous value sat here reading "measured on this tree" while it was 22 tests stale, which is exactly the failure this paragraph exists to prevent. Re-measure before trusting it. The per-change figures in the entries below are snapshots taken when each change landed and are deliberately *not* updated afterwards — several changes landed in the same shared tree within minutes of each other, so a later entry can honestly report a lower number than an earlier one. Read them as "what this change measured", never as current state.

- Python suite: **117 passing, 0 failing**, measured after the crop change landed on top of the day's three other work streams (LTX boundary repair, SongPlanner live smoke, SongPlanner request bounds). `ruff check .` clean; `node --check` clean on both `app.js` and `api.js`. The number has moved repeatedly through the day as parallel work landed — re-run the gate rather than citing this figure. Browser QA is also a required release gate; use the commands in `README.md`. Live GPU smokes are manual, gated on `--confirm-gpu`, and not part of this gate set.
- SongPlanner live smoke (FR-13 and FR-14): **both adapters verified live end to end on 2026-08-17** by `tests/smoke_songplanner_app.py`, driving the running application on port 8766 against ComfyUI 0.33.1 at `127.0.0.1:8188` with an isolated data root. The pre-flight audit passed first (`OK 20 nodes across 2 variants (10 classes)`). One project per adapter, because every `kind=music` job targets `"song"` and a shared project's second run would clobber the first. Nothing in persisted state marks a song as SongPlanner's, so this mapping is the record:
  - Invented (`build_songplanner_invented_payload`), `project_8d4fa036ab9d` / `job_7c607624bd5d`, prompt `d7f86956-243c-45bf-ad48-ddf327588d3b`, seed `20260816`, complete in 216.7 s → `music-video-producer/project_8d4fa036ab9d/songs/Invented Smoke_00001.flac`. `ffprobe`: flac, 44100 Hz, 2 channels, **29.989 s**, 2,496,336 bytes.
  - Known lyrics (`build_songplanner_known_lyrics_payload`), `project_cb0ff891977a` / `job_8c35a0c5fdf6`, prompt `d09f929e-0794-4502-889a-ee1593e76bba`, seed `20260817`, complete in 185.0 s → `music-video-producer/project_cb0ff891977a/songs/Known Lyrics Smoke_00001.flac`. `ffprobe`: flac, 44100 Hz, 2 channels, **29.989 s**, 2,617,365 bytes.
  - Requested length is not produced length: both requests asked for 30 s and `Song.duration` stores `30.0`, while the produced audio measures 29.989 s (re-probed independently at 29.988571 s). The encoder resolves its own length; record the measured value.
  - Playback evidence, stated exactly to its level: after job refresh the app points the player at the ComfyUI `/view` URL, and for both songs that URL returned HTTP 200 with a byte count matching the file on disk, and those returned bytes were themselves `ffprobe`-decodable FLAC. **A browser was not driven, so browser playback is not claimed.**
  - The epic's "≤16 s" target was **not** met and is not achievable: `M3SongPlanner.duration_seconds` carries a schema floor of `min: 30.0`, and a first attempt at 16 s was rejected by ComfyUI at prompt validation (`value_smaller_than_min`) before any GPU time was spent. The floor is recorded in the story's Spec Change Log; the epic's wording stands pending human ratification.
- SongPlanner request bounds: the rejected 16 s attempt showed that numeric schema ranges were never validated offline — the pre-flight audit checks classes, input names, and combo values, but not numeric `min`/`max`. Closed under `_bmad-output/implementation-artifacts/spec-songplanner-duration-bounds.md`, which corrected the route's request bounds to 30–300 s and the planner seed ceiling to 4294967295.
- LTX 2.5 divisor correction: the boundary patch carried `divisible_by=16`, which clears the VAE's 4-pixel patchify but leaves height 720 against a total spatial compression of 32 (720/32 = 22.5). Raised to 32 in `patch_ltx25_dimension_boundary` and in the Director's saved workflow, which had never received the fix at all. `workflow_templates/reference_exports/h3-ltx25-user-export.json` deliberately still captures the pre-fix graph — the patch is applied in memory at submission time. The ratified planning artifacts (`ARCHITECTURE-SPINE.md` AD-12, `epics.md`) were amended to 32 with the live evidence cited, closed under `_bmad-output/implementation-artifacts/spec-ltx25-dimension-boundary-repair.md`.
- LTX 2.5 boundary: **verified live end to end on 2026-08-17.** The patched reference graph (`patch_ltx25_dimension_boundary` applied in memory, divisor 32) was submitted once to ComfyUI 0.33.1 as prompt `a64a0460-64e6-4a14-b207-e644bf9bda5d`; it ran to `success` in 17 min 36 s with no errors. Measured chain, from `ffprobe` on the produced files rather than from prediction:
  - H3 base 1056×608, 192 frames, 8.000 s (`temp/minimax-h3-music/vid_00003-audio.mp4`).
  - SeedVR2 1250×720, 192 frames — the exact size that raised `einops.EinopsError` on the three previous runs (`temp/.../vid_00004-audio.mp4`).
  - LTX 2.5 output 2496×1408, 185 frames, 7.708 s (`output/minimax-h3-music/vid_00003-audio.mp4`). The LTX subgraph applies a 2× latent upsample, so 2496×1408 is exactly 2 × **1248×704** — direct measured confirmation the normalizer produced a 32-divisible size. Divisor 16 would have given 1248×720 and therefore 2496×1440.
  - FILM + RTX VSR 3744×2112, 369 frames, 48 fps, 7.688 s (`output/minimax-h3-music/vid-vfi_00001-audio.mp4`).
  - Frame count is not preserved across the boundary: 192 frames in, 185 out (8k+1), 8.000 s → 7.708 s. Assembly must account for this, as it already must for H3's 17k+5 alignment. Tracked as next-slice item 10.
  - Aspect was not preserved by this run: `keep_proportion: "resize"` resampled without cropping, stretching 1250×720 → 1248×704 by 2.07% (aspect 1.73611 → 1.77273). **Superseded — see the crop entry below.** This remains the record of what this run actually did.
  - This exercised the reference graph, which regenerates H3 from creator-specific media. **Standalone LTX submission from the application is still not implemented** and is not claimed.
- LTX 2.5 geometry — **crop, ruled by the Director, verified live 2026-08-17.** He ruled that geometry must be preserved and that trimming a few pixels is the acceptable price, so the normalizer moved from `keep_proportion="resize"` (stretch) to `"crop"` in both the repo patch and the saved workflow. Second single submission, prompt `0be806a0-4d5a-4c31-8338-7326534896ea`, `success` in **12 min 06 s** with no errors.
  - Dimensions are **identical** to the stretch run at every stage: H3 1056×608/192f → SeedVR2 1250×720/192f → LTX 2496×1408/185f/7.708 s → FILM+RTX VSR 3744×2112/369f. This is expected, not a null result — crop and stretch both output 1248×704, so **no dimension check can distinguish them**. The boundary passed `VAEEncode` again, which is what the run was for.
  - Geometry is now preserved, on two independent lines of evidence. Executing the installed `ImageResizeKJv2` on a synthetic 1250×720 frame: crop centre-crops to 1250×705 (15 rows, split 7 top / 8 bottom) then resamples 705 → 704, leaving **0.02%** residual distortion against resize's **2.07%**. And comparing frame 40 of the two live runs, the crop run's frame is best matched by a **~1.7% vertical zoom** of the stretch run's (clean unimodal correlation peak at scale 1.016–1.018, against 0.9457 at identity 1.000) — a real crop in the live graph, in the predicted direction and close to the predicted 2.13%. The A/B is approximate: the two runs' diffusion detail differs because their input pixels differ, so treat the node-level figure as the precise one.
  - Wall clock fell 17:36 → 12:06 between the two runs. Not attributable to crop; the second run reused a warm model stack, consistent with the residency measurement recorded above.
- H3/LTX full renders: the reference LTX 2.5 chain now completes; application-driven LTX submission is not complete and not claimed complete.
- FR-12 import duration (ffprobe fallback): **now regression-guarded on both sides, and the guard found a real defect.** The frontend defeated the server fallback — a failed browser decode toasted but left `state.audioBuffer` holding the *previously* loaded song, so importing an undecodable file into a project that already had a decodable song sent that song's duration, `app.py`'s `resolved_duration` saw a non-zero value, ffprobe never ran, and a wrong length was persisted as the timing spine. Fixed by clearing the buffer and the displayed duration on decode failure and by routing the sent value through a pure `songImportDuration` in `api.js`. Four new automated tests, all mutation-checked against the defect: the pure decision executed under node (decodable buffer sends its length; failed decode sends 0; failed decode with a prior buffer still sends 0), a source-level guard that the handlers drop the buffer and delegate, a route test that re-reads the manifest through a **fresh `ProjectStore`** to prove the probed duration survives restart and replaces the previous song's, and an ffprobe-unavailable test asserting a stored 0 rather than a fabricated number. Closed under `_bmad-output/implementation-artifacts/spec-1-3b-import-duration-regression-guard.md`. Scope of the claim: these are offline tests plus the existing browser QA of the decodable path — **the undecodable-file path has not been driven in a real browser**, so only the executable contract is claimed for it.
- Song replacement safety: **the epic's "no shot data is deleted" guarantee is now enforced and proven rather than holding by accident.** Every explicit Song mutation — re-import, direct Music 3, SongPlanner — goes through one guard, `_require_song_replacement_confirmation`, which returns 409 naming that shot windows are absolute seconds against the current song and that Assembly synchronization derives from it, once the project has both a Song and Shots. A first import and a Shot-less project stay frictionless. The guard runs before any work: before `_copy_upload` (songs are written without an index prefix, so a same-named file overwrites the previous audio) and before ComfyUI submission, so a refusal costs no GPU time. `DELETE /api/projects/{id}/song` is the new first-class removal path behind the same gate — it nulls `project.song`, leaves every Shot untouched, leaves the audio file on disk, and 404s when there is no song. The browser states the same consequence through one exported constant, `SONG_CHANGE_CONSEQUENCE` in `api.js`, before the import, generate and remove paths send anything, and sends the Director's actual acknowledgement rather than a hardcoded `true`. Nine new tests: five route tests that re-read the manifest through a **fresh `ProjectStore`** and compare every shot field exactly across confirmed replace, confirmed remove, and each refusal, plus assertions that the audio bytes are untouched by a refused replace and that the file survives removal; and four contract tests, including one that executes the Python and JavaScript gates over the same four project states and asserts they agree. **The full-project `PUT` was initially excluded and then closed the same day**, after review demonstrated against the live app that it removed a Song with no acknowledgement (`PUT song=None` → 200, Song null, Shot intact). It is now gated in the narrow form that preserves ordinary saves: only when the incoming Song differs from the stored one and the project has Shots, so an unchanged Song round-trips untouched. Song replacement is gated on all five Song-changing routes. Two further hardenings from the same review: an in-flight music job is matched to its Song by `prompt_id` before reconciliation (so a mid-render removal or replacement can neither misattach nor silently drop the output), and stored song filenames carry an index prefix so a same-named re-import cannot destroy the previous audio. **Still out of scope, and a different concern:** that `PUT` can wipe Shots while carrying an unchanged Song. Closed under `_bmad-output/implementation-artifacts/spec-1-3c-song-replacement-safety.md`. Scope of the claim: `uv run pytest`, `uv run ruff check .`, and `node --check` on both modules — **the new "Remove song" control and the three confirm dialogs have not been driven in a real browser**, so only the executable contract is claimed.

- Mark a shot ready: **implemented and regression-guarded offline on 2026-08-18.** The first-render path, which did not exist — `mark-ready`/`mark-draft`, bodyless, plus a shot-inspector control. Refusals: in-flight (409), already rendered (directed at render-again), locked, approved, and the prompt gate, which reuses `batch.prompt_rejection` rather than forming a second opinion that could drift. Returning to `draft` has no prompt gate, because un-committing a shot whose prompt you just emptied is the point. **Nothing auto-marks** — not the Director applying a plan, not expansion writing prompts — enforced by a test rather than by absence, since a Director running expansion to see suggestions must not silently arm a plan for rendering.
  - The story's centrepiece is the **end-to-end test**, written first and confirmed failing for the right reason. Every guard around rendering had been individually tested and each passed, while the path *through* them was never walked — which is exactly how a journey with no way to start it looked fully covered. That class of gap is what this test makes visible.
  - **Not browser-verified.** Both this control and render-again's are proven by executing `renderShotInspector` under a stub DOM, which is not a browser.
  - Still open by decision: marking every eligible shot ready in one action. Recommended shape if built — report what it *would* arm and what it would skip and why, then arm only on explicit confirm, and **never** queue as part of the same action. `batch.readiness_report` already computes the per-shot reasons and nothing surfaces them here.
- Render a shot again: **implemented and regression-guarded offline on 2026-08-18.** A purpose-built route and a shot-inspector control re-open a settled shot by writing one field, so comparing two takes no longer needs an API client. The prompt gate is asked again rather than remembered; in-flight, locked and approved shots are refused, the last because an approval would otherwise describe a take that no longer exists. **The application does not track takes** — ComfyUI's numbered outputs mean the earlier file survives on disk, but nothing records that it existed.
  - Found by this work and **fixed the same day** by the mark-ready story below: nothing in the shipped frontend ever wrote `status = "ready"`, so a `draft` shot could not reach its *first* render through the interface at all.
  - A stale vision review no longer follows a shot across takes; `Shot.latest_review` is cleared when a new output displaces the file it describes. Pre-existing, but only reachable by API client until this control existed.
- A visible control for the VRAM eject: **implemented and regression-guarded offline on 2026-08-18.** A topbar checkbox beside the ComfyUI status, applying from the next submission with no restart, reporting which models the last render released. Stored in `data/machine-preferences.json`, never in a project manifest. **The free-VRAM half of Story 4.1 was dropped**, not overlooked: across one eject of a 4.71 GB model the reading fell 31.6 → 16.0 GB because ComfyUI released its own cache concurrently, so the figure is confounded. The residency listing is the evidence.
  - Precedence: the environment decides how the application starts, the control decides what happens after. With `MVP_LLM_EJECT_BEFORE_RENDER` pinned, a change made in the interface does not survive a restart, and the control says so.
  - **Not browser-verified end to end.** The control's behaviour is proven by executing `app.js` against a stub DOM; the live sequence with a real model loaded is listed under verification below as still to run.
- VRAM eject before a render: **implemented and verified live on 2026-08-18.** The Director asked for this early in the 2026-08-17 session and it was never built — nothing in the roadmap or the deferred ledger was tracking it either, which is the more useful half of the finding. Every ComfyUI submission now asks LM Studio to release what is resident and **confirms the release by re-reading the host's own listing** rather than trusting an exit code. On by default; never fails a render.
  - Verified with a model deliberately loaded: `resident_before=('huihui-qwen3-vl-4b-instruct-abliterated',)` → `resident_after=()`, status `released`, and `lms ps` agreeing afterwards. Through the application, the eject line is logged **before** the route's 202 and the render completed normally after it.
  - The raw VRAM reading fell 31.6 → 16.0 GB across the eject, which is *more* than the model's 4.71 GB — ComfyUI released its own cache concurrently. The residency listing is the evidence; the VRAM delta is confounded and is not cited as proof.
  - **No REST unload could be committed to.** LM Studio returns HTTP 200 with an error body for every unknown path, so no read-only probe can distinguish "no such route" from "wrong method", and `lms.exe` contains WebSocket RPC strings rather than REST routes. The CLI sits behind a swappable protocol.
- Song context on import: **implemented and regression-guarded offline on 2026-08-18.** `upload_song` accepts optional `lyrics` and `caption`, a `PUT /api/projects/{id}/song/context` corrects them afterwards, and both are proven to reach `RevisingDirector.contexts[0]` rather than assumed to. No new model fields and no parsing of the sheet.
  - **Chat *and* expansion**, as of 2026-08-18. It reached the chat route only at first, and `docs/LLM-DIRECTOR.md` implied otherwise for two commits until review caught it; the Director then ratified extending it rather than narrowing the claim. `timeline.expansion_input`'s `song` block now carries both, omitted rather than emptied when absent, so a song without lyrics produces a byte-identical payload to before.
  - **Recovery slots added 2026-08-18**, after they were first declined here for a frozen "no new model fields" rule and the Director renegotiated it. `lyrics_previous`/`caption_previous` are `str | None`, deliberately unlike the documents' `str = ""`, so a displaced *blank* is still restorable. A no-op save spends no slot, each field moves independently, and slots never outlive their song. The exclusion from the Director's context is a **classification** — every `Song` field must be declared visible or withheld, and an unclassified one aborts the test suite at collection rather than leaking into every prompt.
  - A maximum sheet plus caption adds a constant **~3,050 tokens to every Director chat call**, crossing an 8,192-token window at turn 8 against turn 27 without one. Measured, not capped. It costs the same once per *expansion*, but expansion is a single stateless call so it does not accumulate there — which is exactly why expansion was extended on the same day the chat thread's unbounded growth was deliberately left alone. The Director ruled on 2026-08-18 that the thread stays uncapped; it is the only unbounded term in the prompt.
  - **Not browser-verified.** The enable-and-seed path is covered by executing `app.js` against a stub DOM under node, which is not a browser.
- H3 reference sampling profiles: **implemented and regression-guarded offline on 2026-08-18.** `default` reproduces the audited 20-step no-LoRA graph byte for byte, pinned by a digest taken from commit `7e25ad0`'s builder; `turbo` applies `minimax_h3_ref2v_turbo_4step_v0.1_comfyui_bf16.safetensors` at 0.7 with `beta`/`euler` at 4 steps, every value read out of the in-repo export by walking the model chain. A non-default profile on a text-only Shot is a 422 rather than a silent no-op. **Nothing has been rendered on the turbo profile** — see open item 5.
- Reviewable document replacement (FR-16): **implemented and regression-guarded offline on 2026-08-17.** `director_chat` assigned `project.treatment` and `project.style_bible` unconditionally and kept no copy, so a plausible-looking rewrite the Director never asked for passed `document_rejection` and was permanent — and the reply never said a document had changed at all. `Project` now carries `treatment_previous`/`style_bible_previous` (single-slot, AD-14) and `treatment_locked`/`style_bible_locked`, all defaulted so existing manifests load unchanged. The guard loop skips a locked document before calling `document_rejection`, captures the live value **only when a replacement is actually applied**, and prepends a statement naming each document that changed. `POST /api/projects/{id}/documents/{document}/restore` swaps a document with its kept version without calling the Director and refuses with 409 when nothing was kept. Suite: **136 passing** (125 at the change's baseline commit), `ruff check .` clean, `node --check` clean on both modules. Scope of the claim, stated exactly:
  - **Single-slot, by decision.** One previous version per document, no history stack, and the restore is a symmetric swap, so restoring twice returns to where you started. Two applied replacements in a row leave only the immediately previous version recoverable; the original is gone and a test asserts that.
  - The recovery slots are **excluded from the LLM context dump**, which is a correctness requirement rather than an optimisation: the recorded root cause of the original corruption was degradation under rich context, so echoing a second full copy of both documents into every prompt makes the failure the guard was written for more likely. The two lock booleans stay in context deliberately.
  - Locks stop the **Director**, not the human: `PUT /documents` still writes the text of a locked document, and it treats an omitted lock as "leave it alone" so an ordinary save can neither unlock both documents nor blank a kept version. The recovery slots are not on that request model at all.
  - `Shot.locked` enforcement in `director_chat` was previously untested and now has a route test covering start, duration and prompt.
  - FR-16's undefined locking model was resolved as per-document, human-set locks. A finer-grained or Director-settable lock is a different design and is **not** implemented. "Locked-field LLM change review" under Production editing below is still open — this ships the lock and the recovery, not a review workflow.
  - **Not driven in a real browser.** The restore buttons, the two lock toggles, and the restore toast have offline contract tests only; only the executable contract is claimed for the UI half.

- Opt-in document replacement: **implemented and regression-guarded offline on 2026-08-17.** FR-16 made an unrequested rewrite visible and reversible but not *consented* — every Director turn still replaced both documents whatever the Director actually asked for, while the cheaper artefact (the shot list) already required `apply_shots`. `DirectorRequest` now carries `apply_documents`, defaulted off, and the guard loop writes nothing without it. This change measured 188 passing when it landed, in a tree that already contained Story 2.2; see "Current suite" below for the authoritative figure. `ruff check .` clean, `node --check` clean on both modules. Scope of the claim, stated exactly:
  - **Off by default, and unremembered.** Nothing persists the flag: it is not on `Project`, and the browser control is cleared both when a turn finishes and when a project loads, so consent is neither carried into the next turn nor inherited by the next project. A client that omits the field, or sends it as `null`, is read as a decline. The cost of that safety is real — a Director who wants every turn applied must tick the box every turn — and it is the spec's open question, deliberately resolved the safe way rather than left implicit.
  - **The first cut claimed this without the code for it.** Nothing unchecked the box, so one tick applied every later turn and the box survived a project switch: the feature was "on until the Director notices" while the docs said otherwise. Found in review, fixed, and the lifecycle is now *executed* under node — tick, send, clear, read back — rather than asserted by grepping for identifiers.
  - **A declined turn's toast is decided by the reply, never by the diff.** A restore or hand edit landing during the Director call moves a document without the reply having touched it, and the toast must not credit the reply with it. It blames the opt-in only where the server actually reported a declined proposal, keyed on a phrase from the server's own notice — so the four cases the server is silent about (nothing proposed, an echo, a guard-refusable candidate, a locked document) get the neutral sentence instead of an instruction to retry a turn that would write nothing.
  - **A declined proposal is not stored.** No proposal slot, no new persisted field; the text a declined turn proposed is gone, exactly as a declined shot list's is today. The reply names *which* documents were proposed and says the text is not kept, so the Director is not led to believe it can be recovered.
  - **Locks still take precedence, and keep their own sentence.** A locked document is never written with the flag on, and with the flag off it is still reported as locked rather than merely unrequested — unticking the box must not relabel a protection the Director set as an oversight.
  - **Consent without noise.** The proposed-but-not-applied statement is emitted only when the candidate would genuinely have changed something *and* would genuinely have been applied. An echo of the current text proposed nothing, and a candidate `document_rejection` would have refused would not have landed with consent either — naming it would invite a retry that refuses identically. This is the same silence rule `DOCUMENT_LOCK_NOTICE` already carries.
  - The two flags are **independent**: `apply_shots` merges shots while documents stay untouched, and vice versa. A route test asserts both directions.
  - **Not driven in a real browser.** The composer checkbox, its clearing on send and on project load, and the declined-turn toast have offline contract tests only; only the executable contract is claimed for the UI half.

- Story expansion into fully prompted shots (FR-26): **implemented and regression-guarded offline on 2026-08-17.** Nothing turned a Treatment, a Style Bible and timed Shot windows into a render-ready prompt per Shot: `Shot.prompt` defaults to `""`, no code validated it, and the only writer was the chat route's positional shot merge. `POST /api/projects/{id}/director/expand` now makes one whole-plan `DirectorClient.expand` call and merges the returned prompts by Shot id. This change measured 184 passing when it landed; the opt-in follow-up landed in the same tree immediately after, so **see "Current suite" below for the authoritative figure** rather than reading either per-change number as current. `ruff check .` clean, `node --check` clean on both modules. Scope of the claim, stated exactly:
  - **Keyed by Shot id, never by position.** A prompt is free text, so a positional merge after a concurrent add, delete or split writes a plausible prompt onto the wrong Shot and nothing downstream fails. A route test commits a Shot insertion while the model call is in flight and asserts every prompt still landed on the Shot it was addressed to.
  - **Song-section input is an explicitly empty branch, not an analyser.** FR-26 asks for section boundaries "when analysis exists" and no analysis exists anywhere in this project — no BPM or section field on `Song`, `Project` or `Shot`, a disabled analyse-structure button, hardcoded "Not analyzed" readouts, and a `#section-track` that is never populated. `timeline.song_section` is one named empty branch; the key is **omitted** rather than sent as `""` or `"unknown"`, and a test asserts both the absence and that no data source for it exists. `song_fraction` is absent the same way when there is no Song or its duration is 0, because a fabricated 0.0 would tell the model every Shot opens the song.
  - **Nothing is rendered and nothing is retimed.** The route never touches `comfy`, never advances a Shot's `status`, and assigns only `prompt`; tests assert `comfy.prompts` stays empty and that every window is unchanged. Window checks here are **descriptive flags** on the input, not a gate — expansion writes prompts, so nothing new can fail. Prompt quality is Story 2.3's readiness gate and is deliberately not blocked on here.
  - **The model's input is purpose-built and trimmed.** `timeline.expansion_input` is pure and I/O-free and carries no `status`, `prompt_id`, `latest_output`, `latest_review` or `approved_output` — the recorded root cause of Director degradation is rich context. A test asserts the route hands that builder's output to `expand` **verbatim**, which is what makes "the input includes each Shot's position in the song" falsifiable at all.
  - **Reported, never guessed.** A returned id matching no Shot is discarded and named; a Shot the model omitted keeps its prompt and is named; a prompt that parses as JSON is refused by the same `document_rejection` check FR-15 added and named. None of the three is retried and none creates a Shot — if the Director wants omissions retried or unknown ids created, that is a different design.
  - **Not driven in a real browser.** The Treatment workspace's "Expand shots into prompts" button (`#expand-shot-prompts`, built over what was the disabled `#apply-shot-plan` stub) has offline contract tests only; only the executable contract is claimed for the UI half.

- Shot Plan readiness gate (FR-26): **implemented and regression-guarded offline on 2026-08-17.** An empty-prompt Shot reached ComfyUI and burned a full GPU pass returning noise. `generate_h3` checked only `status == "ready"` and never looked at the prompt, `compile_timeline` serialised `"prompt": ""` in silence, and nothing anywhere computed whether a plan was fit to submit or noticed that two Shots carried the same prompt. `src/music_video_producer/batch.py` now owns `readiness_report(project)` — AD-5's module, and the single implementation Epic 4's batch submission and pre-flight will call. This change measured **235 passing** when it landed, against 210 before it. `ruff check .` clean, `node --check` clean on both modules. Scope of the claim, stated exactly:
  - **Emptiness blocks; sameness warns.** Empty or whitespace-only prompts are refused and named; two prompts identical after lowercase and whitespace collapse, or sharing more than 90% of their tokens (Jaccard, so a short prompt contained in a long one is not scored as a perfect duplicate), are reported and the plan stays submittable. Sameness will never block: a repeated beat can be a deliberate choice, and a gate that refuses one is a gate that gets worked around.
  - **The guard reads `shot.prompt`, at the route, before the payload.** The reference branch builds `f"Reference map: {tags}. {shot.prompt}"`, so an empty prompt arrives downstream as a populated sentence and every truthiness check past that line passes. A test replaces both payload builders with a function that raises and asserts the 422 still comes back, on the text branch *and* on the reference branch, in the same words.
  - **Not keyed to `Shot.status`.** Nothing in the shipped frontend ever writes `ready` — new Shots and duplicates are `draft` — so a status-coupled readiness gate would be unreachable. The readiness refusal is also raised *before* the existing `status != "ready"` check, so an unprompted draft is told the real problem rather than told to change its status.
  - **Derived, never stored.** No new `Shot` field, no persisted flag; `readiness_report` is pure and a test asserts the manifest is unchanged after a report and after the route is called. A stored flag is a second source of truth that goes stale the instant a prompt is edited. `flagged` state is a later decision and is not implemented here.
  - **Best-effort against a half-submitted batch, not a guarantee.** The queue-ready handler asks the server for readiness once, before its loop, and refuses the whole batch naming the blocking Shots — a per-Shot refusal discovered mid-loop would leave the earlier Shots already queued. Only Shots actually in the batch can block it; a blank draft elsewhere in the plan cannot. The GPU-cost confirm was moved after the check, so nobody is asked to accept a cost for a batch that was never going to be sent. It cannot close the window between the check and the last submission, and a direct API caller gets per-Shot semantics only — a server-side batch endpoint (AD-5, Epic 4) is what makes this an invariant.
  - **The batch guard is currently unreachable in the shipped interface.** `#queue-ready` is enabled only by a count of Shots with `status == "ready"`, and nothing in the shipped frontend ever writes that status — new Shots and duplicates are `draft`. So the button the guard protects cannot be enabled without a hand-rolled client. The gate on the route is what actually protects a submission today; the batch check is groundwork for Epic 4's batch submission.
  - **One refusal wording per layer.** `batch.READINESS_REFUSAL` and `api.js`'s `READINESS_REFUSAL` are asserted identical under node, and a contract test enumerates every `comfy.submit` call site in `app.py`, pins the set, and asserts each Shot-sourced one guards before it submits and before the reference interpolation.
  - **The dry run reports and does not block.** `compile_timeline` queues nothing and costs nothing, so it carries a `readiness` block in its response and still compiles an unprompted plan. This was the spec's one open question, resolved as report-not-block: it is the one cheap way to see what a plan would serialise, and what it serialises for a blank Shot is exactly what is worth seeing.
  - **No pre-flight modal.** That surface is Epic 4 (Story 4.2); this ships the backend gate it will consume. What the browser gained is the batch check and one timeline signal: an unprompted clip renders `NO PROMPT` in place of the `Untitled shot` fallback — which a real prompt of that name also renders — plus a dashed border, so the state is never carried by colour alone.
  - **Not driven in a real browser.** The `NO PROMPT` clip flag, the batch refusal toast and the readiness route have offline contract tests only; only the executable contract is claimed for the UI half.

- Director safety notices carried as data (FR-15, FR-17): **implemented and regression-guarded offline on 2026-08-17.** Every protective refusal this project produces was concatenated into the assistant message as plain text after a `\n\n---\n` separator, so a refusal read exactly like a normal reply, nothing rendered it distinctly, and no test touched the thread render at all. Worse, the document-rejection notice embedded up to 400 characters of the model's degraded output directly in `content` — and the chat thread is shipped back to the model as context on the next turn, so the guard that exists to stop "JSON in context begets JSON" was the thing feeding it. `TreatmentMessage` now carries `notices: list[MessageNotice]`, each a display sentence plus the raw model output it is about, and `DIRECTOR_CONTEXT_EXCLUDE` drops the whole list from the prompt. This change measured **271 passing** when it landed, against 262 before it. `ruff check .` clean, `node --check` clean on both modules. Scope of the claim, stated exactly:
  - **The raw output is inspectable and absent from the prompt, and that is asserted against the recorded context.** Two route tests refuse a document (chat) and a prompt (expansion), then take a further chat turn and read the `project_context` the double was actually handed — the whole blob, a fragment of it, and the `notices` key itself are all absent, while the reply that carried the refusal is still there. The containment check walks the payload's strings rather than searching `json.dumps`, because `json.dumps` escapes the quotes degraded model output is made of: the old assertion could not have failed for the text it existed to catch.
  - **The list is excluded whole rather than the `raw` field inside it.** Every notice's sentence is already in `content`, so keeping the structured copy would echo a second copy of each notice into the prompt, and a nested exclusion path stops covering a field that is later renamed or added beside it.
  - **The expansion route's refused prompt is restored.** Story 2.2 deliberately dropped it because there was nowhere to put it the model would not read back; the excluded field is that place, and `EXPANSION_REJECTED_NOTICE`'s final sentence — which claimed the text was not recorded — was rewritten rather than left false.
  - **The renderer never splits on `---`.** The Director's own prose can contain that sequence and so can the raw output a notice embeds, which would split one notice into two blocks with the model's degraded text presented as a protective refusal. `messageParts` strips the exact tail the server joined, reconstructed from the notices, and returns the whole message as prose when it cannot account for that tail — printing a notice twice is a visible defect, swallowing half a refusal is not. `content` still carries the joined text: it is what every saved project holds and what two marker-scanning client helpers read.
  - **FR-15's prose-claims-Shots clause is now implemented**, and it was the one clause with no code at all. A reply whose prose describes a shot plan while the structured list is empty is reported as a mismatch. It is ungated on `apply_shots`, which the browser hardcodes to `false` — gating it there would put the notice where no Director can reach it, which is what happened to the empty-shot-list notice. Detection is two narrow patterns: the word itself, and a counted structure, because the recorded 2026-08-16 defect never said "shot" — it said "a four-beat sequence", and a check that misses the one recorded instance is not a check. It reports and never blocks; with no shots there is nothing to apply.
  - **The sub-40% collapse floor is now exercised through the route.** It had four unit tests against `document_rejection` and no request had ever reached it, because every existing double either echoed the stored document or returned a long replacement.
  - **Unmissable means labelled, not merely coloured.** Each notice renders in its own block with an amber left edge — the existing `--amber` caution token, no new colour — and a text label above the sentence, with the raw output behind a `<details>` collapsed by default. State is never carried by colour alone.
  - **The rendering is executed, not grepped.** The splitter and the markup builder are pure exports run under node against a reply the live route really produced — with the model's own prose containing the separator, and its rejected output containing a `<script>` tag. The test reads the HTML that would reach the browser: two blocks, one label each, one closed disclosure, the script tag escaped, and each notice printed exactly once. Substring assertions over `app.js` were what let three UI guarantees invert with a green suite in Story 2.3.
  - **Twenty-four mutations reintroduced; all twenty-four caught on the first attempt, every file restored byte-identical.** Among them: the exclusion dropping `notices`; the raw field emptied on either route; the raw pasted back into `content`; the mismatch notice deleted or gated on `apply_shots`; the claim patterns narrowed to the literal word or widened to always fire; the consent notice suppressed by the mismatch; the reply built without the shared helper; the collapse floor ratio zeroed; `notices` made required so old manifests fail to load; the splitter searching for the separator or leaving the tail in; the label dropped; the disclosure opened by default; the raw interpolated unescaped; notices rendered before the prose; the thread reverting to escaping `content` whole; the amber becoming a literal hex; a later rule reclaiming the edge; the notice paragraph rule dropped; and `_raw_excerpt` left uncapped.
  - **Not driven in a real browser.** The notice block, its label, the amber edge and the raw disclosure have offline contract tests only; only the executable contract is claimed for the UI half.

## Verification status on 2026-08-18

**The reference path works end to end and now has live evidence. Whether it delivers character consistency is not yet demonstrated.** Epic 3's goal is "a character recognizably the same person across the Shots of a finished video, judged by the Director's eye"; this run earned the first half of that — the mechanism — and none of the second. Read the two entries below together, because the acceptance criteria are met and the picture is not usable.

- H3 Ultra reference path (Story 3.2): **rendered live end to end on 2026-08-18** through the running application against ComfyUI 0.33.1 at `127.0.0.1:8188`, app on port 8766 with an isolated data root, project `project_9d237e8fbddb`. `tests/preflight_h3_ultra.py` ran first and reported `OK 138 nodes across 8 variants (18 classes)`. Two GPU jobs were spent, in that order, by `tests/smoke_h3_reference_app.py`.
  - **The promotion, and the first live multiview from this application.** `Lucy-Metal.png` was uploaded as `kind=character` and stored at `media/assets/000-Lucy-Metal.png`; job `job_cab46ecd37f5`, prompt `1471fa58-24a9-4810-8de7-de7c0861f7b1`, seed `20260818`, complete in **56.1 s**. The child Asset `asset_d0486f608572` carries `source="krea-multiview"` because the route produced it, and its `path` was populated to `music-video-producer/project_9d237e8fbddb/assets/asset_d0486f608572-multiview_00001_.png` by the ordinary `read_job` reconciliation rather than by hand — which is the evidence promotion works end to end, since until that happens the sheet cannot be attached at all. Verified on disk at **2,025,707 bytes, 1536×1024**. Opened and looked at: it is a genuine four-view character sheet — face plate, front, profile, back — with wardrobe, palette and identity consistent across the views.
  - **The reference render.** Shot `shot_f5a5b9f4ab72` with the promoted sheet attached and `use_song_audio`; job `job_cc558fd0ff89`, prompt `6dbe4ff7-08d2-468c-bd5b-8dce37bd68fd`, seed `20260819`, complete in **136.5 s**. Requested 640×384, 4 steps, `duration=3.75` → **90 frames, no grid padding**. `ffprobe`: h264 640×384, `nb_frames` **90**, 24/1 fps, video **3.750 s**; aac **32000 Hz** stereo, audio **3.744 s**; container 3.750 s, **561,942 bytes**. Duration delta from the request: **0.000 s**. Audio/video sync delta: **-0.006 s**.
  - **Approval stayed manual, as required.** `latest_output` was set to the `-audio.mp4` and `approved_output` stayed **empty**; the Shot reconciled to `complete`.
  - **The output trap, measured rather than assumed.** The render left three files on disk (`.png`, `.mp4`, `-audio.mp4`) but the job's `output_files` carried **only** the `-audio.mp4`, so the file the smoke chose by name and the file `latest_output` received were the same one. The smoke picks by name anyway and records whether they agreed, because `output_files[0]` is not a promise.
  - **The node trims the audio reference to the render window.** This was an open unknown before the run: the **154 s** master song was attached whole with `use_song_audio` and the route passes the entire file with no offsets, yet the emitted audio measures **3.744 s**. So a long master does not force the node to encode the whole track.
- **The render is not usable output, and no line above should be read as saying it is.** At **4 steps** the frame is heavily degraded. That is expected at a deliberately minimum-cost setting chosen to prove the path for the least GPU time, and it is not a defect — but nothing here demonstrates picture quality, and nothing here demonstrates that the Reference Sheet produced a recognizable character.
- **An unresolved observation from the first frame, recorded rather than explained.** The Reference Sheet's four-panel layout appears to have carried into the *composition*: the face plate is visible on the left, the vertical panel bands persist, and a figure stands inside one band. The model appears to have treated the sheet's layout as scene structure rather than purely as identity reference. **This is not resolved and no cause is asserted here.** Two factors plausibly contribute and neither is established: 4 steps is far below anything usable, and `ref_image_size` was `"match"` (the default), so the 1536×1024 sheet was conditioned at its own aspect. Whether a production step count, `"max"` sizing, or a single-view reference changes it is **untested**. Filed in `_bmad-output/implementation-artifacts/deferred-work.md`.
- **A second, cheaper consistency layer landed on 2026-08-20 and is also undemonstrated.** `Asset.consistency_prompt` — the appearance anchor — carries the Director's own words for what an asset looks like into the reference map's tag lines, the H3 expansion specialist's per-reference block and the assistant's library, where a bare asset *name* used to be. It is user-owned, wins over any generated or vision-derived description, and nothing infers it. Offline-verified only: 24 cases, 22/22 mutations killed, byte-identity held for every anchor-free project. **No render was submitted against an anchor and no live model has seen the persona rule**, so nothing here moves the Epic 3 verdict above — it adds a lever, not evidence. See `docs/DEVELOPMENT-LOG.md`, 2026-08-20, "A name is not a description".
- Reference smoke: `tests/smoke_h3_reference_app.py` is the reusable evidence. It refuses to submit without `--confirm-gpu`, spends **two** GPU jobs, and stages every piece of state through shipped routes — no hand-written manifest. Before the live run its refusal paths were driven under a socket guard that fails if anything opens a connection, and its staging, aborts and measurement were driven against a stubbed transport and against the real app on an isolated port, so nothing about the sequence was first tried on the GPU. `probe` in `tests/smoke_songplanner_app.py` now reports the video stream alongside the audio one, from one implementation shared by both smokes.
- Suite re-measured on this tree after the docs landed: **337 passing**, unchanged by this story — the deliverable was a manual smoke script, which is not pytest-collected. `ruff check .` clean.

Update this document whenever a live verification changes readiness.
